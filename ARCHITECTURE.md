# KeviN — คู่มือระบบ

เอกสารนี้ตอบคำถามเดียว: **ระบบทำงานยังไง** อ่านจบแล้วแก้ได้เลยโดยไม่ต้องไล่โค้ดเอง

> [CLAUDE.md](CLAUDE.md) = กติกาการทำงาน · [doc/DECISIONS.md](doc/DECISIONS.md) = ทำไมถึงเป็นแบบนี้
> [doc/TRAPS.md](doc/TRAPS.md) = ของที่พังเงียบ ๆ · [doc/HISTORY.md](doc/HISTORY.md) = เดินมายังไงและเหลืออะไร

---

## 1 · KeviN คืออะไร

ผู้ช่วยส่วนตัวของนักศึกษา เก็บ **ตารางเรียน · งาน · การเตือน · โน้ต** ไว้ที่เดียว
แล้วเปิดสามทางให้เข้าถึง

| ประตู | ใช้ตอนไหน | เขียนข้อมูลได้ไหม |
|---|---|---|
| **Claude ผ่าน MCP** | นึกอะไรออกระหว่างเดิน โยนเข้าระบบเร็ว ๆ | ✅ เต็มที่ |
| **เว็บแอป PWA** | นั่งวางแผน ดูปฏิทิน ติ๊กงาน | ✅ เต็มที่ |
| **ผู้ช่วยในแอป `/kevin`** | ถามเร็ว ๆ ระหว่างเดิน ทั้งพิมพ์และพูด | ⚠️ **เสนอได้ · ผู้ใช้กดยืนยันถึงเขียน** |

**ทั้งสามประตูอ่านตารางเดียวกัน ไม่มี sync ไม่มีข้อมูลสองชุด** — การตัดสินใจแกนกลาง
ที่ทำให้ไม่ต้องมีตรรกะรวมข้อมูลเลย

**ใช้ได้หลายคน · ข้อมูลแยกกันเด็ดขาดด้วย RLS** (8 ก.ย. 2026) — ทุกตารางมี `user_id`
และ policy `own_rows` ตั้งแต่วันแรกทั้งที่ตอนนั้นมีผู้ใช้คนเดียว การเปิดจึงไม่ต้อง
แตะโครงข้อมูลเลย · สมัครต้องมี**รหัสเชิญที่ใช้ได้ครั้งเดียว** ซึ่งบังคับด้วย trigger
ที่ DB ไม่ใช่ที่หน้าเว็บ · ผู้ใช้ใหม่ได้สี่ Area ตั้งต้นอัตโนมัติในธุรกรรมเดียวกัน
· **ประตู MCP เป็นของเจ้าของคนเดียวโดยธรรมชาติ** เพราะเป็น OAuth ส่วนตัวเข้า Supabase

ของจริงอยู่ที่ https://kevin-rose.vercel.app

---

## 2 · ภาพรวม

```
                    ┌──────────────────────────┐
                    │   Supabase (Postgres)    │
                    │   RLS ทุกตาราง            │
                    │   CHECK บังคับความหมาย    │
                    └────┬──────────┬──────────┘
             anon key    │          │  service_role (เฉพาะเซิร์ฟเวอร์)
             + RLS       │          │
              ┌──────────┘          └───────────┐
              │                                 │
   ┌──────────┴─────────┐            ┌──────────┴──────────┐
   │  เว็บ Next.js 16   │            │  Edge Function       │
   │  บน Vercel (hnd1)  │            │  send-reminders      │
   └────────────────────┘            └──────────┬──────────┘
                                                │ pg_cron + pg_net ทุกนาที
   ┌────────────────────┐            ┌──────────┴──────────┐
   │  Claude + MCP      │            │  Web Push → มือถือ   │
   └────────────────────┘            └─────────────────────┘

   ── ประตูที่สาม · อ่านได้ · เสนอได้ · เขียนเองไม่ได้ ──────────
   /kevin ┬─ แชต  → /api/chat ──────────► Gemini Flash-Lite
          │                       │
          └─ โทร  → เบราว์เซอร์ ───┼──ws──► Gemini Live
                    ต่อตรง          │       (ephemeral token)
                                    ▼
                               runTool()
                                    │  อ่าน · และเรียกชั้นเสนอ
                                    ▼   Supabase (anon + RLS เดิม)

   propose_* คืน "ร่าง" ─► การ์ดบนจอ ─► ผู้ใช้กดยืนยัน
                                          │
                                          ▼
                            lib/applyDraft.ts ─► Supabase
                                  (จุดเดียวที่เขียนได้)
```

**หลักที่ยึด:** กติกาทุกข้อบังคับที่ **ระดับฐานข้อมูล** ไม่ใช่ที่แอป
เพราะมีสองประตูเขียนข้อมูล ถ้าบังคับที่แอปจะต้องเขียนกฎซ้ำสองที่แล้วหลุดแน่นอน

---

## 3 · โครงสร้างข้อมูล

```
Area (Class / Competition / Personal / General)
└── Project (ใน Class เรียก "วิชา")
    ├── project_schedules   ช่วงเวลาที่ซ้ำรายสัปดาห์ · ไม่แจ้งเตือน
    │   └── time_offsets ↘  ตัดทอนรายวัน — ชี้ได้ทั้งคาบเรียนและกิจกรรม
    ├── events              กิจกรรมครั้งเดียว · มีชื่อของตัวเอง · ขึ้นปฏิทิน · ไม่แจ้งเตือน
    │   └── event_agenda    กำหนดการข้างในงาน · ไม่ขึ้นปฏิทิน
    └── items
        ├── task        มีสถานะเสร็จ · due_at ไม่บังคับ
        ├── reminder    remind_at บังคับ · push เข้ามือถือ · เตือนครั้งเดียว
        └── shortnote   ไม่มีเวลา ไม่มีสถานะเสร็จ
```

### เส้นแบ่งสามเส้น — พร่าเมื่อไหร่ ข้อมูลจะไปอยู่ผิดตาราง

| ถ้าสิ่งนั้น… | ลงที่ |
|---|---|
| ซ้ำทุกสัปดาห์ | `project_schedules` |
| เกิดครั้งเดียว มีช่วงเวลา ต้องขึ้นปฏิทิน | `events` |
| เป็นรายละเอียดภายใน event เดียว | `event_agenda` |

**ห้ามใส่ `week_offsets` ลง `events`** และ **ห้ามให้ `event_agenda` ขึ้นปฏิทิน**

`time_offsets` เก็บ **คำตัดสินของเจ้าของ เฉพาะวันเดียว** — "ไม่ไปคาบนี้" หรือ "อยู่ถึงแค่ 11:00"
ชี้ไปที่ `schedule_id` หรือ `event_id` อย่างใดอย่างหนึ่ง · **เวลาจริงของมหาลัยและของผู้จัดไม่ถูกแตะเลย**
คืนค่า = ลบแถวทิ้ง

> ⚠️ **`week_offsets` ไม่ใช่ที่เก็บ "ฉันจะไม่ไป"** — มันแปลว่า *สัปดาห์นั้นไม่มีคาบ*
> เอาไปใช้แทนกันให้ผลบนจอที่ถูกต้องเป๊ะแต่ทำลายข้อมูลถาวร (ดู `doc/TRAPS.md`)

`items.event_id` ผูกงานเข้ากับกิจกรรมได้ (ไม่บังคับ) · ผูกได้จาก**หน้า event ที่เดียว**
DB บังคับด้วย foreign key **สองคอลัมน์** `(event_id, project_id)` ว่าต้องอยู่ project เดียวกัน

### ทำไม task/reminder/shortnote อยู่ตารางเดียวกัน

เพราะ 80% ของฟิลด์ซ้ำกัน และหน้าจอส่วนใหญ่ต้องดึงมารวมกันเรียงตามเวลาอยู่แล้ว
แยกตารางแปลว่าต้อง `UNION` ทุก query · **ราคาที่จ่ายคือต้องมี CHECK constraint กันข้อมูลผิดรูป**

| ชื่อ | บังคับอะไร |
|---|---|
| `reminder_needs_time` | reminder ต้องมี `remind_at` |
| `reminder_no_due` | reminder ห้ามมี `due_at` |
| `task_no_remind` | task ห้ามมี `remind_at` (อยากเตือนให้สร้าง reminder แยก) |
| `shortnote_timeless` | shortnote ห้ามมีเวลาและสถานะเสร็จ |
| `notified_reminder_only` | `notified_at` ใช้กับ reminder เท่านั้น |
| `sched_time_order` | เวลาเลิกต้องหลังเวลาเริ่ม |
| `sched_start_is_mon` | `start_date` ต้องเป็นวันจันทร์ |
| `sched_weeks_present` | ต้องมีอย่างน้อย 1 สัปดาห์ |
| `sched_weeks_valid` | ห้ามมี NULL และห้ามติดลบ |

> ⚠️ สองข้อสุดท้ายเคยเขียนผิดจน**ไม่ได้กันอะไรเลย** เพราะ `NULL` ใน CHECK ถือว่าผ่าน
> `array_length('{}',1)` คืน NULL ไม่ใช่ 0 · ต้องคง `coalesce(...)` และ
> `array_position(..., null) is null` ไว้

### การซ้ำของช่วงเวลา — รูปแบบเดียว

```
start_date    = วันจันทร์ของสัปดาห์ที่ 0
week_offsets  = [0,1,2,...] สัปดาห์ที่มีคาบนี้
```

UI มีสองโหมด (`ซ้ำอีก N สัปดาห์` กับ `เลือกสัปดาห์เอง`) แต่**ลงมาที่รูปแบบนี้ทั้งคู่**

`start_time`/`end_time` เป็น `time` เปล่า **ไม่ใช่ timestamptz** โดยตั้งใจ —
"09:00 ทุกวันจันทร์" คือเวลาท้องถิ่น ไม่ใช่จุดเวลาจุดเดียว

---

## 4 · ฟังก์ชันใน DB สามตัวที่ต้องรู้

### `schedule_occurrences(from, to)`

คลี่ `week_offsets` ออกเป็นวันที่จริง · `security invoker` จึงเคารพ RLS
**ทุกที่ที่ต้องรู้ว่าคาบไหนตกวันไหน ต้องเรียกตัวนี้ ห้ามคำนวณเอง**

> ฝั่งเว็บมี `web/lib/weeks.ts` ที่คำนวณแบบเดียวกันสำหรับ "ตัวอย่างสด" ตอนกรอกฟอร์ม
> และมีเทสต์เทียบกับผลจาก DB จริง — แก้ตัวใดตัวหนึ่งต้องแก้อีกตัวด้วย

### `calendar_entries(from, to)`

รวม **คาบเรียน + event** เป็นรูปเดียว · `security invoker`
**ทุกที่ที่ต้องรู้ว่าช่วงนั้นติดอะไร ต้องเรียกตัวนี้** — มีสามที่ที่กินข้อมูลชุดนี้
(hero หน้าแรก · ปฏิทินเดือน · ปฏิทินสัปดาห์) ถ้ารวมฝั่งเว็บต้องเขียนตรรกะเดียวกันสามรอบ

event ที่ข้ามเที่ยงคืนถูก**หั่นเป็นบล็อกรายวัน**ที่นี่ เพราะปฏิทินวางบล็อกด้วย `grid-row = วัน`
`ศ 20:00 → ส 09:00` กลายเป็น `ศ 20:00–24:00` + `ส 00:00–09:00`

> `end_time` เป็น `'24:00:00'` ได้ ซึ่ง `new Date()` แปลงไม่ได้ — ใช้ `entryEndMs()`
> ใน `web/lib/calendar.ts` เท่านั้น

คืนสองคอลัมน์ที่ฝั่งเว็บต้องอ่าน — **`skipped`** (ตั้งใจไม่ไป) และ **`trimmed`** (เวลาถูกตัด)
**แถวที่ `skipped` ยังอยู่ในผลลัพธ์** ไม่ถูกกรองทิ้ง · เว็บทำให้จางลงและติดป้ายแทนการซ่อน
เพราะถ้าหายไปเลยจะไม่มีทางรู้ว่าเคยตัดสินใจไว้ หรือกดผิดแล้วคาบหายไปเฉย ๆ
· การ์ดใหญ่และจุดบนช่องวันในโหมดเดือนไม่นับแถวที่ `skipped`

### `claim_due_reminders()`

อ้างสิทธิ์ reminder ที่ถึงเวลา **แบบ atomic**

```sql
update items set notified_at = now()
 where type='reminder' and notified_at is null and remind_at <= now()
returning *;
```

> ⚠️ **ห้ามแยกเป็น SELECT แล้วค่อย UPDATE เด็ดขาด** — cron รันทุกนาที
> ถ้ารอบก่อนยังไม่จบ จะส่ง push ซ้ำ
>
> ⚠️ เป็น `security definer` จึงข้าม RLS · **สิทธิ์เรียกถูก revoke จาก `anon`/`authenticated` แล้ว**

---

## 5 · เว็บแอป (`web/`)

Next.js 16 · App Router · ไม่ใช้ Tailwind (ใช้ CSS custom property ตรง ๆ)

### หน้าจอ

| path | คือ | หมายเหตุ |
|---|---|---|
| `/login` | เข้าสู่ระบบ · สมัคร | อีเมล + รหัสผ่าน · สมัครต้องมี**รหัสเชิญ** และ**ยืนยันอีเมล**ก่อนใช้ |
| `/` | วันนี้ | hero คาบถัดไป (**ข้ามวันได้**) + รายการเรียงเวลาของวันนี้ |
| `/calendar` | ปฏิทิน | เปิดมาเป็นเดือน · `?mode=week` ได้ |
| `/library` | คลัง | กล่อง Area · กิจกรรมที่จะถึง · ทางเข้าของที่เก็บไว้ · **ปุ่ม `+ Area` มุมขวาบน** |
| `/library/[areaId]` | หน้า Area | **ไม่มี `loading.tsx`** โดยตั้งใจ — เงื่อนไขที่ทำให้ morph ติด · ปุ่ม **แก้ไข Area** มุมขวาบน |
| `/library/archive` | ของที่รอถูกลบ | เรียงตาม**เวลาที่เหลือก่อนถูกลบ** ไม่ใช่เวลาที่เก็บ · ไม่มีปุ่มลบ |
| `/project/[id]` | หน้าวิชา | งาน/เตือน/โน้ต แยกกลุ่ม |
| `/project/[id]/schedule` | ตัวจัดการช่วงเวลา | สองโหมดการซ้ำ + ตัวอย่างสด |
| `/project/[id]/event/[eventId]` | หน้ากิจกรรม | **เป็น route ไม่ใช่ overlay เพราะต้องมี URL ให้ Claude ส่งลิงก์ได้** |
| `/kevin` | ประตูที่สาม | **เปิดมาที่โหมดโทรเสมอ** · สลับไปแชตได้ มีผลแค่ครั้งนั้น |
| `/kevin/history` | ประวัติการคุยย้อนหลัง | **อ่านอย่างเดียว ไม่มีการ์ดร่าง** |
| `/settings` | ตั้งค่าแจ้งเตือน | ขอสิทธิ์ + ปุ่มทดสอบ · อีเมลที่ล็อกอินอยู่ + **ออกจากระบบ** ล่างสุด |

แผงรายละเอียดและเพิ่มเร็วเป็น **overlay** ไม่ใช่ route — ลึกเกินสามชั้นให้เปิดทับ

### ลากจัดลำดับ — ที่จับโผล่เฉพาะที่ลากแล้วอยู่จริง

`ItemList` รับ `movable` ต่อแถว · ที่จับแสดงเฉพาะแถวที่ **`sort_order` เป็นตัวตัดสินลำดับจริง**

| กลุ่มในหน้าวิชา | เรียงด้วย | ลากได้ |
|---|---|---|
| โน้ต | `sort_order` ล้วน | ทั้งกลุ่ม |
| งาน | เสร็จ → `due_at` → `sort_order` | เฉพาะที่ยังไม่เสร็จและไม่มีวันกำหนด |
| เตือน | `remind_at` ล้วน | ไม่ได้ |

ถ้าเปิดให้ลากในกลุ่มที่ `due_at`/`remind_at` มาก่อนใน comparator **ลากแล้วจะเด้งกลับ
ตอนโหลดใหม่** ดูเหมือนบั๊กทั้งที่เขียนลง DB สำเร็จ · แถวที่ลากได้ต้องอยู่ติดกันเป็นบล็อกเดียว

ใช้ Pointer Events ไม่ใช่ HTML5 drag-and-drop เพราะ HTML5 ใช้ได้แต่เดสก์ท็อป

### hero "คาบถัดไป" มองข้ามวัน

`/` ดึงคาบล่วงหน้า **14 วัน** แล้วเลือกคาบแรกที่ยังไม่จบ ซึ่งอาจไม่ใช่ของวันนี้
ต้องเผื่อยาวขนาดนี้เพราะตารางเว้นสัปดาห์สอบกลางภาคกับ Commencement Week
**รายการข้างล่าง hero ยังกรองเหลือเฉพาะวันนี้** · ตอนคำนวณเวลาต้องใช้ `o.occurs_on` ไม่ใช่ `dateKey`

### ไฟล์ที่มีเทสต์ล้วนกำกับ — แก้แล้วต้องรันเทสต์

```bash
npm test --prefix web        # 416 เคส · ไม่ถึงวินาที
```

| ไฟล์ | ทำอะไร | เคส |
|---|---|---|
| `lib/time.ts` | เวลาไทยทั้งหมด · ป้ายบอกเวลา | 44 |
| `lib/weeks.ts` | ตรรกะการซ้ำ + คลี่เป็นวันที่ | 28 |
| `lib/applyDraft.ts` | **การเขียนจริง** — นับแถว · การย้อน | 24 |
| `lib/ai/tools.ts` | ชั้น tool อ่านอย่างเดียว | 26 |
| `lib/ai/propose.ts` | ชั้นเสนอ — ร่างที่ไม่แตะ DB | 24 |
| `lib/parse.ts` | ตีความบรรทัดเดียวเป็น item (ภาษาไทย) | 21 |
| `lib/chat/markdown.ts` | แกะ markdown ของผู้ช่วย | 19 |
| `lib/libraryOpen.ts` · `lib/ai/propose.ts` (แก้ร่าง) | สถานะกางของคลัง · `propose_update_draft` | 18 · 18 |
| `lib/ai/prompt.ts` | prompt ของประตูแอป | 16 |
| `lib/agenda.ts` | เติมเวลาจบของกำหนดการที่เว้นว่าง | 15 |
| `lib/archive.ts` | ของที่รอถูกลบ · วันที่เหลือก่อนหาย | 14 |
| `lib/ai/lang.ts` · `lib/voice/mic.ts` | ล็อกภาษา · เลือกไมค์ | 12 · 12 |
| `lib/calendar.ts` · `lib/cardFlight.ts` | เวลาบล็อกปฏิทิน · การ์ดที่บินไป | 11 · 11 |
| `lib/chat/image.ts` | รูปที่แนบในแชต — ด่านตรวจ · ย่อขนาด · ป้าย · เพดานรูปที่ค้างบนจอ | 14 |
| `lib/layout.ts` · `lib/chat/quota.ts` | บล็อกที่เวลาชนกัน · แยก 429 เป็นวัน/นาที | 10 · 11 |
| `lib/ai/voices.ts` · `lib/chat/links.ts` · `lib/chat/stream.ts` | เสียง · กันลิงก์ปลอม · ต่อชิ้นสายคำตอบ | 9 · 9 · 9 |
| `lib/eventOrder.ts` · `lib/voice/transcript.ts` · `lib/drafts.ts` | ลำดับกิจกรรม · ไม่บันทึกเสียง · ฟิลด์เวลาของร่าง | 8 · 8 · 8 |
| *(ด่านกันเขียน)* | อ่านซอร์ส `lib/ai/**` หาทางเขียนที่หลุดเข้ามา | 6 |

> ⚠️ `lib/applyDraft.ts` **นำเข้าแบบ relative ไม่ใช่ `@/`** เหมือนทุกไฟล์ที่เทสต์เอื้อมถึง
> — ชุดเทสต์รันด้วย node ตรง ๆ ซึ่งไม่รู้จัก path alias ของ bundler

### ชุดวัดความแม่นกับโมเดลจริง — `web/test/accuracy/` · **ไม่ได้อยู่ใน `npm test`**

เทสต์ข้างบนวัด**ตรรกะ** · ชุดนี้วัด **โมเดล + prompt + ชั้นเสนอ รวมกัน** ซึ่งเป็นที่
ที่ตรรกะถูกทุกบรรทัดแต่ผลรวมยังผิดได้ · ยิง Gemini จริงทั้งสองโหมด

```bash
npx tsc -p tsconfig.test.json --prefix web
node --env-file=.env.local .test-build/test/accuracy/run.js          # ทั้งสองโหมด
node --env-file=.env.local .test-build/test/accuracy/run.js chat 05,13   # เฉพาะบางข้อ
```

ใช้ **ข้อมูลปลอมที่ตรึงไว้** (ของจริงเปลี่ยนทุกวัน เทียบข้ามวันไม่ได้) และ **ป้อนข้อความ
แทนเสียง** จึงไม่ได้วัดคุณภาพการถอดเสียง · ชื่อวิชาสองอันแรกตั้งให้คล้ายกันเพื่อดักการเดา
· **ผลปัจจุบัน 5 ก.ย. 2026: แชต 17/17 · โทร 7/7**

> ⚠️ **แก้ `lib/ai/prompt.ts` หรือ `lib/ai/propose.ts` เมื่อไหร่ ให้รันชุดนี้ก่อนและหลัง**
> การจูนที่แก้เคสเป้าหมายได้แล้วทำเคสข้างเคียงพัง มองไม่เห็นถ้าวัดแต่เคสที่ตั้งใจแก้
>
> ⚠️ **429 ของ Gemini เป็นได้ทั้งเพดานต่อนาทีและโควตารายวัน** — รันรวดเดียวรอบแรก
> ได้ 4/17 โดย 13 ข้อล้มด้วย 429 แล้วยิงข้อเดียวใหม่ทันทีกลับผ่าน · ชุดวัดเว้นจังหวะ
> 6 วินาทีระหว่างข้อและถอย 20 วินาทีเมื่อเจอ 429 แล้ว · `lib/chat/gemini.ts`
> ยังแปลง 429 ทุกแบบเป็น "โควตาของวันนี้หมดแล้ว" ซึ่ง**บอกผู้ใช้ผิด** — ยังไม่ได้แก้

**ทำไมต้องคอมไพล์ก่อนรัน** — `tsconfig.test.json` คอมไพล์เป็น CommonJS ลง `.test-build/`
เพราะ type stripping ของ node ทำงานในโหมด ESM ซึ่งบังคับให้ `import` ต้องมีนามสกุลไฟล์
แต่โค้ดในแอปเขียนแบบ bundler · ผลพลอยได้คือเทสต์ถูกตรวจ type ด้วย

### จังหวะหน้าจอ — สี่ข้อที่แก้กันมาหลายรอบ

**แสงพื้นอยู่ที่ `html` ไม่ใช่ที่ `.wrap`** พื้นของ root element ถูกส่งไปวาดบน canvas
ของ viewport จึงไม่ถูกจับใน snapshot ของ view transition · ตอนอยู่ที่ `.wrap` มันโดน
cross-fade ทุกครั้งที่เปลี่ยนหน้าแล้วสว่างวาบ (ต้องมี `background-attachment: fixed` คู่กัน)

**แถบล่างมี `view-transition-name: nav`** และปิด animation ของ old/new ทิ้ง
ทุกหน้าเป็น `force-dynamic` Next จึงเรียก `startViewTransition` **สองครั้งต่อการกดหนึ่งครั้ง**
· **ชื่อห้ามซ้ำกันในหน้าเดียว** ซ้ำแล้วเบราว์เซอร์ยกเลิกทั้งชุดเงียบ ๆ

**หน้า `/kevin` ใช้ `name="talk"` ซึ่งสลับทันทีไม่จาง** โครงร่างเหมือนของจริงทุกพิกเซล
· **ภาพเก่าต้อง `opacity: 0`** ไม่ใช่แค่ `animation: none` — สองภาพที่วาดทับกันทำให้
เงาเรืองโปร่งแสงบวกกันจนสว่างเป็นสองเท่า

**ห้องโทรมีคลื่นตอนเข้าห้อง** เล่นบน `VoiceCall` ไม่ใช่ในโครงร่าง เพราะเซิร์ฟเวอร์
ตอบเร็วกว่าคลื่นมาก มันจึงโดนตัดกลางคันเสมอถ้าอยู่ฝั่งโครงร่าง

**`components/Reveal.tsx` ครอบทุก `page.tsx` และ `loading.tsx`** — ถ้าเพิ่มหน้าใหม่
ต้องครอบทั้งคู่ ไม่งั้นหน้านั้นจะกระพริบต่างจากที่อื่น · **ลบ `loading.tsx` ของหน้าไหนทิ้ง
ไม่ได้แปลว่าหน้านั้นไม่มีโครงร่าง** มันจะตกไปใช้ `app/loading.tsx` ของ root ซึ่งผิดกว่าเดิม

---

## 6 · ประตูที่สาม — ผู้ช่วยในแอป (`/kevin`)

แชตกับโหมดโทร · **อ่านได้ทั้งหมด และเสนอการแก้ได้ แต่เขียนเองไม่ได้**
ใช้ Gemini free tier จึงไม่มีค่าใช้จ่ายรายเดือน · เรื่องการเขียนอยู่ที่ **§7**

**เสียงไม่ใช่ระบบใหม่ เป็นอีกช่องทางของสมองตัวเดียวกัน** — สองโหมดใช้โมเดลคนละตัวและ
ต่อคนละแบบ แต่เวลาอ่านข้อมูลจริงลงมาที่ `runTool()` ตัวเดียวกัน · prompt เดียว บุคลิกเดียว
ประวัติกองเดียว · **สองโหมดเขียนลงบทสนทนาเดียวกัน** วางสายแล้วเปิดแชตพิมพ์ต่อได้เลย
โดยมันจำว่าเมื่อกี้คุยอะไร

### สองช่องทาง ชั้น tool ชุดเดียว

| | โหมดแชต | โหมดโทร |
|---|---|---|
| โมเดล | `gemini-3.1-flash-lite` | `gemini-3.1-flash-live-preview` |
| ต่อยังไง | เบราว์เซอร์ → `/api/chat` → Google | **เบราว์เซอร์ ↔ Google ตรง** ผ่าน WebSocket |
| ยืนยันตัวตน | cookie เดิม | **ephemeral token** ที่ล็อก config ไว้แล้ว |
| tool รันที่ไหน | ในเซิร์ฟเวอร์เอง | **เด้งกลับมาที่เบราว์เซอร์** แล้วยิงเข้า `/api/read/*` |
| ใช้จริง/วัน | ~20–50 ครั้ง | ~3–5 สาย |

เสียงต้องต่อตรงเพราะ Vercel เป็น serverless ถือ WebSocket ยาว ๆ ไม่ได้ · แต่ API key
ลงเบราว์เซอร์ไม่ได้ — ephemeral token แก้ทั้งสองข้อพร้อมกัน

**Live API รัน tool ที่ฝั่ง client** — โมเดลส่ง `BidiGenerateContentToolCall` มาแล้ว
เราต้องเรียกเองและส่ง `FunctionResponse` กลับ · ทางที่ง่ายกว่าคือให้เบราว์เซอร์ถาม
Supabase ตรง ๆ **ซึ่งห้ามทำ เพราะการตรวจอินพุตและการแปลงรูปอยู่ใน `runTool()`** · รอบ tool
ระหว่างสาย ≈ **150–250 ms** จากไทย (เบราว์เซอร์ → Vercel โตเกียว → Supabase โตเกียว → กลับ)

> ⚠️ tool ต้องประกาศตอน**เปิด session** และ**แก้กลางสายไม่ได้**

### โควตา — คนละรุ่นเพราะคิดแยกรายโมเดล

free tier คิดโควตาแยกรายโมเดล จงใจให้สองช่องทางใช้คนละตัว —
**คุยเสียงจนหมดโควตาวันนั้น แชตยังใช้ได้ปกติ** และกลับกัน

| เพดาน | ค่าที่ให้มา | เราจะใช้ | ชนไหม |
|---|---|---|---|
| สายพร้อมกัน | 3–5 | 1 | ไม่มีทาง |
| คำขอต่อวัน | 250–1,500 | ~5 | ห่างสองหลัก |
| โทเคนต่อนาที | 250,000 | ~3,000 | ต้องคุย 80 สายพร้อมกัน |
| **ความยาวสาย** | **15 นาที** | คุยยาวได้ | ⚠️ **ข้อเดียวที่ต้องเขียนโค้ดรับมือ** |

### สายเสียง — สิ่งที่ต้องรู้

```
token หนึ่งใบ ────────────── ใช้ได้ 30 นาที ──────────────►
[ สายที่ 1 · 15 นาที ]───handle───[ สายที่ 2 · ต่อด้วย handle เดิม ]
```

- **สายถูกตัดที่ 15 นาที** ตายตัว · ต่อใหม่ด้วย `sessionResumption` handle แบบที่ผู้ใช้ไม่รู้สึก
- **เก็บ handle ไว้ตลอดสาย ไม่ใช่ตอนใกล้ครบ 15 นาที** — เน็ตหลุดกลางทางก็ต่อด้วยอันเดียวกัน
- token อายุ 30 นาที **ยาวกว่าสายหนึ่งเส้นเท่าตัว** ต่อรอบแรกจึงไม่ต้องขอใหม่
- **ตัวจับเวลาห้ามรีเซ็ตตอนต่อสายใหม่** ไม่งั้นผู้ใช้อ่านว่าสายหลุด ทั้งที่บทสนทนาต่อเนื่อง
- **สายอยู่ที่ `components/CallProvider.tsx` ระดับ layout** ไม่ใช่ในหน้า `/kevin`
  ถ้าเก็บไว้ในหน้า พอสลับไปดูปฏิทินกลางสาย คอมโพเนนต์ถูก unmount แล้วสายตายทันที
- **ล็อกหน้าจอ = วางสายให้เลย** เบราว์เซอร์หยุดเสียงอยู่แล้ว ค้างไว้จะได้สายที่ตายแต่ดูเหมือนยังอยู่
- **สลับไปแชตขณะคุยอยู่ สายไม่ตัด** · แถบ `กำลังคุย 03:12 · แตะเพื่อกลับ` ตามไปทุกหน้า
- **ต้องกด "เริ่มโทร" ก่อนเสมอ** ไมค์ไม่เปิดเองตอนเข้าหน้า — กดพลาดแล้วเสียงในห้องเรียน
  วิ่งถึง Google ทันที · และ `AudioContext` ต้องเริ่มจาก user gesture อยู่แล้ว

### สามชั้นที่กันการเขียน

| ชั้น | อยู่ที่ | กันยังไง |
|---|---|---|
| 1 | `lib/ai/tools.ts` | ไม่มี tool ที่เขียนอยู่ในชุดเลย **โมเดลเรียกไม่ได้เพราะไม่มีชื่อให้เรียก** |
| 2 | `lib/ai/db.ts` + `/api/read/*` | `ReadOnlyDb` **ไม่มีเมธอดเขียน** โค้ดที่พยายามเขียนคอมไพล์ไม่ผ่าน |
| 3 | `test/guard.test.ts` | ด่านอัตโนมัติ — อ่านซอร์สจริงแล้วพังถ้ามีทางเขียนโผล่เข้ามาใน `lib/ai/**` |

**ชั้นที่ 3 ไม่ใช่ role ของ Postgres โดยตั้งใจ** ด้วยเหตุผลสามข้อ

1. **สถาปัตยกรรมไม่เอื้อ** — JWT ใบเดียวถูกใช้ทั้งแอป ตอนกดติ๊กงานกับตอนผู้ช่วยอ่านปฏิทิน
   ใช้ token ใบเดียวกัน · แยกสองกรณีนี้ต้องมินต์ JWT ใบที่สองเอง
2. **มันเก็บภาษีทุก migration ในอนาคต** — ตารางใหม่ลืมเพิ่ม policy = ผู้ช่วยมองไม่เห็น
   **โดยไม่มี error** แล้วตอบว่า "ไม่มีข้อมูล" อย่างมั่นใจ · เป็นความพังแบบ*ถูกบนจอ ผิดข้างใน และเงียบ*
3. **มันกันได้แค่ประตูเดียว** — ประตู MCP เขียนได้เต็มที่ด้วย SQL ดิบอยู่แล้วโดยตั้งใจ

สิ่งที่กลัวจริง ๆ คือ "อนาคตมีคนเผลอเติมทางเขียนเข้ามาในชั้น tool" ซึ่ง `guard.test.ts`
จับได้ตรงจุดในราคาเศษเสี้ยว

> ⚠️ **ยอมรับตรง ๆ ว่านี่ไม่ใช่การันตีของ DB** ถ้าวันหนึ่งมีคนแก้โค้ดพร้อมแก้เทสต์ ก็ทะลุได้

### `runTool()` — ประตูเดียวที่ข้อมูลออกไปหาโมเดล

ลำดับตายตัว: **ตรวจชื่อ → ตรวจอินพุต → ดึงข้อมูล → กรอง Area → แปลงรูป**

> เคยมีขั้น **กรอง Area** คั่นระหว่างดึงข้อมูลกับแปลงรูป และ `areaOf` เป็นฟิลด์บังคับ
> ของทุก tool เพื่อให้ลืมไม่ได้ · ถอดออกทั้งกลไกเมื่อ 8 ก.ย. 2026 (doc/DECISIONS.md)
> · `hidden` ยังอยู่ในรูปผลลัพธ์แต่**เป็น 0 เสมอ** เพราะกติกาข้อ 4 ใน `prompt.ts`
> ยังอ้างถึงมัน และการแตะ prompt ต้องรันชุดวัดกับโมเดลจริงซึ่งกินโควตา

**`tool ล่ม` ห้ามกลายเป็นรายการว่าง** — ถ้าคืน `rows: []` แทน error โมเดลจะพูดอย่างมั่นใจ
ว่า "พรุ่งนี้ไม่ติดอะไรเลย" ทั้งที่แค่ query ไม่ผ่าน · **คำโกหกที่แพงที่สุดที่แอปนี้พูดได้**

### เส้นแบ่งข้อมูล

**ไม่มีตัวกรองแล้ว** — `VISIBLE_AREAS` ถูกถอดทั้งกลไกเมื่อ 8 ก.ย. 2026 ตอนเปิดให้ใช้
หลายคน เพราะมันเป็น allowlist ที่ผูกกับ **ชื่อ Area** ซึ่งตอนนี้ผู้ใช้แก้เองได้
(เปลี่ยนชื่อแล้วผู้ช่วยตาบอดทั้งด้านโดยไม่มี error) · เหตุผลเต็มอยู่ใน `doc/DECISIONS.md`

| สิ่งนั้น | ข้ามไป Google ไหม |
|---|---|
| เสียงที่พูด · ข้อความที่พิมพ์ | **ข้าม** — เป็นอินพุตของโมเดลโดยตรง |
| ตารางเรียน · กิจกรรม · งาน · โน้ต **ทั้งหมดของผู้ใช้คนนั้น** | **ข้าม** — เป็นผลของ tool ที่โมเดลต้องใช้ตอบ |
| ข้อมูลของผู้ใช้คนอื่น | **ไม่ข้าม** — RLS กันไว้ที่ระดับ DB ผู้ช่วยเห็นเฉพาะของ session ปัจจุบัน |
| รหัสผ่าน · session · key | **ไม่ข้าม** — ไม่เคยอยู่ในเส้นทางนี้ |

> ⚠️ **ราคาที่แท้จริงของคำว่าฟรี ไม่ใช่โควตา** — free tier ของ Gemini ระบุว่าเอาสิ่งที่ส่งไป
> ไปพัฒนาผลิตภัณฑ์ · ตอนมีผู้ใช้คนเดียว เจ้าของยืนยันแทนตัวเองได้ว่าไม่มีข้อมูลสำคัญ
> · **ตอนนี้สิ่งที่ถูกส่งไปเป็นของผู้ใช้แต่ละคน ซึ่งเป็นเรื่องที่เจ้าของยืนยันแทนเขาไม่ได้**
> ทางกลับมาถ้าเปลี่ยนใจคือคอลัมน์ `areas.ai_visible` + สวิตช์ในแผงแก้ไขด้าน
> ซึ่งรักษาความหมายเดิมของ allowlist ไว้ครบ (ด้านใหม่ถูกซ่อนไว้ก่อน)

> ⚠️ **โควตา Gemini เป็นของทั้งโปรเจกต์ร่วมกัน ไม่ใช่ของรายคน** และตั้งใจไม่ใส่เพดาน
> รายคน (เจ้าของเคาะ) · คนเดียวคุยรัวจนหมดโควตาวันนั้น คนอื่นใช้ไม่ได้ทั้งวัน
> — สิ่งที่บรรเทาคือ `lib/chat/quota.ts` ที่บอกความจริงว่าชนเพดานต่อนาทีหรือหมดจริง

### ภาษาและเสียง

**ภาษาค่าเดียว** (ไทย/อังกฤษ) ใช้ทั้งฟังและตอบ · บังคับได้แค่ที่ระดับ prompt เพราะ
**Live API ไม่รับ language code เลย** — เป็นแรงกด ไม่ใช่การล็อก · จุดที่พังคือตอนฟังไม่ชัด
มันไปเดาเป็นภาษาอื่นแทนที่จะถามซ้ำ

**เสียง 9 ตัวเลือก** แถวแรกคือ *ค่าตั้งต้น* ที่แยกตามภาษา (ไทย `Charon` · อังกฤษ `Fenrir`)
แถวที่เหลือใช้เสียงนั้นทั้งสองภาษา · ที่ต้องแยกเพราะโมเดล**ทับเสียงที่เราตั้งในบางภาษา**

ปุ่มฟังตัวอย่างเปิดสาย Live API จริงสั้น ๆ ไม่ใช้ TTS — TTS ให้เสียงไม่เหมือน Live
ถ้าให้ฟังจาก TTS ผู้ใช้จะเลือกเสียงจากสิ่งที่ไม่ใช่สิ่งที่ได้ยินตอนโทรจริง

**ทั้งแผงถูกล็อกระหว่างกำลังคุย** — ภาษาและเสียงถูกล็อกไปกับ token ตั้งแต่เปิดสาย
เปลี่ยนกลางสายไม่มีผล ล็อกไว้ดีกว่าให้กดแล้วเงียบ · **ยกเว้นตัวเลือกไมค์**
ซึ่งไม่ได้ผูกกับ token จึงเปลี่ยนได้ตลอด

### โหมดแชตไหลทีละคำ

`/api/chat` ตอบเป็น **สาย NDJSON** ไม่ใช่ก้อนเดียวตอนจบ · หนึ่งบรรทัดคือหนึ่งเหตุการณ์
(`delta` · `reset` · `draft` · `done` · `error`) · ตัวต่อชิ้นอยู่ที่ `lib/chat/stream.ts`

⚠️ **`delta` ยังไม่ผ่านด่านตรวจลิงก์** — ลิงก์ถูกหั่นข้ามก้อนได้ ตรวจทีละชิ้นจึงไม่มีทางถูก
หน้าจอต้องเอา `reply` ใน `done` ไป**แทน**ข้อความที่ไหลมาทั้งหมด

⚠️ **`reset` มีไว้เพราะโมเดลพิมพ์แล้วเปลี่ยนใจไปเรียก tool ได้** · สิ่งที่ไหลไปในรอบนั้น
ไม่ใช่คำตอบ ต้องสั่งให้จอทิ้ง · การ์ดร่างถูกส่งทันทีที่เสนอ ไม่รอคำตอบจบ

### แนบรูปได้ในโหมดแชต · **รูปไม่ถูกเก็บที่ไหนเลย**

ถ่ายกระดานแล้วส่ง → ผู้ช่วยอ่านสิ่งที่เขียนอยู่ → **เสนอเป็นร่างให้กดยืนยัน**
ไม่ใช่บรรยายรูปให้ฟัง · ลงล็อกกับ `propose_*` ที่มีอยู่แล้ว ไม่ได้สร้างทางเขียนใหม่เลย

```
เลือกรูป → เบราว์เซอร์วาดใหม่ (ย่อ ≤1600px · JPEG · EXIF หายทั้งก้อน)
        → /api/chat  parts: [inlineData, text]
        → โมเดลอ่าน → propose_* → การ์ดยืนยัน (เส้นทางเดิมทุกขั้น)
        → ประวัติลง "[รูป] ข้อความที่พิมพ์"   ← ตัวรูปไม่ถูกเก็บ
```

| ข้อ | เป็นแบบนี้เพราะ |
|---|---|
| **แชตอย่างเดียว** | Live API เป็นคนละเส้นทาง · และการยกมือถือขึ้นถ่ายระหว่างคุยไม่ใช่ท่าที่คนทำจริง |
| **ไม่เก็บรูป** | ไม่ต้องมี bucket · ไม่ต้องมี RLS · ไม่ต้องมีสายพานลบ · `/kevin/history` ไม่ต้องแก้ · เหตุผลเดียวกับที่ `tool_calls` ไม่เก็บผลลัพธ์ |
| **แต่รูปยังอยู่บนจอจนปิดหน้า** | จังหวะที่ต้องดูรูปมากที่สุดคือ**ตอนทานการ์ด** ซึ่งเกิดหลังกดส่ง · เก็บไว้ในหน่วยความจำแท็บ 3 ใบล่าสุด แตะดูเต็มจอได้ · **ป้าย `[รูป]` อยู่คู่กับรูปเสมอ** เพื่อบอกล่วงหน้าว่ามันไม่ได้ถูกเก็บ |
| **วาดใหม่ฝั่งเบราว์เซอร์** | ได้ขนาดที่เล็กลง **และ EXIF หายไปด้วย** — พิกัด GPS ที่ถ่ายไม่ควรเดินทางไปพร้อมภาพกระดาน |
| **กติกาข้อ 12 อยู่ใน prompt ของแชตเท่านั้น** | prompt ฝั่งโทรไม่เปลี่ยนสักตัวอักษร ชุดวัดฝั่งนั้นจึงถอยหลังไม่ได้จากการแก้นี้ |

> ⚠️ **ประวัติที่ส่งกลับเข้าโมเดลเป็นข้อความล้วนเสมอ** — รูปของเทิร์นก่อนไม่เคย
> เดินทางกลับเข้าไป · นี่คือสิ่งที่ทำให้ "ไม่เก็บรูป" เป็นจริงตลอดสาย ไม่ใช่แค่
> ตอนไม่ลง DB · ถ้าประวัติพารูปกลับเข้าไปทุกรอบ มันจะถูกส่งไป Google ซ้ำทุกครั้ง
> ที่ผู้ใช้พิมพ์ต่อ · กติกาข้อ 12 จึงต้องบอกด้วยว่า **รูปที่ขึ้นเป็น `[รูป]` มองไม่เห็นแล้ว**
> ไม่งั้นโมเดลจะเดาว่าในรูปมีอะไรจากบริบทที่เหลืออยู่ ด้วยความมั่นใจเท่าเดิม

> ⚠️ **ด่านตรวจชนิดและขนาดอยู่ที่ `/api/chat` ไม่ใช่ที่หน้าจอ** — การย่อขนาด
> ฝั่งเบราว์เซอร์เป็นเรื่องของความเร็ว ไม่ใช่เรื่องของการกัน

> ⚠️ **ราคาที่ต้องบอกให้ครบ** — รูปถึง Google และ free tier เอาไปพัฒนาผลิตภัณฑ์
> · ภาพถ่ายต่างจากข้อความตรงที่**มักติดของที่ไม่ได้ตั้งใจส่ง** (หน้าคนอื่นในห้อง
> ชื่อในสไลด์ บัตรนักศึกษาบนโต๊ะ) · คำเตือนจึงอยู่**ตรงจุดที่แนบรูป** ไม่ใช่ในหน้าตั้งค่า

### สิ่งที่พูดระหว่างสายไม่ถูกบันทึก

เก็บเฉพาะสิ่งที่ KeviN ตอบ · ฝั่งผู้ใช้ลงเป็น `- voice -`
บังคับ **สองชั้น** — ฝั่งเบราว์เซอร์เพื่อให้สิ่งที่เห็นตรงกับสิ่งที่เก็บ และฝั่งเซิร์ฟเวอร์
ใน `/api/voice/transcript` เพื่อบังคับต่อให้โค้ดฝั่งจอถูกแก้ในอนาคต

ที่ยังลงแถวไว้แทนที่จะตัดทิ้ง เพราะยังเห็นจังหวะว่าคุยกันกี่รอบ
· **คำบรรยายบนจอขึ้นเฉพาะฝั่ง KeviN** — เห็นคำที่ตัวเองเพิ่งพูดไม่ได้ช่วยอะไร
และถ้าถอดเสียงผิดจะอ่านว่าระบบฟังผิดทั้งที่มันฟังถูก

### ประวัติย้อนหลัง

`/kevin/history` เห็นทุกบทสนทนาที่ยังไม่ถูกล้าง · **อ่านอย่างเดียว ไม่มีการ์ดร่าง**
เพราะร่างมีอายุแค่หน้าจอตอนนั้น · ทางเข้าอยู่เหนือปุ่มล้างในแผงตั้งค่า —
การเห็นว่ามีอะไรอยู่ต้องมาก่อนการลบถาวรเสมอ · ปุ่มล้าง **ลบจริง ไม่เก็บเข้าคลัง**
และมีชั้นกันพลาดคือแตะสองครั้ง

### ตารางที่เพิ่มมา

```
conversations (started_via · title · started_at · ended_at)
messages      (role · content · via)      ← via = ข้อความนี้มาจากแชตหรือสาย
tool_calls    (name · input · ok · rows_out · rows_hidden · error)
```

`tool_calls` **เก็บอินพุตแต่ไม่เก็บผลลัพธ์** — ผลลัพธ์คือข้อมูลที่อยู่ในตารางอื่นอยู่แล้ว
เก็บซ้ำได้สำเนาชุดที่สองที่เก่าลงเรื่อย ๆ และขยายพื้นที่ที่ข้อมูลส่วนตัวไปโผล่ได้

### เมื่อของพัง

| อาการ | ผู้ใช้เห็นอะไร | ทางถอย |
|---|---|---|
| โควตาเสียงหมด | เปิดสายไม่ได้ | สลับไปโหมดแชต — คนละโควตา |
| สายครบ 15 นาที | **ไม่เห็นอะไรเลย** | ต่อด้วย resumption handle |
| เน็ตหลุดกลางสาย | เสียงเงียบไปครู่หนึ่ง | handle เดิม ต่อได้เลย |
| token หมดอายุ | ต่อสายใหม่ไม่ติด | ขอใบใหม่ที่ `/api/voice/token` |
| **tool ล่ม** | โมเดลบอกว่าดึงข้อมูลไม่ได้ | คืน error ให้โมเดล **ห้ามคืนค่าว่างเงียบ ๆ** |
| ไมค์ถูกปฏิเสธ | ปุ่มเป็น "อนุญาตไมค์" | ขอตอนกด ไม่ใช่ตอนเข้าหน้า |

---

## 7 · ผู้ช่วยเสนอ ผู้ใช้ยืนยัน

> **โมเดลไม่เขียน โมเดลเสนอ**

```
พูด/พิมพ์ → โมเดลเรียก propose_* → ชั้น tool คืน "ร่าง" (ไม่แตะ DB)
          → เว็บวาดการ์ด → ผู้ใช้กดยืนยัน → lib/applyDraft.ts เขียนจริง
```

**จุดเดียวที่เขียนได้อยู่หลังปุ่มที่ผู้ใช้กด** · ชั้น tool ยังไม่มีทางเขียนเหมือนเดิม
`test/guard.test.ts` จึงยังผ่านโดยไม่ต้องแก้ — **วันไหนต้องแก้เทสต์นั้นเพื่อให้ผ่าน
แปลว่าเดินผิดทางแล้ว**

### ทำไมต้องยืนยัน ไม่ให้เขียนตรง ๆ

| เหตุ | รายละเอียด |
|---|---|
| ASR ไทยพลาดที่ตัวเลข | "ส่งวันที่ 4" กับ "วันที่ 14" ต่างกันหนึ่งพยางค์ · รหัสห้อง "E11-S604" ยิ่งหนัก |
| Live API ไม่มีจังหวะทาน | โมเดลตอบเร็วมาก กว่าจะรู้ว่าผิดก็เขียนไปแล้ว |
| ของจริงมีผลจริง | นี่คือตารางเรียนจริง ผิดแล้วไม่ใช่ข้อมูลเพี้ยน แต่**พลาดส่งงาน** |
| ห้องเรียนมีเสียงรบกวน | จุดที่จะใช้เสียงมากที่สุดคือจุดที่ ASR แม่นน้อยที่สุด |

### ขอบเขต — แบ่งตามความเสี่ยง

| ระดับ | การกระทำ | ให้ทำไหม |
|---|---|---|
| ย้อนได้ทันที | ติ๊กเสร็จ · เลิกติ๊ก · เลื่อนเวลา · เปลี่ยนชื่อ | **ได้** · ยืนยัน 1 ครั้ง + แถบเลิกทำ |
| สร้างของใหม่ | เพิ่มงาน · เตือน · โน้ต · กิจกรรม | **ได้** · ยืนยัน 1 ครั้ง |
| ย้ายที่ | ย้าย item ข้าม project | **ได้** · ย้ายกลับได้ |
| เก็บเข้าคลัง | `archived_at` ของ item/event | **ได้** · สายพานลบ 7 วัน กู้คืนได้ |
| **ทำลาย** | ลบ project · ลบถาวร · แตะ `project_schedules` | **ไม่ให้เลย · ไม่มี tool ให้เรียก** |

**เส้นที่ไม่ข้าม** — `projects` มี `on delete cascade` ลบวิชาเดียวแล้ว items หายทั้งกอง
คำสั่งเสียงที่ฟังผิดคำเดียวไม่ควรมีอำนาจนั้น แม้จะมี confirm · ส่วน `project_schedules`
เป็นโครงที่ทุกอย่างอ้างอิง แก้ผิดหนึ่งแถวแล้วผิดทั้งเทอมโดยไม่มีอะไรฟ้อง

### tool ชุดเสนอ

| tool | คืนอะไร |
|---|---|
| `propose_add_item` | ร่างงาน/เตือน/โน้ต — ชื่อ · project · เวลา · ชนิด |
| `propose_edit_item` | ร่างการแก้ — id เดิม + ฟิลด์ที่เปลี่ยน + **ค่าเดิมคู่กัน** |
| `propose_complete_item` | ติ๊กเสร็จ/เลิกติ๊ก |
| `propose_archive_item` | เก็บเข้าคลัง |
| `propose_add_event` | ร่างกิจกรรมครั้งเดียว |
| `propose_update_draft` | **แก้ร่างใบเดิม** ด้วยเสียง ("เปลี่ยนเป็นวันศุกร์") |

`propose_update_draft` ไม่ประกอบร่างเอง แต่เรียก `build` ของ tool ตัวเดิมใหม่ทั้งใบ
การตรวจทุกข้อจึงเกิดซ้ำครบ · ร่างค้างเดินทางไปกับ tool จากเบราว์เซอร์ เพราะร่างไม่ได้ลง DB

**ทุกตัวคืน `draft` ที่มี `id` ชั่วคราวของตัวเอง** เพื่อให้ปุ่มยืนยันชี้ถูกใบตอนมีหลายใบซ้อนกัน

**สิ่งที่ร่างต้องมีเสมอ** — ชื่อ project แบบเต็มไม่ใช่ id · เวลาแบบอ่านออก (`พฤ 4 ก.ย. 23:59`
ไม่ใช่ ISO) · ค่าเดิมคู่กับค่าใหม่สำหรับการแก้ · ประโยคที่ทำให้เกิดร่างนี้

### การ์ดอยู่ตรงไหน

| ที่ไหน | ใช้แบบ | เพราะ |
|---|---|---|
| ระหว่างสาย · โหมดแชต | **07** ในกล่องคำบรรยาย / ในฟองข้อความ | คำพูดกับสิ่งที่เสนออยู่ด้วยกัน · ทรงเดียวกันทั้งสองโหมด เรียนรู้ครั้งเดียว |
| กด "แก้" | **10** ครึ่งจอล่าง | ตอนนั้นการทานสำคัญกว่าการเห็นปุ่มวางสาย |
| ออกจากหน้าโทรทั้งที่ยังค้าง | **02** แผ่นเลื่อนขึ้นจากล่าง | หน้าอื่นไม่มีปุ่มวางสายให้บัง · มีปุ่ม "ไว้ก่อน" ที่ยุบเป็นแถบบาง |
| เสนอหลายใบ | **06** การ์ดซ้อน (`DraftStack.tsx`) | เห็นใบเดียวเต็ม ๆ + "อีก N ใบรออยู่" · สูง 220px บนจอ 360×640 |

**สามอย่างที่ห้ามโดนบังระหว่างสาย** — ปุ่มวางสาย · ตัวจับเวลา · วงเสียง
(อย่างหลังคือสิ่งเดียวบนจอที่บอกว่าไมค์ยังทำงาน)

> ⚠️ **07 สูงกว่าทุกแบบ** — ต้องคุมด้วย `max-height` + ให้ตัวร่างเลื่อนในตัวเอง
> ไม่ใช่ปล่อยให้ฉากยืด · วัดบนจอ 360×640 ทุกครั้งที่แตะความสูงของการ์ด
>
> ⚠️ **ไม่มีปุ่ม "ยืนยันทั้งหมด"** ทั้งที่ในภาพร่าง 06 มี — การยืนยันของที่ยังไม่ได้อ่าน
> ขัดกับเหตุผลทั้งหมดที่ทำให้ระบบนี้มีการ์ดยืนยันตั้งแต่แรก · ใช้ปุ่มก่อนหน้า/ถัดไปแทน

### สิ่งที่ต้องระวัง

**ตัวที่กันว่าร่างชี้ไปของคนอื่นไม่ได้คือ RLS ไม่ใช่โค้ด** — query ใน `applyDraft`
วิ่งด้วย session ของผู้ใช้คนนั้น วิชาของคนอื่นจึงคืนค่าว่างเหมือนวิชาที่ไม่มีอยู่จริง
· เคยมีด่านตรวจ Area ขาเข้าซ้อนอยู่ด้วย ถอดออกพร้อมกับตัวกรองทั้งกลไก

**ร่างมีอายุ · เก็บในหน่วยความจำฝั่งเว็บ ไม่ลง DB** — ร่างที่ค้างข้ามวันไม่ควรยืนยันได้อีก
เพราะบริบทหมดอายุ · หายไปพร้อมรีเฟรชคือพฤติกรรมที่ถูก · ร่างค้างตอนวางสาย**ยังอยู่**
(วางสายเพราะพูดจบ ไม่ใช่เพราะเปลี่ยนใจ) แต่ล้างตอนเริ่มสายใหม่

**เขียนแล้วต้องเห็นผลทันที** — `router.refresh()` หลังยืนยัน ไม่งั้นผู้ใช้กดซ้ำเพราะคิดว่าไม่ติด

**การเลิกทำคือการคืนค่าเดิม ไม่ใช่การล้างค่า** — ปุ่มเลิกทำต้องพก**ค่าก่อนหน้า**ไปด้วย
เคยเขียน `done_at = null` ทั้งตอนทำและตอนย้อน ผลคือการเลิกทำหลัง "เอาติ๊กออก"
ไม่เปลี่ยนอะไรเลยแต่จอขึ้นว่า "เลิกทำแล้ว"

**ช่องแก้ที่ไม่มีที่ให้ค่าลง ห้ามขึ้นบนจอ** — `draftTimeField()` ใน `lib/drafts.ts`
เป็นคนตัดสินทั้งว่าช่องขึ้นไหมและค่าลงฟิลด์ไหน **ที่เดียวกัน** จึงเถียงกันเองไม่ได้

**การ์ดคือการถามอยู่แล้ว** — พอรู้ว่าผู้ใช้ต้องการอะไร ต้องเรียก `propose_` ไปเลย
ห้ามถามก่อนว่า "จะให้ร่างไว้ไหม" · การถามก่อนคือถามสองรอบเพื่อสิ่งเดียวกัน
และแพงที่สุดในโหมดเสียง · **ข้อยกเว้นเดียวคือยังไม่รู้ว่าจะลงวิชาไหน**

**ชั้นเสนอกันชื่อวิชากำกวมได้ไม่หมด** — `findProject()` โยน "ตรงกับหลายวิชา" เฉพาะตอน
โมเดลส่งคำกำกวมมาจริง ๆ · ถ้ามันเลือกเองแล้วส่งชื่อเต็มมา ด่านนั้นไม่ทำงานเลย ·
ที่รอดจริงคือการ์ดแสดง**ชื่อวิชาเต็ม** ผู้ใช้จึงทานเจอก่อนกด

---

## 8 · การแจ้งเตือน

```
1. pg_cron ทุกนาที
2. → pg_net ยิง POST พร้อม Bearer token จาก Vault (kevin_cron_token)
3. → Edge Function send-reminders
4. → claim_due_reminders() อ้างสิทธิ์แบบ atomic **เฉพาะของเจ้าของที่มีเครื่องเปิดรับอยู่**
5. → web-push ยิงเข้าแต่ละเครื่อง จัดกลุ่มตาม user_id
6. → เครื่องที่ตอบ 404/410 ถูกลบทิ้ง
```

**ข้อ 4 สำคัญมาก** — `claim_due_reminders()` เขียน `notified_at` ทันทีที่หยิบ
ถ้าหยิบมาแล้วไม่มีที่ส่ง การเตือนจะถูกทำเครื่องหมายว่าส่งแล้วและ**หายไปเงียบ ๆ**

> ⚠️ **เงื่อนไข "มีเครื่องเปิดรับอยู่" ต้องอยู่ใน `UPDATE` คำสั่งเดียวกับที่เลือกแถว**
> เคยอยู่ที่ Edge Function ในรูป "นับเครื่องทั้งหมด ถ้าเป็นศูนย์ก็ไม่อ้างสิทธิ์"
> ซึ่งถูกตอนมีผู้ใช้คนเดียว · พอมีคนที่สอง มันนับ**รวมทุกคน** แล้วการเตือนของคนที่
> ยังไม่ได้ลงเครื่องถูกอ้างสิทธิ์ทิ้งโดยไม่มีที่ส่ง — หายถาวรโดยไม่มี error
> · **ห้ามย้ายกลับไปกรองที่ Edge Function** เพราะนั่นคือ select แล้วค่อย update
> ซึ่งทำให้ cron รอบที่ทับกันส่งซ้ำ (doc/TRAPS.md)

| ความลับ | อยู่ที่ |
|---|---|
| VAPID **private** | Supabase secrets |
| VAPID **public** | Vercel env `NEXT_PUBLIC_VAPID_PUBLIC_KEY` |
| service_role key | Supabase Vault ชื่อ `kevin_cron_token` (ห้ามฝังใน cron เป็น literal) |
| สำเนา VAPID | `web/.vapid-backup.json` (gitignore) |

> ⚠️ **ห้าม regenerate VAPID** — subscription ทุกเครื่องจะตายหมด

**iOS** — Safari บน iPhone ไม่ให้เว็บทั่วไปส่ง push ต้อง **เพิ่มลงหน้าจอโฮมก่อน**
แล้วเปิดจากไอคอน · หน้า `/settings` ตรวจแล้วแสดงวิธีติดตั้งเฉพาะกรณีนั้น

---

## 9 · ภาษาภาพ

ชื่อสไตล์ **Nightfall** · ธีมมืดอย่างเดียว · token อยู่ใน `doc/globals.css`

> **กฎข้อเดียวที่ทุกอย่างตามมา: ม่วงคือสิ่งที่กดได้ · สีที่เหลือคือข้อมูล**

ม่วงแบรนด์คงความจัดไว้เต็ม ส่วนสีชนิดข้อมูลลดความจัดลง ~45% ผลคือลำดับความสำคัญ
เกิดขึ้นเอง — **สิ่งที่สว่างที่สุดบนจอคือสิ่งที่กดได้เสมอ** ไม่ต้องเพิ่มขนาดหรือเงาช่วย

### สี

| บทบาท | ค่า | ใช้ที่ไหน |
|---|---|---|
| พื้นหลัง | `#120E22` | พื้นแอปทั้งหมด |
| พื้นผิวยก | `#1B1630` | การ์ด แผง |
| ชั้นยก | `#241E3C` | hover / เลือกอยู่ |
| เส้นคั่น | `#2E2748` | ขอบ · เส้นคั่นรายการ |
| ตัวอักษรหลัก | `#F1F0F7` | **อย่าลดลงเพื่อความสวย** — ตั้งสูงเพื่อให้อ่านกลางแดดได้ |
| ตัวอักษรรอง | `#A5A2B8` | คำอธิบาย |
| ตัวอักษรจาง | `#8E8BA4` | label · meta · **ผ่าน WCAG AA ทุกพื้น** |
| **แบรนด์** | `#A594FF` | ปุ่ม · สถานะเลือก · เส้นเวลาปัจจุบัน (265°) |
| คาบเรียน | `#79C2D0` | ช่วงเวลาประจำ (187°) |
| กิจกรรม | `#D5BB6D` | event ที่เกิดครั้งเดียว (45°) |
| เส้นตาย · เตือน | `#E0897E` | task ที่มี due_at และ reminder (6°) |
| งาน | `#74C6A4` | task ที่ยังไม่กำหนดวัน · เครื่องหมายเสร็จ (157°) |
| โน้ต | `#8E88A8` | shortnote |

องศาคือระยะบนวงล้อสี · ทุกสีห่างจากม่วงอย่างน้อย 78° จึงแยกออกได้แม้บนบล็อกเล็กในปฏิทิน

> ⚠️ **`--faint` ขยับมาแล้วสามครั้งเพราะ WCAG AA** (`#6E6B83` → `#87849D` → `#8E8BA4`)
> มันคือตัวที่แตะเส้น 4.5 ก่อนเพื่อนเสมอ · **ถ้าจะดันพื้นสว่างขึ้นอีก ให้คำนวณคู่ `--faint`
> ก่อนทุกครั้ง** ตอนนี้ต่ำสุด 4.81 บน `--raised` · ตรวจครบ 27 คู่ (9 สี × 3 พื้น) ผ่านหมด

### Gradient ประจำ Area

ใช้แทนภาพประกอบ ความทึบ **11%** เท่านั้น — พอให้แยกว่าของมาจากไหน แต่ไม่แย่งความสนใจ

| Area | คีย์ | gradient |
|---|---|---|
| Class | `class` | `#79C2D0` → `#8878F0` |
| Competition | `comp` | `#E0897E` → `#A594FF` |
| Personal | `pers` | `#74C6A4` → `#79C2D0` |
| General | `gen` | `#A594FF` → `#D98BB4` |

> ⚠️ **เปลี่ยนคีย์เมื่อไหร่ ลำดับสำคัญ** — คีย์ที่เป็นทั้งชื่อเก่าและชื่อใหม่ต้องถูกปลดออกก่อน
> ไม่งั้นสองแถวจะเป็นค่าเดียวกันชั่วคราวแล้วโดน `update` รอบถัดไปเปลี่ยนพร้อมกันทั้งคู่
> · แก้พร้อมกันสี่ที่: `areas.color` ใน DB · `AREA_CLASS` ใน `app/library/page.tsx`
> · `--g-*` และ `.acard--*` ใน CSS

### จังหวะการเคลื่อนไหว

| token | ค่า | ใช้กับ |
|---|---|---|
| `--t-fast` | 140ms | ตอบสนองการแตะ · ติ๊ก · ปุ่มยุบ |
| `--t-base` | 220ms | แผงเปิดปิด · แถวเข้าออก |
| `--t-slow` | 320ms | เปลี่ยนหน้า · morph ข้ามหน้า |
| `--ease` | `cubic-bezier(.22,.61,.36,1)` | ทุกอย่าง · ออกตัวเร็ว จบช้า |

**ห้ามตั้งค่าใหม่เอง** · **ไม่มีอะไรเกิน 320ms** เกินกว่านั้นคนเริ่มรู้สึกว่า "รอ" แทน "ลื่น"

> ⚠️ **ทุก `@keyframes` และ `transition` ใหม่ต้องมี `prefers-reduced-motion` คุม**
> ของเดิมทำครบทุกตัว อย่าให้ของใหม่เป็นตัวแรกที่ทำพัง

`lib/useFlip.ts` ทำให้แถวที่ย้ายตำแหน่งเดินทาง — แถวใหม่ต้องมี `data-flip` ที่เป็นตัวตนคงที่
· `lib/cardFlight.ts` ทำการ์ดที่กดบินไปเป็นหัวของหน้าถัดไป **ไม่ใช่ View Transition**
เพราะ Next.js ทำ transition สองรอบจนจับคู่ไม่ได้ · ต้องลบค่าที่จดทันทีที่อ่าน
· `lib/haptic.ts` สั่นตอนติ๊กเสร็จและตอนของหายจากจอ — **การสั่นห้ามพาการกระทำหลักล้มตาม**
(iOS ไม่รองรับเลย)

### ตัวอักษร รูปทรง และรายการ

- **Inter** (ละติน) + **Noto Sans Thai** โหลดผ่าน `next/font/google`
- `letter-spacing` ติดลบเล็กน้อย (`-0.005em` เนื้อหา · `-0.025em` หัวข้อ)
- `font-feature-settings: "cv11", "ss01"` ให้เลข 1 กับตัว l แยกกันชัด
- mono สำหรับเวลาและตัวเลข: Geist Mono / Cascadia Mono / Consolas
- การ์ด `14px` · ช่องเล็ก/บล็อกปฏิทิน `7–10px` · ปุ่มและป้ายโค้งเต็ม
- **เงาไม่มีตอนนิ่ง** เรืองเฉพาะตอน hover · **ชั้นพื้นผิวมีแค่ 2 ชั้น** ห้ามซ้อนกล่องในกล่อง
- `components/Odometer.tsx` ตัวเลขไหลทีละหลัก · แถบ 0-9 อยู่ใน DOM เสมอ จึง `aria-hidden`
  ทั้งก้อนแล้วใส่ค่าจริงใน `aria-label` ไม่งั้นอ่านออกมาเป็น "0123456789"

**องค์ประกอบของรายการหนึ่งแถว**

| ส่วน | กติกา |
|---|---|
| แถบสีซ้าย | กว้าง 2px บอกชนิด |
| ช่องติ๊ก | มีเฉพาะ **task** เท่านั้น |
| หัวเรื่อง | บรรทัดเดียว ยาวเกินตัดด้วย … |
| บรรทัดข้อมูล | mono ตัวเล็กสีจาง · **ในหน้าวันนี้และปฏิทิน** ขึ้นต้นด้วยชื่อ project เสมอ · **ในหน้า project** ไม่ต้องซ้ำชื่อตัวเอง |
| ป้ายขวา | โผล่เฉพาะตอนมีอะไรผิดปกติ — เลยกำหนด · อีกไม่ถึงชั่วโมง · ยังไม่ตั้งวัน |
| เสร็จแล้ว | จาง 38% + ขีดฆ่า · ร่วงท้ายกลุ่ม **ไม่ซ่อน** |

### ปฏิทินสัปดาห์ — โครงที่กลับด้าน

**วัน = แกนตั้ง · เวลา = แกนนอน** (ไม่ใช่แบบ Google Calendar)

```css
.wk { display: grid; grid-template-columns: 2.9rem repeat(12, minmax(40px, 1fr)); grid-auto-rows: 40px; }
/* คาบวันจันทร์ 09:00–11:00 → แถว 2 (จันทร์), คอลัมน์ 3 (09:00) ยาว 2 ช่อง */
.blk { grid-row: 2; grid-column: 3 / span 2; }
```

- แถว 1 = หัวตารางเวลา · แถว 2–8 = จันทร์ถึงอาทิตย์
- เส้นเวลาปัจจุบันเป็น**เส้นแนวตั้ง** สีแบรนด์
- บล็อกซ้อนกัน → **แบ่งความสูงในแถวของวันนั้น** ไม่ใช่แบ่งความกว้าง
- **แกนเวลาคำนวณจากข้อมูลจริง** เร็วสุด −1 ชม. ถึงช้าสุด +1 ชม. **ห้าม fix 00:00–24:00**

ได้อะไร: 7 แถวสูงราว 280px ลงจอมือถือได้ครบไม่ต้องตัดเหลือ 3 วัน · บล็อกแนวนอน
จึงมีที่พอเขียนชื่อวิชาเต็มพร้อมเลขห้อง

### มือถือ กับ เดสก์ท็อป

| เรื่อง | มือถือ (< 768px) | เดสก์ท็อป |
|---|---|---|
| การนำทาง | แถบล่าง 5 ช่อง (ไอคอนบนคำ) | แถบข้างซ้ายค้างไว้ (เรียงนอน) |
| ปฏิทินสัปดาห์ | 7 วันครบ · ปัดซ้ายขวาเลื่อนเวลา | เห็นช่วงเวลากว้างขึ้น |
| ปฏิทินเดือน | 2 รายการต่อวัน | 3–4 รายการต่อวัน |
| แผงรายละเอียด | เลื่อนขึ้นจากล่าง เต็มความกว้าง | แผงข้างขวา 380px |
| หน้า project | เรียงลงมาชั้นเดียว | สองคอลัมน์ |
| เพิ่มเร็ว | ปุ่มลอยมุมขวาล่าง | กด `N` ที่คีย์บอร์ด |

**ช่อง KeviN กลางแถบล่าง** — แคปซูลกว้างกว่าช่องอื่น 1.6 เท่า ไม่ใช่วงกลมยกขึ้น
เพราะปุ่มเพิ่มเร็วสีม่วงอยู่มุมขวาล่างแล้ว ม่วงสองก้อนห่างกันไม่ถึงนิ้วจะแย่งกันเป็นปุ่มหลัก
· เนื้อในเป็น**เวิร์ดมาร์ก `KEVIN`** ที่แกะจากฟอนต์ Unbounded เป็น SVG path
**ไม่โหลดฟอนต์** — `aria-label` จึงเป็นทางเดียวที่ screen reader จะรู้ว่าปุ่มนี้ไปไหน

### คำที่ใช้บนหน้าจอ

| ใน DB | บนหน้าจอ |
|---|---|
| `area` | **Area** (คำอังกฤษ · เจ้าของเคาะ 8 ก.ย. 2026 — "ด้าน" อ่านแล้วไม่รู้ว่าหมายถึงอะไร) |
| `project` | โปรเจกต์ · ใน Class เรียก "วิชา" |
| `task` / `reminder` / `shortnote` | งาน / เตือน / โน้ต |
| `project_schedule` | ช่วงเวลาประจำ · ใน Class เรียก "คาบเรียน" |
| `archived` | เก็บเข้าคลัง |

### สถานะพิเศษ

| สถานะ | ทำอะไร |
|---|---|
| วันนี้ไม่มีอะไร | ไม่ขึ้นหัวข้อว่างเรียงกัน — "วันนี้ว่าง — สัปดาห์นี้มี 3 งานรออยู่ ดูเลยไหม" |
| Area ยังไม่มี project | ปุ่มสร้างอยู่ในตัว Area นั้น ไม่ใช่ข้อความว่าง |
| สัปดาห์ที่ไม่มีอะไร | **ยังวาดตารางเปล่าไว้** เพราะตารางเปล่าคือคำตอบว่า "ว่างทั้งสัปดาห์" |
| ออฟไลน์ | แสดงข้อมูลที่แคชไว้ + แถบเตือน ปิดปุ่มแก้ไข |
| บันทึกไม่สำเร็จ | ไม่ทิ้งสิ่งที่พิมพ์ เก็บไว้ในช่องเดิม + ปุ่มลองใหม่ |
| กำลังโหลด | โครงร่างเทาในตำแหน่งเดียวกับของจริง ไม่ใช้วงกลมหมุน |
| ผู้ช่วยเสนอการแก้ | **การ์ดยืนยัน** ขอบม่วง อยู่ในกล่องเดียวกับคำพูดที่ทำให้เกิดมัน · ค่าเดิมขีดฆ่าไว้หน้าค่าใหม่เสมอ |
| ยืนยันแล้ว | การ์ดกลายเป็นแถบพร้อมปุ่ม **เลิกทำ** — ไม่หายไปเฉย ๆ เพราะการเขียนที่มองไม่เห็นคือสิ่งที่น่ากลัวที่สุดของผู้ช่วยที่แก้ข้อมูลได้ |

**ไม่มีสีใหม่ ฟอนต์ใหม่ หรือรูปทรงใหม่เข้าระบบตอนทำประตูที่สาม** — ฟ้า `--sched` =
กำลังเกิดขึ้นตอนนี้ · ส้ม `--due` = สิ่งที่ต้องระวัง (ปุ่มวางสาย) · ม่วง = สิ่งที่กดได้
· **แถบสีซ้ายของแถวรายการยกมาใช้กับการ์ดทางลัดไม่ได้** เพราะแถบสีแปลว่า "ชนิดของข้อมูล"
ส่วนการ์ดทางลัดคือ*การกระทำ* จึงใช้ลูกศรม่วงแทน

> ⚠️ **ฟองข้อความฝั่งเรากลับด้านสีจากทั้งแอป** (พื้นไล่เฉดม่วงสว่าง ตัวอักษรเข้ม)
> ของใหม่ที่วางบนนั้นต้องคำนวณคอนทราสต์กับ **`#8878F0` ซึ่งเป็นปลายเข้มสุด**

---

## 10 · ความเร็วและความปลอดภัย

### สามข้อที่ห้ามรื้อ

**1 · ฟังก์ชันต้องรันที่ `hnd1` (โตเกียว)** — `web/vercel.json` ตั้ง `regions: ["hnd1"]`
**เพราะ Supabase อยู่ที่ `ap-northeast-1`** · กฎคือให้ฟังก์ชันอยู่ region เดียวกับฐานข้อมูล
ไม่ใช่ใกล้ผู้ใช้ — หนึ่งหน้ามี query หลายรอบ แต่ขาเบราว์เซอร์มีรอบเดียว
**ถ้าลบไฟล์นี้ Vercel จะกลับไปใช้ `iad1` (เวอร์จิเนีย)** แล้วทุก query วิ่งข้ามแปซิฟิกไปกลับ

> ย้าย Supabase ไป region อื่นเมื่อไหร่ **ต้องย้าย `regions` ตามด้วย**

**2 · ใน server action ใช้ `currentUserId()` ไม่ใช่ `getUser()`** — `getUser()` เป็น
network call ไป Supabase **ทุกครั้ง** ไม่ใช่การอ่าน cookie · `proxy.ts` ยืนยันตัวตนให้แล้ว
ทุก request · `currentUserId()` ใช้ `getClaims()` ซึ่งตรวจลายเซ็น ES256 ในเครื่อง

> ⚠️ **`proxy.ts` ยังต้องใช้ `getUser()` ต่อไป** — ตรงนั้นคือด่านที่ต้องยืนยันกับเซิร์ฟเวอร์จริง

**3 · ทุกหน้ามี `loading.tsx`** — ทุกหน้าเป็น `force-dynamic` เพราะข้อมูลผูกกับ `auth.uid()`
แคชร่วมกันไม่ได้ · โครงร่างจาก `components/Skeleton.tsx` ทำให้จอไม่ค้างนิ่งระหว่างรอ
(**ยกเว้น `/library/[areaId]` โดยตั้งใจ** — เงื่อนไขที่ทำให้ morph ติด)

**ผลวัดจริงหลัง deploy (19 ส.ค.)**

| | ก่อนแก้ | หลังแก้ |
|---|---|---|
| `X-Vercel-Id` | `sin1::iad1` | `sin1::hnd1` |
| TTFB `/login` ค่ากลาง | ~445 ms | **268 ms** |
| server action ที่แค่ update | — | 128–158 ms |

### เขียนข้อมูลต้องนับแถว

`update` ที่ไม่โดนสักแถวถือว่า "สำเร็จ" ในสายตา PostgREST · **เขียนข้อมูลเมื่อไหร่ให้
`.select()` แล้วเช็กจำนวนแถวที่ได้กลับมาทุกครั้ง** — เคยทำให้ลากจัดลำดับดูเหมือนได้
แต่ไม่ได้เขียนอะไรลง DB เลย

> ⚠️ **การนับแถวไม่ได้พิสูจน์ว่าข้อมูลเปลี่ยน** มันพิสูจน์แค่ว่าคำสั่งไปโดนแถว
> เขียนค่าเดิมทับค่าเดิมก็ได้แถวกลับมาเท่ากัน

### ความปลอดภัยฝั่งเว็บ

- ใช้ **anon key เท่านั้น** ทั้ง client และ server — RLS เป็นด่านจริง
- `service_role` key อยู่แค่ใน Edge Function · **ตัวแปรที่ขึ้นต้น `NEXT_PUBLIC_` ทุกตัว
  ไปอยู่ใน bundle ที่ผู้ใช้เปิดดูได้** — `GEMINI_API_KEY` จึงห้ามมีคำนำหน้านั้นเด็ดขาด
- `proxy.ts` ต่ออายุ session ทุก request และเด้งคนที่ยังไม่ล็อกอินไปหน้า login
- `matcher` ยกเว้น `manifest.json` กับ `sw.js` ไว้ **ห้ามเอาออก** ไม่งั้น PWA พังทั้งหมด
- **เพิ่มฟังก์ชัน `security definer` ใหม่เมื่อไหร่ ต้อง revoke จาก public/anon/authenticated
  ทุกครั้ง** — ไม่งั้นใครถือ anon key ก็ยิง `rpc/purge_archived` ลบข้อมูลทิ้งได้
  · `invite_code_valid()` เป็นข้อยกเว้นที่ grant ให้ `anon` **โดยตั้งใจ** เพราะคนที่
  ยังไม่มีบัญชีคือคนเดียวที่ต้องเรียกมัน · มันคืน **boolean เปล่า ๆ ห้ามคืนแถว**
- **ด่านสมัครอยู่ที่ trigger บน `auth.users` ไม่ใช่ที่หน้า `/login`** — `signUp()`
  เรียกตรงด้วย anon key ได้จากที่ไหนก็ได้ · การเช็กในหน้าเว็บมีไว้ทำ**ข้อความ**เท่านั้น
- **`invite_codes` เปิด RLS แล้วไม่มี policy สักข้อ** = ปิดสนิท · อ่านได้เมื่อไหร่
  คือได้รหัสที่ยังไม่ถูกใช้ไปทั้งกอง

---

## 11 · เก็บกวาดอัตโนมัติ

`housekeeping()` รันวันละครั้ง 20:00 UTC (ตีสามไทย) เรียกจาก pg_cron ตรง ๆ
ไม่ผ่าน Edge Function เพราะไม่ต้องออกไปข้างนอกและไม่ต้องถือ secret

- reminder ข้ามวัน · task ที่ติ๊กเสร็จ**และ**เลยวันส่ง → `archived_at` อัตโนมัติ
- ของในคลังครบ **7 วัน → `DELETE` จริง** ครอบคลุม `items` · `events` · **และ `projects`**
- **ลบ project แล้ว items ข้างในหายตามทั้งกอง** (`on delete cascade`) — ตั้งใจ เจ้าของเคาะเอง
  · `purge_archived()` นับของที่จะหายตามไว้**ก่อนลบ** แล้วรายงานเป็นแถวของตัวเอง
  ถ้านับหลังลบจะได้ 0 เสมอ
- **ไม่แตะ `areas`** — ลบ area หนึ่งใบเท่ากับล้างทั้งด้าน คนละขนาดกัน

> ⚠️ **`KEEP_DAYS` ใน `lib/archive.ts` ต้องตรงกับ `interval '7 days'` ใน `purge_archived()`**
> แก้ที่เดียวแล้วอีกที่จะนับผิดเงียบ ๆ · `/library/archive` คือหน้าที่มองเห็นของที่รอถูกลบ

---

## 12 · คำสั่งที่ใช้บ่อย

```bash
npm run dev --prefix web              # รันเว็บในเครื่อง (พอร์ต 3000)
npm test --prefix web                 # เทสต์ตรรกะแกน 416 เคส
npm run lint --prefix web             # ต้องสะอาด 0 error 0 warning
npx tsc --noEmit --project web        # ตรวจ type

cd web && npx vercel --prod --scope nicetsuuu   # deploy (--scope ขาดไม่ได้)
npx supabase functions deploy send-reminders     # deploy Edge Function
npx supabase db push                             # รัน migration
```

**ตรวจของก่อน deploy ให้ใช้ `tsc --noEmit` + `npm test` + `lint`** ไม่ใช่ `next build`
— ดู "สิ่งที่ยังไม่ได้ทำ" ใน [doc/HISTORY.md](doc/HISTORY.md)

---

## 13 · แผนที่ไฟล์

```
KeviN/
├── ARCHITECTURE.md          ← ไฟล์นี้ · ระบบทำงานยังไง
├── CLAUDE.md                กติกาการทำงานกับโปรเจกต์นี้
├── .claude/skills/invite/   ตัวสั่งงาน `/invite` — ออกรหัสเชิญ · ดูผู้ใช้ · ไล่เหตุเมลไม่มา
├── doc/
│   ├── DECISIONS.md         ทุกข้อที่เคาะแล้วพร้อมเหตุผล ← อย่าเปลี่ยนโดยไม่ถาม
│   ├── TRAPS.md             กับดักที่รู้ล่วงหน้า
│   ├── HISTORY.md           เดินมายังไง + งานค้างที่รู้แล้ว
│   ├── SCHEMA.sql           schema จริง รันได้ทั้งไฟล์
│   ├── globals.css          design token
│   ├── CLAUDE-PROJECT-PROMPT.md   custom instructions ของประตู MCP
│   ├── phase0/              สคริปต์ตั้งค่า DB + คู่มือ
│   └── phase6/              prompt ของ Cowork
├── archive/                 เอกสารและไฟล์เทียบแบบที่ทำหน้าที่เสร็จแล้ว
├── SYSTEM.html · UXUI.html · WRITE.html · CONFIRM-CARD.html · UXUI-LIGHT.html
├── supabase/functions/send-reminders/
└── web/                     Next.js app
    ├── vercel.json          ตรึง region ไว้ที่โตเกียว ห้ามลบ
    ├── tsconfig.test.json   คอมไพล์เทสต์เป็น CommonJS ลง .test-build/
    ├── test/                เทสต์ตรรกะแกน 416 เคส · `npm test`
    │   └── accuracy/          ชุดวัดกับโมเดลจริง · **รันด้วยมือ กินโควตา**
    ├── lib/
    │   ├── applyDraft.ts   **การเขียนจริง** · นับแถว · การย้อน
    │   ├── drafts.ts       รูปของ "ร่าง" ที่ client และ server ใช้ร่วมกัน
    │   │                   (อยู่นอก `lib/ai/` เพราะ guard.test.ts ตรวจเฉพาะที่นั่น)
    │   ├── ai/             tool อ่าน + ชั้นเสนอ · prompt · ภาษา · เสียง
    │   │   ├── tools.ts      ทะเบียน tool + runTool() ประตูเดียวที่ข้อมูลออกไป
    │   │   ├── db.ts         interface ที่ไม่มีเมธอดเขียน (ชั้นกันเขียนที่ 2)
    │   │   └── prompt.ts     ห้ามฝังรายชื่อวิชา — มีเทสต์กันไว้
    │   ├── chat/           เรียก Gemini · เก็บบทสนทนา · ด่านตรวจลิงก์ปลอม
    │   │   └── quota.ts      แยก 429 เป็น "หมดโควตาวัน" กับ "เร็วเกินไป"
    │   ├── voice/          สาย Live API · ฟังตัวอย่างเสียง · ไม่บันทึกเสียงที่พูด
    │   ├── calendar.ts · agenda.ts · weeks.ts · layout.ts · time.ts   (มีเทสต์ทุกตัว)
    │   ├── cardFlight.ts · useFlip.ts · haptic.ts    การเคลื่อนไหว
    │   └── autolink.tsx    ทำ URL/อีเมลใน body ให้กดได้ · ไม่ใช้ innerHTML
    ├── app/
    │   ├── actions/propose.ts   เปลือก `'use server'` ของ applyDraft
    │   ├── actions/areas.ts     สร้าง/แก้/เก็บเข้าคลัง Area · "ลบ" ไม่ใช่ DELETE
    │   ├── actions/auth.ts      ออกจากระบบ (เพิกถอน token จริง ไม่ใช่ล้าง cookie)
    │   ├── login/               เข้าสู่ระบบ + สมัครด้วยรหัสเชิญ
    │   ├── library/AreaEditor.tsx  แผงสร้าง/แก้ไข Area (overlay) + AreaAdd · AreaEdit
    │   ├── api/                 read/[tool] · chat · voice/token · voice/transcript
    │   ├── kevin/               ประตูที่สาม · TalkRoom · VoiceCall · Settings
    │   └── */loading.tsx        โครงร่างระหว่างรอ · มีครบทุกหน้า
    ├── components/
    │   ├── CallProvider.tsx  **เจ้าของสายตัวจริง อยู่ระดับ layout**
    │   ├── DraftCard.tsx     การ์ดยืนยัน (07) + แผงแก้ชั้นสอง (10)
    │   ├── DraftSheet.tsx    ร่างค้างตอนออกจากหน้าโทร (02)
    │   ├── DraftStack.tsx    การ์ดซ้อนเมื่อเสนอหลายใบ (06)
    │   └── Reveal.tsx        ครอบทุก page.tsx และ loading.tsx
    └── public/pcm-recorder.js   AudioWorklet แปลงไมค์เป็น PCM16 (ไม่ผ่าน bundler)
```
