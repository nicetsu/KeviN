# KeviN — คู่มือระบบฉบับเต็ม

เอกสารนี้เขียนไว้ให้ **คนหรือ AI ที่เพิ่งเปิดโปรเจกต์นี้ครั้งแรก** อ่านจบแล้วแก้ระบบได้เลย
ไม่ต้องไล่โค้ดเองตั้งแต่ต้น

> อ่าน [CLAUDE.md](CLAUDE.md) ก่อนถ้าต้องการแค่กติกาการทำงาน
> อ่านไฟล์นี้ถ้าต้องการเข้าใจ**ว่าระบบทำงานยังไง**

---

## 1 · KeviN คืออะไร

ผู้ช่วยส่วนตัวของนักศึกษาคนเดียว เก็บ **ตารางเรียน · งาน · การเตือน · โน้ต** ไว้ที่เดียว
แล้วเปิดสามทางให้เข้าถึง

| ประตู | ใช้ตอนไหน | เขียนข้อมูลได้ไหม |
|---|---|---|
| **คุยกับ Claude ผ่าน MCP** | นึกอะไรออกระหว่างเดิน โยนเข้าระบบเร็ว ๆ | ✅ เขียนได้เต็มที่ |
| **เว็บแอป PWA** | นั่งวางแผน ดูปฏิทิน ติ๊กงาน | ✅ เขียนได้เต็มที่ |
| **ผู้ช่วยในแอป `/kevin`** | ถามเร็ว ๆ ระหว่างเดิน ทั้งพิมพ์และพูด | ⚠️ **เสนอได้ · ผู้ใช้กดยืนยันถึงเขียน** |

**ทั้งสามประตูอ่านตารางเดียวกัน ไม่มีการ sync ไม่มีข้อมูลสองชุด** — นี่คือการตัดสินใจแกนกลาง
ที่ทำให้ไม่ต้องมีตรรกะรวมข้อมูลเลย

ประตูที่สามเคยอ่านอย่างเดียว · **เปลี่ยนเมื่อ 2 ก.ย. 2026** เป็น "โมเดลไม่เขียน โมเดลเสนอ" —
ชั้น tool ยังเขียนไม่ได้เหมือนเดิม การเขียนจริงอยู่หลังปุ่มยืนยันบนการ์ด
ดีไซน์เต็มอยู่ที่ [`doc/CHAT.md`](doc/CHAT.md) · เรื่องการแก้ข้อมูลอยู่ที่ [`doc/WRITE.md`](doc/WRITE.md)

---

## 2 · ภาพรวมสถาปัตยกรรม

```
                    ┌──────────────────────────┐
                    │   Supabase (Postgres)    │
                    │   RLS ทุกตาราง            │
                    │   CHECK บังคับความหมาย    │
                    └────┬──────────┬──────────┘
             anon key    │          │  service_role (เฉพาะเซิร์ฟเวอร์)
             + RLS       │          │
              ┌──────────┘          └───────────┐
              │                                 │
   ┌──────────┴─────────┐            ┌──────────┴──────────┐
   │  เว็บ Next.js 16   │            │  Edge Function       │
   │  บน Vercel         │            │  send-reminders      │
   │  kevin-rose        │            └──────────┬──────────┘
   │  .vercel.app       │                       │ เรียกทุกนาทีโดย
   └────────────────────┘                       │ pg_cron + pg_net
                                                │ (กุญแจอยู่ใน Vault)
   ┌────────────────────┐            ┌──────────┴──────────┐
   │  Claude + MCP      │            │  Web Push → มือถือ   │
   │  (คุยสั่งงาน)       │            └─────────────────────┘
   └────────────────────┘

   ── ประตูที่สาม · อ่านได้ · เสนอได้ · เขียนเองไม่ได้ ──────────
   หน้า /kevin ┬─ แชต  → /api/chat ──────────► Gemini Flash-Lite
               │                     │
               └─ โทร  → เบราว์เซอร์ ─┼───ws──► Gemini Live
                         ต่อตรง       │        (ephemeral token)
                                      ▼
                            runTool() + ตัวกรอง Area
                                      │  อ่าน · และเรียกชั้นเสนอ
                                      ▼   Supabase (anon + RLS เดิม)

   propose_* คืน "ร่าง" ─► การ์ดบนจอ ─► ผู้ใช้กดยืนยัน
                                            │
                                            ▼
                              app/actions/propose.ts ─► Supabase
                              (จุดเดียวที่เขียนได้ · ตรวจ Area ขาเข้าซ้ำ)
```

**หลักที่ยึด:** กติกาทุกข้อบังคับที่ **ระดับฐานข้อมูล** ไม่ใช่ที่แอป
เพราะมีสองประตูเขียนข้อมูล ถ้าบังคับที่แอปจะต้องเขียนกฎซ้ำสองที่แล้วหลุดแน่นอน

---

## 3 · โครงสร้างข้อมูล

```
Area (Class / Competition / Personal / General)
└── Project (ใน Class เรียก "วิชา")
    ├── project_schedules   ช่วงเวลาที่ซ้ำรายสัปดาห์ · ไม่แจ้งเตือน
    │   └── time_offsets ↘  ตัดทอนรายวัน — ชี้ได้ทั้งคาบเรียนและกิจกรรม
    ├── events              กิจกรรมครั้งเดียว · มีชื่อของตัวเอง · ขึ้นปฏิทิน · ไม่แจ้งเตือน
    │   └── event_agenda    กำหนดการข้างในงาน · ไม่ขึ้นปฏิทิน
    └── items
        ├── task        มีสถานะเสร็จ · due_at ไม่บังคับ
        ├── reminder    remind_at บังคับ · push เข้ามือถือ · เตือนครั้งเดียว
        └── shortnote   ไม่มีเวลา ไม่มีสถานะเสร็จ
```

### เส้นแบ่งสามเส้น — ถ้าพร่าเมื่อไหร่ ข้อมูลจะเริ่มไปอยู่ผิดตาราง

| ถ้าสิ่งนั้น… | ลงที่ |
|---|---|
| ซ้ำทุกสัปดาห์ | `project_schedules` |
| เกิดครั้งเดียว มีช่วงเวลา ต้องขึ้นปฏิทิน | `events` |
| เป็นรายละเอียดภายใน event เดียว | `event_agenda` |

**ห้ามใส่ `week_offsets` ลง `events`** และ **ห้ามให้ `event_agenda` ขึ้นปฏิทิน** — สองข้อนี้คือสิ่งที่ทำให้แต่ละตารางมีงานของตัวเองชัดเจน

`time_offsets` เก็บ **คำตัดสินของเจ้าของ เฉพาะวันเดียว** — "ไม่ไปคาบนี้" หรือ "อยู่ถึงแค่ 11:00"
ชี้ไปที่ `schedule_id` หรือ `event_id` อย่างใดอย่างหนึ่ง · **เวลาจริงของมหาลัยและของผู้จัดไม่ถูกแตะเลย**
คืนค่า = ลบแถวทิ้ง

> ⚠️ **`week_offsets` ไม่ใช่ที่เก็บ "ฉันจะไม่ไป"** — มันแปลว่า *สัปดาห์นั้นไม่มีคาบ*
> การเอาไปใช้แทนกันให้ผลบนจอที่ถูกต้องเป๊ะแต่ทำลายข้อมูลถาวร (ดู `doc/TRAPS.md`)

`items.event_id` ผูกงานเข้ากับกิจกรรมได้ (ไม่บังคับ) · ผูกได้จาก**หน้า event ที่เดียว** เพื่อไม่ให้ทุกที่ที่สร้างงานต้องถามเรื่องกิจกรรม
DB บังคับด้วย foreign key **สองคอลัมน์** `(event_id, project_id)` ว่าต้องอยู่ project เดียวกัน — ดู `doc/TRAPS.md` เรื่อง `set null (event_id)`

### ทำไม task/reminder/shortnote อยู่ตารางเดียวกัน

เพราะ 80% ของฟิลด์ซ้ำกัน และหน้าจอส่วนใหญ่ต้องดึงมารวมกันเรียงตามเวลาอยู่แล้ว
แยกตารางแปลว่าต้อง `UNION` ทุก query · **ราคาที่จ่ายคือต้องมี CHECK constraint กันข้อมูลผิดรูป**

### CHECK constraint ที่ห้ามถอด

| ชื่อ | บังคับอะไร |
|---|---|
| `reminder_needs_time` | reminder ต้องมี `remind_at` |
| `reminder_no_due` | reminder ห้ามมี `due_at` |
| `task_no_remind` | task ห้ามมี `remind_at` (อยากเตือนให้สร้าง reminder แยก) |
| `shortnote_timeless` | shortnote ห้ามมีเวลาและสถานะเสร็จ |
| `notified_reminder_only` | `notified_at` ใช้กับ reminder เท่านั้น |
| `sched_time_order` | เวลาเลิกต้องหลังเวลาเริ่ม |
| `sched_start_is_mon` | `start_date` ต้องเป็นวันจันทร์ |
| `sched_weeks_present` | ต้องมีอย่างน้อย 1 สัปดาห์ |
| `sched_weeks_valid` | ห้ามมี NULL และห้ามติดลบ |

> ⚠️ สองข้อสุดท้ายเคยเขียนผิดจน**ไม่ได้กันอะไรเลย** เพราะ `NULL` ใน CHECK ถือว่าผ่าน
> `array_length('{}',1)` คืน NULL ไม่ใช่ 0 · `0 <= all('{0,NULL}')` ก็คืน NULL
> ถ้าจะแก้ต้องคง `coalesce(...)` และ `array_position(..., null) is null` ไว้

### การซ้ำของช่วงเวลา — รูปแบบเดียว

```
start_date    = วันจันทร์ของสัปดาห์ที่ 0
week_offsets  = [0,1,2,...] สัปดาห์ที่มีคาบนี้
```

UI มีสองโหมด (`ซ้ำอีก N สัปดาห์` กับ `เลือกสัปดาห์เอง`) แต่**ลงมาที่รูปแบบนี้ทั้งคู่**
โหมดแรกเป็นแค่ปุ่มลัดที่เติม `[0..N-1]`

`start_time`/`end_time` เป็น `time` เปล่า **ไม่ใช่ timestamptz** โดยตั้งใจ —
"09:00 ทุกวันจันทร์" คือเวลาท้องถิ่น ไม่ใช่จุดเวลาจุดเดียว

---

## 4 · ฟังก์ชันใน DB สองตัวที่ต้องรู้

### `schedule_occurrences(from, to)`

คลี่ `week_offsets` ออกเป็นวันที่จริง · `security invoker` จึงเคารพ RLS
**ทุกที่ที่ต้องรู้ว่าคาบไหนตกวันไหน ต้องเรียกตัวนี้ ห้ามคำนวณเอง**

> ฝั่งเว็บมี `web/lib/weeks.ts` ที่คำนวณแบบเดียวกันสำหรับ "ตัวอย่างสด" ตอนกรอกฟอร์ม
> และมีเทสต์เทียบกับผลจาก DB จริงว่าตรงกัน — ถ้าแก้ตัวใดตัวหนึ่งต้องแก้อีกตัวด้วย

### `calendar_entries(from, to)`

รวม **คาบเรียน + event** เป็นรูปเดียว · `security invoker` จึงเคารพ RLS
**ทุกที่ที่ต้องรู้ว่าช่วงนั้นติดอะไรบ้าง ต้องเรียกตัวนี้** — มีสามที่ที่กินข้อมูลชุดนี้
(hero หน้าแรก · ปฏิทินเดือน · ปฏิทินสัปดาห์) ถ้ารวมฝั่งเว็บต้องเขียนตรรกะเดียวกันสามรอบ

event ที่ข้ามเที่ยงคืนถูก**หั่นเป็นบล็อกรายวัน**ที่นี่ เพราะปฏิทินวางบล็อกด้วย `grid-row = วัน`
บล็อกใบหนึ่งจึงอยู่ได้วันเดียว · `ศ 20:00 → ส 09:00` กลายเป็น `ศ 20:00–24:00` + `ส 00:00–09:00`

> `end_time` เป็น `'24:00:00'` ได้ ซึ่ง `new Date()` แปลงไม่ได้ — ใช้ `entryEndMs()`
> ใน `web/lib/calendar.ts` เท่านั้น (ดู `doc/TRAPS.md`)

คืนสองคอลัมน์ที่ฝั่งเว็บต้องอ่าน — **`skipped`** (ตั้งใจไม่ไป) และ **`trimmed`** (เวลาถูกตัด)
**แถวที่ `skipped` ยังอยู่ในผลลัพธ์** ไม่ถูกกรองทิ้ง · เว็บทำให้จางลงและติดป้ายแทนการซ่อน
เพราะถ้าหายไปเลยจะไม่มีทางรู้ว่าเคยตัดสินใจไว้ หรือกดผิดแล้วคาบหายไปเฉย ๆ
· การ์ดใหญ่และจุดบนช่องวันในโหมดเดือนไม่นับแถวที่ `skipped`

### `claim_due_reminders()`

อ้างสิทธิ์ reminder ที่ถึงเวลา **แบบ atomic**

```sql
update items set notified_at = now()
 where type='reminder' and notified_at is null and remind_at <= now()
returning *;
```

> ⚠️ **ห้ามแยกเป็น SELECT แล้วค่อย UPDATE เด็ดขาด** — cron รันทุกนาที
> ถ้ารอบก่อนยังไม่จบ จะส่ง push ซ้ำ
>
> ⚠️ เป็น `security definer` จึงข้าม RLS · **สิทธิ์เรียกถูก revoke จาก `anon`/`authenticated` แล้ว**
> เหลือแค่ `service_role` ถ้าเผลอ grant กลับ ใครก็ตามที่ถือ anon key จะอ่าน reminder ทุกแถวได้

---

## 5 · เว็บแอป (`web/`)

Next.js 16 · App Router · ไม่ใช้ Tailwind (ใช้ CSS custom property ตรง ๆ)

### หน้าจอ

| path | คือ | หมายเหตุ |
|---|---|---|
| `/login` | เข้าสู่ระบบ | อีเมล + รหัสผ่าน |
| `/` | S1 วันนี้ | hero คาบถัดไป (**ข้ามวันได้**) + รายการเรียงเวลาของวันนี้ |
| `/calendar` | S2/S3 ปฏิทิน | เปิดมาเป็นเดือน · `?mode=week` ได้ |
| `/library` | S4 คลัง | dropdown สองชั้น + การ์ด Area |
| `/project/[id]` | S5 หน้าวิชา | งาน/เตือน/โน้ต แยกกลุ่ม |
| `/project/[id]/schedule` | S6 ตัวจัดการช่วงเวลา | สองโหมดการซ้ำ + ตัวอย่างสด |
| `/project/[id]/event/[eventId]` | หน้ากิจกรรม | กำหนดการ + งานที่ผูกไว้ · **เป็น route ไม่ใช่ overlay เพราะต้องมี URL ให้ Claude ส่งลิงก์ได้** |
| `/project/[id]/event/[eventId]/edit` · `/event/new` | ตัวแก้กิจกรรม | โครงเดียวกับ S6 · มีตัวอย่างสดว่าเวลาจบที่เว้นว่างถูกเติมเป็นอะไร |
| `/kevin` | ประตูที่สาม | **เปิดมาที่โหมดโทรเสมอ** · สลับไปแชตได้ มีผลแค่ครั้งนั้น · ดู `doc/CHAT.md` |
| `/kevin/history` | ประวัติการคุยย้อนหลัง | รายการบทสนทนา + หน้าอ่านทีละอัน · **อ่านอย่างเดียว ไม่มีการ์ดร่าง** |
| `/settings` | S9 ตั้งค่าแจ้งเตือน | ขอสิทธิ์ + ปุ่มทดสอบ |

S7 แผงรายละเอียด และ S8 เพิ่มเร็ว เป็น **overlay** ไม่ใช่ route
เพราะ ux.html กำหนดว่าลึกเกินสามชั้นให้เปิดทับ ไม่พาไปหน้าใหม่

### ลากจัดลำดับ — ที่จับโผล่เฉพาะที่ลากแล้วอยู่จริง

`ItemList` รับ `movable` ต่อแถว · ที่จับ (จุด 6 จุด) แสดงเฉพาะแถวที่ **`sort_order`
เป็นตัวตัดสินลำดับจริง** เท่านั้น

| กลุ่มในหน้าวิชา | เรียงด้วย | ลากได้ |
|---|---|---|
| โน้ต | `sort_order` ล้วน | ทั้งกลุ่ม |
| งาน | เสร็จ → `due_at` → `sort_order` | เฉพาะที่ยังไม่เสร็จและไม่มีวันกำหนด |
| เตือน | `remind_at` ล้วน | ไม่ได้ |

ถ้าเปิดให้ลากในกลุ่มที่ `due_at`/`remind_at` มาก่อนใน comparator **ลากแล้วจะเด้งกลับ
ตอนโหลดใหม่** ดูเหมือนบั๊กทั้งที่เขียนลง DB สำเร็จ · แถวที่ลากได้ต้องอยู่ติดกัน
เป็นบล็อกเดียวด้วย เพราะตัวลากสลับตำแหน่งกันเองภายในบล็อก

ใช้ Pointer Events ไม่ใช่ HTML5 drag-and-drop เพราะ HTML5 ใช้ได้แต่เดสก์ท็อป
ซึ่งไม่พอสำหรับ PWA ที่ใช้บนมือถือเป็นหลัก · มีลูกศรขึ้น/ลงให้ใช้แป้นพิมพ์ได้ด้วย

### hero "คาบถัดไป" มองข้ามวัน

`/` ดึงคาบล่วงหน้า **14 วัน** แล้วเลือกคาบแรกที่ยังไม่จบ ซึ่งอาจไม่ใช่ของวันนี้
ต้องเผื่อยาวขนาดนี้เพราะตารางเว้นสัปดาห์สอบกลางภาคกับ Commencement Week
ช่องว่างยาวสุดราว 10 วัน · **รายการข้างล่าง hero ยังกรองเหลือเฉพาะวันนี้** อย่าเผลอ
เอาคาบล่วงหน้าไปแสดงในนั้นด้วย

> ตอนคำนวณเวลาต้องใช้ `o.occurs_on` ไม่ใช่ `dateKey` — คาบที่เจอไม่ใช่ของวันนี้เสมอไปแล้ว

### ไฟล์ที่มีเทสต์ล้วนกำกับ — แก้แล้วต้องรันเทสต์

```bash
npm test --prefix web        # 394 เคส · ใช้เวลาไม่ถึงวินาที
```

| ไฟล์ | ทำอะไร | เทสต์ | เคส |
|---|---|---|---|
| `lib/time.ts` | เวลาไทยทั้งหมด · ป้ายบอกเวลา | `test/time.test.ts` | 44 |
| `lib/weeks.ts` | ตรรกะการซ้ำ + คลี่เป็นวันที่ | `test/weeks.test.ts` | 28 |
| `lib/parse.ts` | ตีความบรรทัดเดียวเป็น item (ภาษาไทย) | `test/parse.test.ts` | 21 |
| `lib/libraryOpen.ts` | สถานะกางของหน้าคลังใน cookie | `test/libraryOpen.test.ts` | 18 |
| `lib/agenda.ts` | เติมเวลาจบของกำหนดการที่เว้นว่าง | `test/agenda.test.ts` | 15 |
| `lib/archive.ts` | ของที่รอถูกลบ · วันที่เหลือก่อนหาย | `test/archive.test.ts` | 14 |
| `lib/calendar.ts` | เวลาของบล็อกปฏิทิน รวมบล็อกที่ชนเที่ยงคืน | `test/calendar.test.ts` | 11 |
| `lib/cardFlight.ts` | การ์ดที่กดบินไปเป็นหัวของหน้าถัดไป | `test/cardFlight.test.ts` | 11 |
| `lib/layout.ts` | จัดบล็อกที่เวลาชนกันในปฏิทินสัปดาห์ | `test/layout.test.ts` | 10 |
| `lib/eventOrder.ts` | ลำดับกิจกรรม | `test/eventOrder.test.ts` | 8 |
| `lib/ai/tools.ts` | ชั้น tool อ่านอย่างเดียวของประตูที่สาม | `test/tools.test.ts` | 26 |
| `lib/ai/propose.ts` | ชั้นเสนอ — ร่างที่ไม่แตะ DB | `test/propose.test.ts` | 24 |
| `lib/ai/prompt.ts` | prompt ของประตูแอป | `test/prompt.test.ts` | 16 |
| `lib/ai/lang.ts` | ล็อกภาษาไทย/อังกฤษ | `test/lang.test.ts` | 12 |
| `lib/ai/visibility.ts` | Area ไหนออกจากเครื่องไปหาโมเดลได้ | `test/visibility.test.ts` | 10 |
| `lib/ai/voices.ts` | เสียงของ KeviN · ค่าตั้งต้นแยกตามภาษา | `test/voices.test.ts` | 9 |
| `lib/chat/markdown.ts` | แกะ markdown ของผู้ช่วย | `test/markdown.test.ts` | 19 |
| `lib/chat/links.ts` | กันผู้ช่วยยื่นลิงก์ที่ไม่มีอยู่จริง | `test/links.test.ts` | 9 |
| `lib/voice/mic.ts` | เลือกไมค์ | `test/mic.test.ts` | 12 |
| `lib/voice/transcript.ts` | ไม่บันทึกสิ่งที่ผู้ใช้พูดระหว่างสาย | `test/voiceTranscript.test.ts` | 8 |
| `lib/drafts.ts` | ฟิลด์เวลาที่ร่างแต่ละใบแก้ได้จริง | `test/drafts.test.ts` | 8 |
| `lib/applyDraft.ts` | **การเขียนจริง** — ตัวกรอง Area ขาเข้า · นับแถว · การย้อน | `test/applyDraft.test.ts` | 28 |
| `lib/ai/propose.ts` | **แก้ร่างใบเดิม** (`propose_update_draft`) | `test/updateDraft.test.ts` | 18 |
| `lib/chat/stream.ts` | ต่อชิ้นของสายคำตอบแชตเป็นบรรทัด | `test/stream.test.ts` | 9 |
| *(ด่านกันเขียน)* | อ่านซอร์ส `lib/ai/**` หาทางเขียนที่หลุดเข้ามา | `test/guard.test.ts` | 6 |

> **จุดเดียวที่เขียนข้อมูลได้จริงมีเทสต์แล้ว** (5 ก.ย. 2026) — ตรรกะย้ายจาก
> `app/actions/propose.ts` ออกมาที่ `lib/applyDraft.ts` ซึ่งรับ client เข้ามาเป็น
> พารามิเตอร์ · ไฟล์ `'use server'` เหลือเป็นเปลือกที่ประกอบ client แล้ว
> `revalidatePath` เท่านั้น · db ปลอมในเทสต์ **จดทุกคำสั่งที่ยิงออกไป**
> จึงตรวจได้ว่าเขียนอะไรลงไปจริง ไม่ใช่แค่ว่าฟังก์ชันตอบว่า ok
>
> ⚠️ `lib/applyDraft.ts` **นำเข้าแบบ relative ไม่ใช่ `@/`** เหมือนทุกไฟล์ที่เทสต์
> เอื้อมถึง — ชุดเทสต์รันด้วย node ตรง ๆ ซึ่งไม่รู้จัก path alias ของ bundler

ไฟล์กลุ่มนี้เคยมีบั๊กที่ตามองไม่เห็นแต่เทสต์จับได้ · **แก้เมื่อไหร่ให้รันเทสต์ทุกครั้ง**
และถ้าเพิ่มพฤติกรรมใหม่ ให้เขียนเคสก่อนแก้

### ชุดวัดความแม่นกับโมเดลจริง — `web/test/accuracy/` · **ไม่ได้อยู่ใน `npm test`**

เทสต์ข้างบนวัด**ตรรกะ** · ชุดนี้วัด **โมเดล + prompt + ชั้นเสนอ รวมกัน** ซึ่งเป็นที่
ที่ตรรกะถูกทุกบรรทัดแต่ผลรวมยังผิดได้ · ยิง Gemini จริงทั้งสองโหมด

| | ทำยังไง |
|---|---|
| แชต | คัดลอกวงจรของ `app/api/chat/route.ts` มาทั้งดุ้น — วน tool · ส่ง `parts` กลับทั้งก้อน |
| โทร | ต่อ **Live API จริงผ่าน WebSocket** ด้วย config ชุดเดียวกับ `/api/voice/token` |

```bash
npx tsc -p tsconfig.test.json --prefix web
node --env-file=.env.local .test-build/test/accuracy/run.js          # ทั้งสองโหมด
node --env-file=.env.local .test-build/test/accuracy/run.js chat 05,13   # เฉพาะบางข้อ
```

ใช้ **ข้อมูลปลอมที่ตรึงไว้** (ของจริงเปลี่ยนทุกวัน เทียบข้ามวันไม่ได้) และ **ป้อนข้อความ
แทนเสียง** จึงไม่ได้วัดคุณภาพการถอดเสียง · ชื่อวิชาสองอันแรกตั้งให้คล้ายกันเพื่อดักการเดา

> **รอบแรก 4 ก.ย. 2026 เจอบั๊กที่เทสต์ตรรกะมองไม่เห็นเลย** — `propose_complete_item`
> ประกาศ `done` เป็น `string` แต่ตัวอ่านรับแต่ boolean · ฝั่งแชตกลบไว้ด้วยการยิงซ้ำ
> ฝั่งเสียงยอมแพ้แล้วบอกผู้ใช้ว่า "เกิดข้อผิดพลาดบนหน้าจอ" (ดู `doc/TRAPS.md`)
> · แก้แล้ว · **ผลปัจจุบัน 5 ก.ย. 2026: แชต 17/17 · โทร 7/7**
>
> ⚠️ **429 ของ Gemini เป็นได้ทั้งเพดานต่อนาทีและโควตารายวัน** — รันชุดนี้รวดเดียว
> รอบแรกได้ 4/17 โดย 13 ข้อล้มด้วย 429 แล้วยิงข้อเดียวใหม่ทันทีกลับผ่าน
> (เจอ 5 ก.ย. 2026) · ชุดวัดเว้นจังหวะ 6 วินาทีระหว่างข้อและถอย 20 วินาทีเมื่อเจอ 429 แล้ว
> · `lib/chat/gemini.ts` ยังแปลง 429 ทุกแบบเป็น "โควตาของวันนี้หมดแล้ว" ซึ่ง
> **บอกผู้ใช้ผิด**เวลาชนแค่เพดานต่อนาที และฝั่งเสียงใช้คำว่า "โควตา" เป็นสัญญาณ
> ดันไปโหมดแชต — ยังไม่ได้แก้
>
> ⚠️ **จูน prompt เมื่อไหร่ให้รันชุดนี้ก่อนและหลัง** — การจูนที่แก้เคสเป้าหมายได้
> แล้วทำเคสข้างเคียงพัง เป็นสิ่งที่มองไม่เห็นถ้าวัดแต่เคสที่ตั้งใจแก้ (เจอมาแล้ว)

> ⚠️ ชุดนี้**เขียนขึ้นใหม่ 30 ส.ค. 2026** ของเดิม 81 เคสรันนอก repo แล้วหายไปกับเครื่อง
> เคสที่ครอบคลุมจึงไม่ใช่ชุดเดียวกันเป๊ะ แม้จะไล่ตามกับดักใน `doc/TRAPS.md` ทุกข้อแล้ว
> **ห้ามให้เทสต์อยู่นอก repo อีก** — สิ่งที่รันไม่ได้จาก checkout สด เท่ากับไม่มี

**ทำไมต้องคอมไพล์ก่อนรัน** — `tsconfig.test.json` คอมไพล์เป็น CommonJS ลง `.test-build/`
เพราะ type stripping ของ node ทำงานในโหมด ESM ซึ่งบังคับให้ `import` ต้องมีนามสกุลไฟล์
แต่โค้ดในแอปเขียนแบบ bundler (`from './time'`) ตามที่ Next.js ใช้ · ผลพลอยได้คือเทสต์ถูกตรวจ type ด้วย

### จังหวะหน้าจอ — สี่ข้อที่แก้กันมาหลายรอบ

**แสงพื้นอยู่ที่ `html` ไม่ใช่ที่ `.wrap`** พื้นของ root element ถูกส่งไปวาดบน canvas
ของ viewport จึงไม่ถูกจับใน snapshot ของ view transition · ตอนอยู่ที่ `.wrap` มันโดน
cross-fade ทุกครั้งที่เปลี่ยนหน้า แล้วภาพเก่ากับใหม่ซ้อนกันจนสว่างวาบ (ต้องมี
`background-attachment: fixed` คู่กัน ไม่งั้นแสงก้อนล่างจะไหลไปท้ายหน้าที่เลื่อนยาว)

**แถบล่างมี `view-transition-name: nav`** และปิด animation ของ old/new ทิ้ง
ทุกหน้าเป็น `force-dynamic` Next จึงเรียก `startViewTransition` **สองครั้งต่อการกดหนึ่งครั้ง**
ของที่ไม่มีชื่อของตัวเองจะติดอยู่ใน snapshot ของ root แล้วโดนจางสองรอบ

**หน้า `/kevin` ใช้ `name="talk"` ซึ่งสลับทันทีไม่จาง** โครงร่างของมันเหมือนของจริง
ทุกพิกเซล การจางสลับจึงไม่ได้เล่าอะไร มีแต่จะสร้างวินาทีที่ภาพหายแล้วกลับมา
· **ภาพเก่าต้อง `opacity: 0`** ไม่ใช่แค่ `animation: none` ทั้งคู่ — สองภาพที่วาดทับกัน
ทำให้เงาเรืองโปร่งแสงบวกกันจนสว่างเป็นสองเท่า

**ห้องโทรมีคลื่นตอนเข้าห้อง** เล่นบน `VoiceCall` ไม่ใช่ในโครงร่าง เพราะเซิร์ฟเวอร์
ตอบเร็วกว่าคลื่นมาก มันจึงโดนตัดกลางคันเสมอถ้าอยู่ฝั่งโครงร่าง

### ความเร็ว — สามข้อที่ห้ามรื้อ

**1 · ฟังก์ชันต้องรันที่ `hnd1` (โตเกียว)**
`web/vercel.json` ตั้ง `regions: ["hnd1"]` ไว้ **เพราะ Supabase อยู่ที่ `ap-northeast-1` (โตเกียว)**
กฎคือให้ฟังก์ชันอยู่ region เดียวกับฐานข้อมูล — หนึ่งหน้ามี query หลายรอบ แต่ขาเบราว์เซอร์มีรอบเดียว
ถ้าลบไฟล์นี้ Vercel จะกลับไปใช้ค่าเริ่มต้น `iad1` (เวอร์จิเนีย) แล้วทุก query
วิ่งข้ามแปซิฟิกไปกลับ · ตอนวัดก่อนแก้ TTFB อยู่ที่ 420–470 ms กับหน้าที่ไม่มี query สักตัว

> ถ้าย้าย Supabase ไป region อื่นเมื่อไหร่ **ต้องย้าย `regions` ตามด้วย**
> เช็ก region ปัจจุบันได้จาก `db.<project_ref>.supabase.co` แล้วเอา IP ไปเทียบกับ
> `https://ip-ranges.amazonaws.com/ip-ranges.json`

**2 · ใน server action ใช้ `currentUserId()` ไม่ใช่ `getUser()`**
`getUser()` เป็น network call ไป Supabase **ทุกครั้ง** ไม่ใช่การอ่าน cookie
`proxy.ts` ยืนยันตัวตนกับเซิร์ฟเวอร์ให้แล้วทุก request การเรียกซ้ำใน action จึงจ่าย round trip ฟรี ๆ
`currentUserId()` ใน `lib/supabase/server.ts` ใช้ `getClaims()` ซึ่งตรวจลายเซ็น ES256 ในเครื่อง

action ที่แค่ update **ไม่ต้องรู้ด้วยซ้ำว่าใครล็อกอินอยู่** — RLS ปฏิเสธแถวที่ไม่ใช่ของเจ้าของให้แล้ว
แต่ต้องมี `.select()` แล้วนับแถวที่ได้กลับมาเสมอ ไม่งั้น update ที่ไม่โดนอะไรเลยจะรายงานว่าสำเร็จ

> ⚠️ **`proxy.ts` ยังต้องใช้ `getUser()` ต่อไป** — ตรงนั้นคือด่านที่ต้องยืนยันกับเซิร์ฟเวอร์จริง

**ผลวัดจริงหลัง deploy (19 ส.ค.)** — ใช้เป็นฐานเทียบถ้าจะจูนต่อ

| | ก่อนแก้ | หลังแก้ |
|---|---|---|
| `X-Vercel-Id` | `sin1::iad1` | `sin1::hnd1` |
| TTFB `/login` ค่ากลาง | ~445 ms | **268 ms** |
| server action ที่แค่ update | — | 128–158 ms |

หน้า `/login` ไม่มี query สักตัว ตัวเลขนี้จึงวัดเฉพาะขาเบราว์เซอร์↔ฟังก์ชัน
หน้าที่มี query จริงได้กำไรมากกว่านี้ เพราะขาฟังก์ชัน↔DB เหลือระดับมิลลิวินาที

**3 · ทุกหน้ามี `loading.tsx`**
ทุกหน้าเป็น `force-dynamic` เพราะข้อมูลผูกกับ `auth.uid()` แคชร่วมกันไม่ได้ กดเปลี่ยนหน้าจึง
ต้องรอเซิร์ฟเวอร์เสมอ · โครงร่างจาก `components/Skeleton.tsx` ทำให้จอไม่ค้างนิ่งระหว่างรอ

### ความปลอดภัยฝั่งเว็บ

- ใช้ **anon key เท่านั้น** ทั้ง client และ server — RLS เป็นด่านจริง
- `proxy.ts` ต่ออายุ session ทุก request และเด้งคนที่ยังไม่ล็อกอินไปหน้า login
- `matcher` ยกเว้น `manifest.json` กับ `sw.js` ไว้ **ห้ามเอาออก** ไม่งั้น PWA พังทั้งหมด

---

## 6 · ประตูที่สาม — ผู้ช่วยในแอป (`/kevin`)

แชตกับโหมดโทร · **อ่านได้ทั้งหมด และเสนอการแก้ได้ แต่เขียนเองไม่ได้**
ใช้ Gemini free tier จึงไม่มีค่าใช้จ่ายรายเดือน
ดีไซน์เต็มพร้อมเหตุผลทุกข้ออยู่ที่ [`doc/CHAT.md`](doc/CHAT.md) · เรื่องการแก้ข้อมูลอยู่ที่
[`doc/WRITE.md`](doc/WRITE.md)

### โหมดแชตไหลทีละคำ

`/api/chat` ตอบเป็น **สาย NDJSON** ไม่ใช่ก้อนเดียวตอนจบ · หนึ่งบรรทัดคือหนึ่งเหตุการณ์
(`delta` · `reset` · `draft` · `done` · `error`) · ตัวต่อชิ้นอยู่ที่ `lib/chat/stream.ts` (มีเทสต์)

⚠️ **`delta` ยังไม่ผ่านด่านตรวจลิงก์** — ลิงก์ถูกหั่นข้ามก้อนได้ ตรวจทีละชิ้นจึงไม่มีทางถูก
หน้าจอต้องเอา `reply` ใน `done` ไป**แทน**ข้อความที่ไหลมาทั้งหมด สิ่งที่ค้างบนจอ
จึงเป็นฉบับที่ผ่าน `sanitizeLinks()` แล้วเสมอ

⚠️ **`reset` มีไว้เพราะโมเดลพิมพ์แล้วเปลี่ยนใจไปเรียก tool ได้** · สิ่งที่ไหลไปในรอบนั้น
ไม่ใช่คำตอบ ต้องสั่งให้จอทิ้ง ไม่ใช่ปล่อยค้าง · การ์ดร่างถูกส่งทันทีที่เสนอ ไม่รอคำตอบจบ

### ประวัติย้อนหลัง

`/kevin/history` เห็นทุกบทสนทนาที่ยังไม่ถูกล้าง · กดเข้าไปอ่านทีละอันได้
**อ่านอย่างเดียว ไม่มีการ์ดร่าง** เพราะร่างมีอายุแค่หน้าจอตอนนั้น (doc/WRITE.md §8)
· ทางเข้าอยู่เหนือปุ่มล้างในแผงตั้งค่า — การเห็นว่ามีอะไรอยู่ต้องมาก่อนการลบถาวรเสมอ

### โมเดลไม่เขียน โมเดลเสนอ

```
พูด/พิมพ์ → โมเดลเรียก propose_* → ชั้น tool คืน "ร่าง" (ไม่แตะ DB)
          → เว็บวาดการ์ด → ผู้ใช้กดยืนยัน → app/actions/propose.ts เขียนจริง
```

**จุดเดียวที่เขียนได้คือ server action** ซึ่งอยู่หลังปุ่มที่ผู้ใช้กด · ชั้น tool
ยังไม่มีทางเขียนเหมือนเดิม `test/guard.test.ts` จึงยังผ่านโดยไม่ต้องแก้

| ทำได้ | ทำไม่ได้เลย |
|---|---|
| เพิ่มงาน/เตือน/โน้ต/กิจกรรม · แก้ชื่อและเวลา · ย้ายวิชา · ติ๊กเสร็จ · เก็บเข้าคลัง | ลบวิชา · ลบถาวร · แตะ `project_schedules` |

⚠️ **ตัวกรอง Area ต้องตรวจสองทาง** — `runTool()` กันข้อมูล*ขาออก* ส่วนชั้นเสนอกับ
server action กัน*ขาเข้า* · ร่างที่ชี้ไปวิชาในกลุ่มที่ซ่อนไว้ถูกปฏิเสธทั้งสองจุด

### สองช่องทาง ชั้น tool ชุดเดียว

| | โหมดแชต | โหมดโทร |
|---|---|---|
| โมเดล | `gemini-3.1-flash-lite` | `gemini-3.1-flash-live-preview` |
| ต่อยังไง | เบราว์เซอร์ → `/api/chat` → Google | **เบราว์เซอร์ ↔ Google ตรง** ผ่าน WebSocket |
| ยืนยันตัวตน | cookie เดิม | **ephemeral token** ที่ล็อก config ไว้แล้ว |
| tool รันที่ไหน | ในเซิร์ฟเวอร์เอง | **เด้งกลับมาที่เบราว์เซอร์** แล้วยิงเข้า `/api/read/*` |

เสียงต้องต่อตรงเพราะ Vercel เป็น serverless ถือ WebSocket ยาว ๆ ไม่ได้ · แต่ API key
ลงเบราว์เซอร์ไม่ได้ — ephemeral token แก้ทั้งสองข้อพร้อมกัน

**แต่ tool ต้องวิ่งกลับมาที่เซิร์ฟเวอร์เราเสมอ** เพราะ **ตัวกรอง Area อยู่ใน `runTool()`**
ให้เบราว์เซอร์ถาม Supabase ตรง ๆ ง่ายกว่ามาก แต่นั่นคือการข้ามตัวกรอง

### สามชั้นที่กันการเขียน

| ชั้น | อยู่ที่ | กันยังไง |
|---|---|---|
| 1 | `lib/ai/tools.ts` | ไม่มี tool ที่เขียนอยู่ในชุดเลย **โมเดลเรียกไม่ได้เพราะไม่มีชื่อให้เรียก** |
| 2 | `lib/ai/db.ts` | `ReadOnlyDb` **ไม่มีเมธอดเขียน** โค้ดที่พยายามเขียนคอมไพล์ไม่ผ่าน |
| 3 | `test/guard.test.ts` | ด่านอัตโนมัติ — อ่านซอร์สจริงแล้วพังถ้ามีทางเขียนโผล่เข้ามาใน `lib/ai/**` |

ชั้นที่ 3 **ไม่ใช่ role ของ Postgres** โดยตั้งใจ — role อ่านอย่างเดียวจะเก็บภาษีทุก migration
ในอนาคต (ตารางใหม่ลืมเพิ่ม policy = ผู้ช่วยมองไม่เห็นแบบเงียบ ๆ) และกันได้แค่ประตูเดียว
ทั้งที่ประตู MCP เขียนได้เต็มที่อยู่แล้ว · เหตุผลเต็มอยู่ใน `doc/CHAT.md` §6

### `runTool()` — ประตูเดียวที่ข้อมูลออกไปหาโมเดล

ลำดับตายตัว: **ตรวจชื่อ → ตรวจอินพุต → ดึงข้อมูล → กรอง Area → แปลงรูป**

`areaOf` เป็นฟิลด์บังคับของทุก tool — tool ที่บอกไม่ได้ว่า Area ของแถวตัวเองอยู่ตรงไหน
**ประกาศไม่ผ่านตั้งแต่คอมไพล์** ตัวกรองจึงลืมไม่ได้

คืน `hidden` = จำนวนแถวที่ถูกกรองออก · prompt สั่งให้พูดว่า "มีอีก N อันที่ผมดูไม่ได้"
ไม่งั้นตัวกรองที่ fail-closed จะทำให้ผู้ช่วยตอบว่า "ไม่มีอะไร" ทั้งที่ความจริงคือ "มี แต่ดูไม่ได้"

### เส้นแบ่งข้อมูล

`VISIBLE_AREAS` ใน `lib/ai/visibility.ts` เป็น **allowlist ผูกกับชื่อ Area**
ตอนนี้เปิดครบทั้งสี่ (`Class` `Competition` `Personal` `General`) เพราะเจ้าของยืนยันว่า
ไม่มีข้อมูลสำคัญ · **กลไกยังต้องเป็น allowlist ต่อไป** เพราะ Area ที่สร้างใหม่วันหน้า
จะถูกซ่อนไว้ก่อนโดยไม่ต้องมีใครไปสั่ง

> ⚠️ กรองได้แค่ **ข้อมูลที่ tool ตอบกลับ** ไม่ได้กรอง **คำถามที่ผู้ใช้พูด**
> และ free tier ของ Gemini ระบุว่าเอาสิ่งที่ส่งไปพัฒนาผลิตภัณฑ์

### ภาษาและเสียง

**ภาษาค่าเดียว** (ไทย/อังกฤษ) ใช้ทั้งฟังและตอบ · บังคับได้แค่ที่ระดับ prompt เพราะ
**Live API ไม่รับ language code เลย** — เป็นแรงกด ไม่ใช่การล็อก

**เสียง 9 ตัวเลือก** แถวแรกคือ *ค่าตั้งต้น* ที่แยกตามภาษา (ไทย `Charon` · อังกฤษ `Fenrir`)
แถวที่เหลือใช้เสียงนั้นทั้งสองภาษา · ที่ต้องแยกเพราะของจริงพิสูจน์แล้วว่าโมเดล
**ทับเสียงที่เราตั้งในบางภาษา**

ปุ่มฟังตัวอย่างเปิดสาย Live API จริงสั้น ๆ ไม่ใช้ TTS มาเล่นแทน — TTS ให้เสียงไม่เหมือน Live
ถ้าให้ฟังจาก TTS ผู้ใช้จะเลือกเสียงจากสิ่งที่ไม่ใช่สิ่งที่ได้ยินตอนโทรจริง

### สายเสียง — สิ่งที่ต้องรู้

- **สายถูกตัดที่ 15 นาที** ตายตัว · ต่อใหม่ด้วย `sessionResumption` handle แบบที่ผู้ใช้ไม่รู้สึก
- **ตัวจับเวลาห้ามรีเซ็ตตอนต่อสายใหม่** ไม่งั้นผู้ใช้อ่านว่าสายหลุด ทั้งที่บทสนทนาต่อเนื่อง
- token อายุ 30 นาที **ยาวกว่าสายหนึ่งเส้นเท่าตัว** ต่อรอบแรกจึงไม่ต้องขอใหม่
- **สายอยู่ที่ `components/CallProvider.tsx` ระดับ layout** ไม่ใช่ในหน้า `/kevin`
  ถ้าเก็บไว้ในหน้า พอสลับไปดูปฏิทินกลางสาย คอมโพเนนต์ถูก unmount แล้วสายตายทันที
- **ล็อกหน้าจอ = วางสายให้เลย** เบราว์เซอร์หยุดเสียงอยู่แล้ว ค้างไว้จะได้สายที่ตายแต่ดูเหมือนยังอยู่

### สิ่งที่พูดระหว่างสายไม่ถูกบันทึก

เก็บเฉพาะสิ่งที่ KeviN ตอบ · ฝั่งผู้ใช้ลงเป็น `- voice -`
บังคับ **สองชั้น** — ฝั่งเบราว์เซอร์เพื่อให้สิ่งที่เห็นตรงกับสิ่งที่เก็บ และฝั่งเซิร์ฟเวอร์
ใน `/api/voice/transcript` เพื่อบังคับ ต่อให้โค้ดฝั่งจอถูกแก้ในอนาคต

ที่ยังลงแถวไว้แทนที่จะตัดทิ้ง เพราะยังเห็นจังหวะว่าคุยกันกี่รอบ

### ตารางที่เพิ่มมา

```
conversations (started_via · title · started_at · ended_at)
messages      (role · content · via)      ← via = ข้อความนี้มาจากแชตหรือสาย
tool_calls    (name · input · ok · rows_out · rows_hidden · error)
```

`tool_calls` **เก็บอินพุตแต่ไม่เก็บผลลัพธ์** — ผลลัพธ์คือข้อมูลที่อยู่ในตารางอื่นอยู่แล้ว
เก็บซ้ำได้สำเนาชุดที่สองที่เก่าลงเรื่อย ๆ และขยายพื้นที่ที่ข้อมูลส่วนตัวไปโผล่ได้

---

## 7 · การแจ้งเตือน — เส้นทางเต็ม

```
1. pg_cron ทุกนาที
2. → pg_net ยิง POST พร้อม Bearer token จาก Vault (kevin_cron_token)
3. → Edge Function send-reminders
4. → ถ้าไม่มีเครื่องสมัครรับเลย จบตรงนี้ ไม่อ้างสิทธิ์
5. → claim_due_reminders() อ้างสิทธิ์แบบ atomic
6. → web-push ยิงเข้าแต่ละเครื่อง
7. → เครื่องที่ตอบ 404/410 ถูกลบทิ้ง
```

**ข้อ 4 สำคัญมาก** — `claim_due_reminders()` เขียน `notified_at` ทันทีที่หยิบ
ถ้าหยิบมาแล้วไม่มีที่ส่ง การเตือนจะถูกทำเครื่องหมายว่าส่งแล้วและ**หายไปเงียบ ๆ**

### ความลับเก็บที่ไหน

| อะไร | อยู่ที่ |
|---|---|
| VAPID **private** | Supabase secrets |
| VAPID **public** | Vercel env `NEXT_PUBLIC_VAPID_PUBLIC_KEY` |
| service_role key | Supabase Vault ชื่อ `kevin_cron_token` |
| สำเนา VAPID | `web/.vapid-backup.json` (gitignore) |

> ⚠️ **ห้าม regenerate VAPID** — subscription ทุกเครื่องจะตายหมด ต้องให้ผู้ใช้กดเปิดใหม่ทุกเครื่อง

### iOS

Safari บน iPhone ไม่ให้เว็บทั่วไปส่ง push — ต้อง **เพิ่มลงหน้าจอโฮมก่อน** แล้วเปิดจากไอคอน
หน้า `/settings` ตรวจเรื่องนี้แล้วแสดงวิธีติดตั้งให้เฉพาะกรณีนั้น

---

## 8 · ภาษาภาพ

ชื่อสไตล์ **Nightfall** · ธีมมืดอย่างเดียว · token อยู่ใน `doc/globals.css`

> **กฎข้อเดียวที่ทุกอย่างตามมา: ม่วงคือสิ่งที่กดได้ · สีที่เหลือคือข้อมูล**

| บทบาท | สี |
|---|---|
| แบรนด์ (กดได้) | `#A594FF` |
| คาบเรียน | `#79C2D0` |
| กิจกรรม | `#D5BB6D` |
| เส้นตาย · เตือน | `#E0897E` |
| งาน · เครื่องหมายเสร็จ | `#74C6A4` |
| โน้ต | `#8E88A8` |

**ปฏิทินสัปดาห์กลับแกน** — วัน = แกนตั้ง · เวลา = แกนนอน
ทำให้เห็นครบ 7 วันบนมือถือโดยไม่ต้องตัดเหลือ 3 วัน · บล็อกที่ชนกัน **แบ่งความสูง** ไม่ใช่ความกว้าง

**แกนเวลาคำนวณจากข้อมูลจริง** เร็วสุด −1 ชม. ถึงช้าสุด +1 ชม. ห้าม fix 00:00–24:00

---

## 9 · กับดักที่เคยเสียเวลาไปแล้ว

| เรื่อง | สิ่งที่ต้องระวัง |
|---|---|
| CHECK ที่ได้ผล NULL | Postgres ถือว่า "ผ่าน" — ด่านที่ดูเหมือนกันกลับไม่กันอะไรเลย |
| `security definer` | เปิดให้ `PUBLIC` เรียกเป็นค่าเริ่มต้น ต้อง revoke เอง |
| ภาษาไทยไม่เว้นวรรค | regex ที่บังคับ `\s` ขนาบคำจะจับ "ติวพุธ" ไม่ได้ |
| `1fr` ใน CSS Grid | มี `min-width:auto` ข้อความยาวดันคอลัมน์จนตารางล้น ต้องใช้ `minmax(0,1fr)` |
| permission ≠ subscription | เบราว์เซอร์อนุญาตแล้วไม่ได้แปลว่าสมัครรับแล้ว ต้องเช็กทั้งสองชั้น |
| proxy matcher | ถ้าไม่ยกเว้น `sw.js`/`manifest.json` จะโดนเด้งไป login แล้ว PWA พังเงียบ ๆ |
| Next 16 | `middleware` เปลี่ยนชื่อเป็น `proxy` · path CSS เป็น `immutable/chunks` |
| psql บน Windows ไทย | `client_encoding` เป็น WIN874 อ่านไฟล์ UTF-8 พัง ต้องตั้ง `PGCLIENTENCODING=UTF8` |
| เชลล์ | cmd ใช้ `&&` · PowerShell 5.1 ใช้ `;` · PowerShell บล็อก `npx.ps1` ให้ใช้ `npx.cmd` |

---

## 10 · คำสั่งที่ใช้บ่อย

```bash
# รันเว็บในเครื่อง (พอร์ต 3000)
npm run dev --prefix web

# รันเทสต์ตรรกะแกน (394 เคส)
npm test --prefix web

# deploy  (--scope ขาดไม่ได้ ไม่งั้นตอบ Not authorized เพราะ project อยู่ใต้ team
#          แต่ scope เริ่มต้นของ CLI เป็นบัญชีส่วนตัว)
cd web && npx vercel --prod --scope nicetsuuu

# deploy Edge Function
npx supabase functions deploy send-reminders

# รัน SQL กับ DB จริง (migration อยู่ใน supabase/migrations)
npx supabase db push
```

---

## 11 · สิ่งที่ยังไม่ได้ทำ

- **เวลางาน UniHack 2026** ที่เป็น onsite ตั้ง 09:00 ไว้ชั่วคราว รอเวลาจริง
- **ปุ่มล้างประวัติในตั้งค่า ลบถาวรทันที ไม่เก็บเข้าคลัง** — ตั้งใจ (เจ้าของเคาะ 1 ก.ย. 2026)
  · ตั้งแต่ 5 ก.ย. 2026 มีหน้า `/kevin/history` ให้เห็นก่อนว่ามีอะไรอยู่
- **`next build` ล้มบนเครื่องนี้เครื่องเดียว — ไม่กระทบการ deploy** (5 ก.ย. 2026)
  `InvariantError: Expected workStore to be initialized` ตอน prerender หน้าที่เป็น static
  (`/login` · `/_global-error` · `/_not-found`) · Next พิมพ์เองว่า "This is a bug in Next.js"
  · เกิดเหมือนกันทั้ง turbopack และ webpack · เกิดบน master ที่ยังไม่มีของใหม่ ·
  ถอด `CallProvider` ออกก็ยังเกิด (แค่ย้ายไปล้มที่ `/login`)
  · **ยืนยันแล้วว่า build บน Vercel ผ่าน** ด้วย preview deploy — ขั้น
  "Generating static pages (4/4)" ที่ล้มบนเครื่องนี้ ผ่านที่นั่นใน 179ms
  · แปลว่าเป็นเรื่องของ Windows/เครื่องนี้ **อย่าเสียเวลาไล่ถ้าไม่ได้จะใช้ `next build` จริง ๆ**
  · เช็คของก่อน deploy ให้ใช้ `npx tsc --noEmit` + `npm test` + `npm run lint` ตามเดิม
- **การแก้ข้อมูลผ่านผู้ช่วย** — ตัวแก้ในชั้นสองแก้ได้แค่ชื่อกับเวลา
  การย้ายวิชาต้องพูดเอา (`propose_update_draft` รับได้ · ปุ่ม "แก้" ยังไม่มีช่องนั้น)
- **`doc/CLAUDE-PROJECT-PROMPT.md` กับ `doc/SCHEMA.sql` ต้องเอาไปวางใน Claude Project ด้วยมือ**
  แก้ไฟล์ในโปรเจกต์แล้วของจริงบน claude.ai ไม่เปลี่ยนตาม

> `npm run lint --prefix web` สะอาด 0 error 0 warning (30 ส.ค.) · `next build` ไม่ได้รัน eslint
> จึงไม่มีอะไรบล็อก deploy ให้เลย — **ต้องรันเอง ไม่งั้นหนี้จะสะสมโดยไม่มีใครรู้**

---

## 12 · แผนที่ไฟล์

```
KeviN/
├── ARCHITECTURE.md          ← ไฟล์นี้
├── CLAUDE.md                กติกาการทำงานกับโปรเจกต์นี้
├── PLAN.md                  แผน 6 เฟส พร้อมสถานะ
├── archive/                 เอกสารออกแบบที่ทำหน้าที่เสร็จแล้ว (proposal/ux/ui-kit)
├── doc/
│   ├── DECISIONS.md         ทุกข้อที่เคาะแล้วพร้อมเหตุผล ← อย่าเปลี่ยนโดยไม่ถาม
│   ├── TRAPS.md             กับดักที่รู้ล่วงหน้า
│   ├── DESIGN.md            ภาษาภาพ
│   ├── SCHEMA.sql           schema จริง รันได้ทั้งไฟล์
│   ├── globals.css          design token
│   ├── CLAUDE-PROJECT-PROMPT.md   custom instructions ของ MCP
│   ├── phase0/              สคริปต์ตั้งค่า DB + คู่มือ
│   └── phase6/              prompt ของ Cowork
├── supabase/functions/send-reminders/
└── web/                     Next.js app
    ├── vercel.json          ตรึง region ไว้ที่โตเกียว ห้ามลบ
    ├── tsconfig.test.json   คอนฟิกคอมไพล์เทสต์เป็น CommonJS ลง .test-build/
    ├── test/                เทสต์ตรรกะแกน 394 เคส · `npm test`
    │   └── accuracy/          ชุดวัดความแม่นกับโมเดลจริง · **รันด้วยมือ กินโควตา**
    ├── lib/drafts.ts        รูปของ "ร่าง" ที่ทั้ง client และ server ใช้ร่วมกัน
    │                        (อยู่นอก `lib/ai/` เพราะ guard.test.ts ตรวจเฉพาะที่นั่น)
    ├── app/actions/propose.ts  **จุดเดียวที่ผู้ช่วยทำให้ข้อมูลเปลี่ยนได้** อยู่หลังปุ่มยืนยัน
    ├── components/DraftCard.tsx   การ์ดยืนยัน (แบบ 07) + แผงแก้ชั้นสอง (แบบ 10)
    ├── components/DraftSheet.tsx  ร่างค้างตอนออกจากหน้าโทร (แบบ 02)
    ├── lib/ai/              tool อ่าน + ชั้นเสนอ (propose) · ตัวกรอง Area · prompt · ภาษา · เสียง
    │   ├── tools.ts           ทะเบียน tool + runTool() ประตูเดียวที่ข้อมูลออกไป
    │   ├── db.ts              interface ที่ไม่มีเมธอดเขียน (ชั้นกันเขียนที่ 2)
    │   ├── visibility.ts      allowlist ของ Area
    │   ├── lang.ts · voices.ts  ภาษาและเสียง
    │   └── prompt.ts          ห้ามฝังรายชื่อวิชา — มีเทสต์กันไว้
    ├── lib/chat/            เรียก Gemini · เก็บบทสนทนา · ด่านตรวจลิงก์ปลอม
    ├── lib/voice/           สาย Live API · ฟังตัวอย่างเสียง · ไม่บันทึกเสียงที่พูด
    ├── lib/talkPrefs.ts     ภาษาและเสียงที่เลือก (localStorage)
    ├── components/CallProvider.tsx  **เจ้าของสายตัวจริง อยู่ระดับ layout**
    ├── public/pcm-recorder.js       AudioWorklet แปลงไมค์เป็น PCM16 (ไม่ผ่าน bundler)
    ├── app/kevin/           หน้าประตูที่สาม · TalkRoom · VoiceCall · Settings
    ├── app/api/             read/[tool] · chat · voice/token · voice/transcript
    ├── app/*/loading.tsx    โครงร่างระหว่างรอ · มีครบทุกหน้า
    │                        (ของ `/kevin` เหมือนของจริงทุกพิกเซล และสลับแบบไม่จาง)
    ├── components/Skeleton.tsx   ชิ้นส่วนของโครงร่างข้างบน
    ├── app/project/[id]/event/   หน้ากิจกรรม · ตัวแก้ · ปุ่มเพิ่มงานที่ผูกกับกิจกรรม
    └── lib/
        ├── libraryOpen.ts   สถานะกางของหน้าคลัง เก็บใน cookie (มีเทสต์)
        ├── calendar.ts      รูปเดียวที่ปฏิทินและ hero กิน (มีเทสต์)
        ├── agenda.ts        เติมเวลาจบของกำหนดการ (มีเทสต์)
        ├── autolink.tsx     ทำ URL/อีเมลใน body ให้กดได้ · ไม่ใช้ innerHTML
        └── time.ts          เวลาไทยทั้งหมดอยู่ไฟล์นี้ที่เดียว
```
