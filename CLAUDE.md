# KeviN — คำแนะนำสำหรับ Claude

ผู้ช่วยส่วนตัวของนักศึกษา เก็บข้อมูลบน Supabase · **ใช้ได้หลายคน ข้อมูลแยกกันด้วย RLS**
(สมัครต้องมีรหัสเชิญ · ยืนยันอีเมลก่อนใช้)
สั่งงานได้สามทาง — Claude ผ่าน MCP · เว็บแอป PWA · และแท็บ **KeviN** ในเว็บที่คุยได้ทั้งพิมพ์และพูด
(ประตูที่สาม: อ่านได้ · **เสนอการแก้ได้ · เขียนเองไม่ได้** — ผู้ใช้กดยืนยันบนการ์ดถึงเขียนจริง)

**สถานะ: ใช้งานได้จริงครบวงแล้ว · เปิดให้ใช้หลายคนแล้ว 8 ก.ย. 2026**
https://kevin-rose.vercel.app · โค้ดอยู่ใน `web/` (Next.js 16)
· ล่าสุดบน production คือ `288117a` (9 ก.ย. 2026) · `master` นำหน้าเฉพาะคอมมิตเอกสาร

✅ **คำอธิบายของ Area + ผู้ช่วยสร้างโปรเจกต์ได้ — ทำเสร็จแล้ว 9 ก.ย. 2026**
(กลับมติ "ผู้ช่วยสร้าง project ไม่ได้" ที่จดไว้ 8 ก.ย. · **ลบยังทำไม่ได้เหมือนเดิม**)

*(คำขอ "เลิกเรียกว่าวิชา · เรียก `project` ว่า `item`" — เจ้าของยกเลิกครึ่งหลัง
แต่**เคาะให้เลิกใช้คำว่า "วิชา" บนหน้าจอ** เรียก "โปรเจกต์" หมดทุกที่ 9 ก.ย. 2026)*

**เพิ่มผู้ใช้ใหม่ พิมพ์ `/invite`** — ออกรหัสเชิญ · ดูว่าใครใช้ไปแล้ว · ไล่เหตุเวลาเมลไม่มา
(`.claude/skills/invite/SKILL.md`)

---

## อ่านตามลำดับนี้ก่อนแตะอะไร

| # | ไฟล์ | คือ |
|---|---|---|
| 0 | **[ARCHITECTURE.md](ARCHITECTURE.md)** | ระบบทำงานยังไงทั้งหมด — อ่านอันนี้ก่อนถ้าเพิ่งเปิดโปรเจกต์ |
| 1 | **[doc/DECISIONS.md](doc/DECISIONS.md)** | ทุกข้อที่เคาะแล้วพร้อมเหตุผล · **อย่าเปลี่ยนโดยไม่ถามเจ้าของ** |
| 2 | **[doc/TRAPS.md](doc/TRAPS.md)** | กับดักที่พังเงียบ ๆ · อ่านหัวข้อที่เกี่ยวก่อนเขียนโค้ดส่วนนั้น |
| 3 | **[doc/HISTORY.md](doc/HISTORY.md)** | เดินมายังไง + **งานค้างที่รู้แล้ว** |
| 4 | **[doc/SCHEMA.sql](doc/SCHEMA.sql)** | schema จริงที่ deploy อยู่ |

**`TUTORIAL.html` = คู่มือของ*ผู้ใช้* ไม่ใช่ของนักพัฒนา** (9 ก.ย. 2026) — ส่งให้คนที่เพิ่งได้
รหัสเชิญอ่านก่อนเริ่มใช้ · **มีฉบับ artifact เป็นลิงก์ด้วย (ตารางข้างล่าง) ซึ่งสร้างจาก
ไฟล์นี้โดยตัดเปลือก `<html>/<head>/<body>` ออก — แก้ไฟล์แล้วต้องสร้างใหม่แล้ว publish ทับ
ไม่งั้นสองฉบับจะพูดคนละเรื่อง** · **ใช้ token ชุดเดียวกับแอปจริง แก้พาเลตเมื่อไหร่ต้องขยับที่นี่ด้วย**
ไม่งั้นคู่มือจะสอนสีที่ไม่มีอยู่ · เนื้อหาอ้างของจริงทุกบรรทัด (ชื่อปุ่ม · คำบนจอ · ข้อจำกัด)
**แก้พฤติกรรมแอปแล้วต้องกลับมาดูว่าคู่มือยังไม่โกหก**

**เอกสารภาพที่ยังตรงกับของจริง** — `SYSTEM.html` (ระบบ 10 หัวข้อ) · `UXUI.html` (ภาษาภาพ + หน้าจอ)
· `WRITE.html` (แผนให้ผู้ช่วยแก้ข้อมูล) · `CONFIRM-CARD.html` (การ์ดยืนยันสิบแบบ — เจ้าของเลือก
07 หลัก · 10 ชั้นสอง · 02 นอกหน้าโทร) · `UXUI-LIGHT.html` (**ข้อเสนอ ยังไม่ได้ลงมือ** ·
เป็นการกลับมติ "ธีมมืดอย่างเดียว" ต้องเคาะก่อนทำ)
· ไฟล์เทียบแบบที่ใช้เลือกดีไซน์ไปแล้วอยู่ใน `archive/` — มติจดอยู่ใน DECISIONS.md แล้ว

**ฉบับภาพบนคลาวด์** (artifact — ไม่มีไฟล์ในเครื่อง)

| เนื้อหา | ลิงก์ |
|---|---|
| **คู่มือผู้ใช้ใหม่ — ส่งลิงก์นี้ให้คนที่เพิ่งได้รหัสเชิญ** (คู่กับ `TUTORIAL.html`) | https://claude.ai/code/artifact/cc814194-2751-473e-9d0c-89e7e183cf3e |
| ประตูที่สาม · pipeline · เส้นทางข้อมูล · โควตา | https://claude.ai/code/artifact/0453a1ca-a08e-4c7f-b845-314aba3d26a9 |
| ประตูที่สาม · UX/UI ของแท็บ KeviN | https://claude.ai/code/artifact/0106dd44-993c-48fc-ad90-513c7fcae7b6 |
| ให้ผู้ช่วยแก้ข้อมูลได้ (คู่กับ `WRITE.html`) | https://claude.ai/code/artifact/42828617-34f1-4631-a13d-d3a7ed4fc278 |
| การ์ดยืนยันสิบแบบ (คู่กับ `CONFIRM-CARD.html`) | https://claude.ai/code/artifact/44d1351c-8ab1-47f3-bab8-7d763d103f4d |
| *(เก่า)* ข้อเสนอโปรเจกต์ · UX S1–S9 · ui-kit — คู่กับไฟล์ใน `archive/` | [proposal](https://claude.ai/code/artifact/6f50ef21-9670-40e0-84dd-86b29852ec36) · [ux](https://claude.ai/code/artifact/06b0354f-7284-4f28-9b6a-c70b078855dc) · [ui-kit](https://claude.ai/code/artifact/2c6a95f6-e51a-4e9a-a80a-b398bdc43046) |

---

## กติกาการทำงานกับโปรเจกต์นี้

- **ภาษา** — คุยกับเจ้าของเป็นภาษาไทย · โค้ด ตัวแปร และ schema เป็นอังกฤษ · UI เป็นไทย
- **อย่าเปลี่ยนสิ่งที่ตัดสินใจไปแล้วโดยไม่ถาม** ทุกข้อใน `doc/DECISIONS.md` ผ่านการถกมาแล้ว
  ถ้าเจอเหตุผลที่ควรเปลี่ยน ให้เสนอ อย่าเปลี่ยนเงียบ ๆ
- **ปฏิทินสัปดาห์กลับแกน** — วัน = แกนตั้ง, เวลา = แกนนอน ไม่ใช่แบบ Google Calendar
- **timestamptz เสมอ** ยกเว้น `project_schedules.start_time/end_time` ที่เป็น `time` เปล่า

### แก้แล้วต้องรันเทสต์

```bash
npm test --prefix web        # 435 เคส · ไม่ถึงวินาที
```

ตรรกะแกนทั้งหมดมีเทสต์ล้วนกำกับ (รายชื่อไฟล์อยู่ใน ARCHITECTURE §5)
**`npm test` ล้าง `.test-build/` ก่อนคอมไพล์ทุกครั้ง ห้ามถอดออก** — `tsc` ไม่ลบผลลัพธ์
ของไฟล์ที่ซอร์สหายไปแล้ว และคำสั่งรันทั้งโฟลเดอร์ · เคยรันเทสต์ของโค้ดที่ลบไปแล้ว
10 เคสโดยรายงานว่าผ่านมาตลอด (เจอ 9 ก.ย. 2026)
`npm run lint --prefix web` ตอนนี้สะอาด 0 error 0 warning — **ถ้าเพิ่มขึ้นมาให้แก้ อย่าปล่อยสะสม**

**แก้ `lib/ai/prompt.ts` หรือ `lib/ai/propose.ts` เมื่อไหร่ ให้รันชุดวัดกับโมเดลจริง
ก่อนและหลัง** (`web/test/accuracy/` · ไม่ได้อยู่ใน `npm test` เพราะกินโควตา) —
เทสต์ตรรกะมองไม่เห็นบั๊กแบบ "ชนิดใน schema ไม่ตรงกับตัวอ่าน" และมองไม่เห็นการจูน
prompt ที่แก้เคสหนึ่งแล้วทำอีกเคสพัง (เจอทั้งสองแบบ 4 ก.ย. 2026)

---

## สิ่งที่รื้อแล้วพังเงียบ ๆ

> รายละเอียดทุกข้ออยู่ใน `ARCHITECTURE.md` และ `doc/TRAPS.md`

**ระบบ**

- **"ลบ" Area = `archived_at` ห้าม `DELETE`** — `projects.area_id` เป็น cascade
  ลบด้านเดียวแล้ว project/items/events หายทั้งกอง · และต้องเคลียร์ของข้างในก่อนถึงเก็บได้
- **ห้ามลบ `web/vercel.json`** — ตรึงฟังก์ชันไว้ที่โตเกียว region เดียวกับ Supabase
  ถ้าหาย Vercel กลับไปใช้เวอร์จิเนีย แล้วทุก query ข้ามแปซิฟิกไปกลับ
- **ใน server action ใช้ `currentUserId()` ไม่ใช่ `getUser()`** — `getUser()` ยิงเน็ตทุกครั้ง
  (แต่ `proxy.ts` เองยังต้องใช้ `getUser()` ต่อไป)
- **เขียนข้อมูลต้องมี `.select()` แล้วนับแถวเสมอ** — `update` ที่ไม่โดนสักแถวรายงานว่าสำเร็จ
- **เพิ่มฟังก์ชัน `security definer` ใหม่ ต้อง revoke จาก public/anon/authenticated ทุกครั้ง**
- **`KEEP_DAYS` ใน `lib/archive.ts` ต้องตรงกับ `interval '7 days'` ใน `purge_archived()`**
- **ด่านรหัสเชิญอยู่ที่ trigger บน `auth.users` ไม่ใช่ที่หน้า `/login`** — `signUp()`
  เรียกตรงด้วย anon key ได้จากที่ไหนก็ได้ · การเช็กในหน้าเว็บมีไว้ทำ**ข้อความ**เท่านั้น
  **ห้ามถอดชั้น DB ออกเพราะเห็นว่าหน้าเว็บเช็กแล้ว**
- **`invite_codes` เปิด RLS แล้วไม่มี policy สักข้อ** = ปิดสนิท · อย่าเผลอเติม policy
- **ผู้ช่วยสร้าง project ได้แล้ว (9 ก.ย. 2026) แต่ Area ยังสร้างเองไม่ได้** ·
  **ลบ project ยังทำไม่ได้เลย** (`on delete cascade` พางานหายทั้งกอง) — เส้นนั้นไม่ได้ถูกแตะ
  · `handle_new_user()` ยัง seed ทั้งสี่ Area **พร้อมคำอธิบาย** และโปรเจกต์ "ทั่วไป"
  ให้ผู้ใช้ใหม่เหมือนเดิม · ถอดโปรเจกต์นั้นออกเมื่อไหร่ ผู้ใช้ใหม่จะชน
  "ยังไม่มีวิชาให้เลือกเลย" ตั้งแต่ประโยคแรกแล้วจบทางตัน
- **คำอธิบายของ Area ถูกส่งเข้าโมเดลทุกครั้งที่เรียก tool `areas`** — เพดาน 200 ตัว
  บังคับที่ CHECK `area_desc_len` และมี `MAX_DESC` ฝั่งเว็บคู่กัน · แก้ข้างเดียวแล้ว
  อีกข้างจะปฏิเสธเงียบ ๆ · **แก้ข้อความ seed เมื่อไหร่ = เปลี่ยนพฤติกรรมโมเดล
  ต้องรันชุดวัดก่อนและหลัง** ไม่ต่างจากแก้ prompt
- **`unique` ทุกตัวต้องมี `user_id` อยู่ในคีย์** — `push_subscriptions.endpoint` เคยพลาด
  ข้อนี้ แล้วสองบัญชีบนเครื่องเดียวกันเปิดแจ้งเตือนไม่ได้
- **เงื่อนไข "มีเครื่องเปิดรับอยู่" ต้องอยู่ใน `UPDATE` ของ `claim_due_reminders()`**
  ห้ามย้ายไปกรองที่ Edge Function (นั่นคือ select แล้วค่อย update = push ซ้ำ)
- **`send-reminders` ตรวจสิทธิ์ด้วยการให้ Postgres ปฏิเสธ ไม่ใช่เทียบสตริงในโค้ด**
  — client สร้างด้วย **token ของผู้เรียก** · `claim_due_reminders()` ถูก revoke จาก
  anon/authenticated ไว้แล้ว · **ห้ามเปลี่ยนกลับไปใช้ `SUPABASE_SERVICE_ROLE_KEY`
  ของตัวเองสร้าง client** endpoint จะเปิดให้ใครก็ได้อีกครั้งโดยที่ทุกอย่างยังดูปกติ
  · **ห้ามเทียบ token กับ env** (cron ส่ง JWT รุ่นเก่า 219 ตัว · env เป็น `sb_secret_…`
  41 ตัว ไม่มีทางตรงกัน) · **ห้ามเปิด `verify_jwt`** (คนละชนิดกับ token ที่ cron ส่ง)
- **ลบ `push_subscriptions` ต้องลบด้วย `id` ไม่ใช่ `endpoint`** — unique เป็น
  `(user_id, endpoint)` สองบัญชีบนเครื่องเดียวกันใช้ endpoint เดียวกันได้

**สามตารางเวลา แยกหน้าที่กันชัด ๆ ห้ามให้พร่า**

- ซ้ำทุกสัปดาห์ → `project_schedules` · เกิดครั้งเดียว → `events` · รายละเอียดในงาน → `event_agenda`
- **"ไม่ไป" ไม่ใช่ "ไม่มีคาบ"** — ไม่ไปลง `time_offsets` · ไม่มีคาบลง `week_offsets`
  เอาไปปนกันจะทำลายข้อมูลถาวรโดยผลบนจอดูถูกต้อง
- อยากรู้ว่าช่วงไหนติดอะไร ให้เรียก **`calendar_entries()`** ห้ามดึง `events` มาปนเองฝั่งเว็บ

**หน้าจอ**

- **`components/Reveal.tsx` ครอบทุก `page.tsx` และ `loading.tsx`** — เพิ่มหน้าใหม่ต้องครอบทั้งคู่
  · `/kevin` ใช้ `name="talk"` ซึ่งสลับทันทีไม่จาง · **ลบ `loading.tsx` ทิ้งไม่ได้แปลว่าไม่มีโครงร่าง**
  มันจะตกไปใช้ `app/loading.tsx` ของ root ซึ่งผิดกว่าเดิม
- **แสงพื้นอยู่ที่ `html` ไม่ใช่ `.wrap`** และต้องมี `background-attachment: fixed` คู่กันเสมอ
- **`.nav` มี `view-transition-name: nav`** — Next ทำ view transition **สองรอบต่อการกดหนึ่งครั้ง**
  · **ชื่อห้ามซ้ำกันในหน้าเดียว** ซ้ำแล้วเบราว์เซอร์ยกเลิกทั้งชุดเงียบ ๆ
- **`lib/cardFlight.ts` ไม่ใช่ View Transition** — ต้องลบค่าที่จดทันทีที่อ่าน
- **token จังหวะ** `--t-fast` 140 · `--t-base` 220 · `--t-slow` 320 · `--ease`
  **ห้ามตั้งค่าใหม่เอง** · **ทุก `@keyframes` และ `transition` ใหม่ต้องมี `prefers-reduced-motion` คุม**
- **การสั่น (`lib/haptic.ts`) ห้ามพาการกระทำหลักล้มตาม** — iOS ไม่รองรับเลย

**ประตูที่สาม (`/kevin`)**

- **ผู้ช่วยในแอป "เสนอ" ได้ แต่ "เขียน" เองไม่ได้** (เปลี่ยน 2 ก.ย. 2026)
  tool ที่ขึ้นต้นด้วย `propose_` คืน**ร่าง** · การเขียนจริงอยู่ที่ `lib/applyDraft.ts`
  ซึ่งทำงานตอนผู้ใช้กดยืนยันเท่านั้น · `lib/ai/db.ts` ยังไม่มีเมธอดเขียน
  · **`test/guard.test.ts` ต้องผ่านโดยไม่ต้องแก้** — วันไหนต้องแก้เทสต์นั้นเพื่อให้ผ่าน แปลว่าผิดแล้ว
- **สิ่งที่ผู้ช่วยทำไม่ได้แม้จะเสนอ** — ลบวิชา (cascade ลบงานทั้งกอง) · ลบถาวร ·
  แตะ `project_schedules` · ที่ทำได้มากสุดคือเก็บเข้าคลัง ซึ่งกู้คืนได้ 7 วัน
- **ห้ามให้อะไรบนหน้าจอตัดสินจากชื่อ Area** — ผู้ใช้เปลี่ยนชื่อเองได้ตั้งแต่ 8 ก.ย.
  · ป้าย "วิชา/โปรเจกต์" เคยผูกกับ `area.name === 'Class'` แล้วเพี้ยนเงียบ ๆ
  · แก้แล้ว 9 ก.ย. โดย**เรียก "โปรเจกต์" ทั้งหมด** และมี `test/areaName.test.ts` กันไว้
- **ไม่มีตัวกรอง Area แล้ว** — `VISIBLE_AREAS` ถูกถอดทั้งกลไก 8 ก.ย. 2026 เพราะผู้ใช้
  เปลี่ยนชื่อ Area เองได้แล้ว และ allowlist ที่ผูกกับชื่อจะพังแบบเงียบ ๆ ทันที
  · ผู้ช่วยเห็นข้อมูลของผู้ใช้คนนั้นทั้งหมด · **ตัวที่กันข้ามคนคือ RLS**
- **รูปที่แนบในแชตไม่ถูกเก็บที่ไหนเลย** — ประวัติลงเป็น `[รูป]` และ**ประวัติที่ส่งกลับ
  เข้าโมเดลต้องเป็นข้อความล้วนเสมอ** · สะสม `contents` ไว้ข้ามเทิร์นเมื่อไหร่ รูปใบเดิม
  จะถูกส่งไป Google ซ้ำทุกครั้งที่ผู้ใช้พิมพ์ต่อ โดยไม่มีอะไรผิดบนจอเลย
  · **กติกาข้อ 12 (เรื่องรูป) อยู่ใน prompt ของแชตเท่านั้น** ห้ามย้ายเข้า `CORE`
  โดยไม่รันชุดวัด**ฝั่งโทร**ก่อนและหลัง · *(ประโยคปิดท้ายหลังเสนอร่างย้ายเข้า
  `CORE` แล้ว 9 ก.ย. 2026 หลังวัดฝั่งโทรครบทั้งสองรอบ — 9/11 → 10/11)*
- **ตัวตรวจที่หาคำไทยในคำตอบต้องผ่าน `spoken()` เสมอ** — Live API แทรกช่องว่าง
  กลางคำ (`บันทึก ให้แล้ว ครับ`) แล้วด่าน `fakeSuccessWords` ตาบอดทั้งฝั่งโทร
- **`doc/CLAUDE-PROJECT-PROMPT.md` กับ `doc/SCHEMA.sql` ต้องเอาไปวางใน Claude Project ด้วยมือ**
  แก้ไฟล์แล้วของจริงบน claude.ai ยังไม่เปลี่ยนจนกว่าจะไปวางเอง

---

## ความลับเก็บที่ไหน

VAPID private key → Supabase secrets · VAPID public key → Vercel env (`NEXT_PUBLIC_`)
service_role key → Supabase Vault ชื่อ `kevin_cron_token` (ห้ามฝังใน cron เป็น literal)
`GEMINI_API_KEY` → Vercel Secret · **ห้ามมี `NEXT_PUBLIC_` นำหน้า**
สำเนา VAPID อยู่ที่ `web/.vapid-backup.json` ซึ่ง gitignore ไว้แล้ว — **ห้าม regenerate**

## สภาพแวดล้อม

- Windows · เชลล์หลักเป็น PowerShell
- **Supabase MCP ต่ออยู่แล้ว** — รัน migration และ query ได้เลยผ่าน `apply_migration`
  / `execute_sql` **ไม่ต้องให้เจ้าของไปเปิด SQL Editor เอง** · แต่ **`supabase` CLI
  ไม่เคย `link`** (ไม่มี `config.toml`) ดังนั้น `db push` และ `functions deploy`
  ใช้ไม่ได้จนกว่าจะ login · project ref คือ `minfibikrogtptozszxv`
- git remote `origin` → https://github.com/nicetsu/KeviN (private)
- `gh.exe` อยู่ที่ `C:\Users\LENOVO\AppData\Local\gh\` และอยู่ใน PATH แล้ว
  git credential helper ชี้มาที่ไฟล์นี้ — **ถ้าไฟล์หาย `git push` จะพัง**
- `npm run dev --prefix web` เปิดที่ **พอร์ต 3000**
- deploy ต้องใส่ `--scope nicetsuuu` ทุกครั้ง ไม่งั้น Vercel CLI ตอบ `Not authorized`
- **`next build` ล้มบนเครื่องนี้เครื่องเดียว · build บน Vercel ผ่าน** — อย่าเสียเวลาไล่
  ตรวจของก่อน deploy ให้ใช้ `npx tsc --noEmit` + `npm test` + `npm run lint`
