# PLAN — KeviN

อัปเดตล่าสุด **19 ส.ค. 2026** · สถานะ: **ครบทั้ง 6 เฟส · ใช้งานจริงอยู่**
เว็บ https://kevin-rose.vercel.app · โค้ดอยู่ใน `web/` · repo https://github.com/nicetsu/KeviN (private)

> งานที่ทำหลังครบ 6 เฟสแล้วอยู่ท้ายไฟล์ หัวข้อ **"หลังเฟส 6"**

---

## KeviN คืออะไร

ผู้ช่วยส่วนตัวที่เก็บทุกอย่างไว้บนคลาวด์ที่เดียว แล้วเปิดสองประตูให้เข้าถึง

- **ประตูที่คุยได้** — Claude ต่อกับฐานข้อมูลผ่าน MCP ใช้ได้ทั้งบนคอมและในแอปมือถือ เหมาะกับตอนนึกอะไรออกแล้วอยากโยนเข้าระบบเร็ว ๆ
- **ประตูที่มองเห็น** — เว็บแอป PWA แสดงทุกอย่างเป็นภาพ ทั้งรายการและปฏิทิน เหมาะกับตอนนั่งวางแผน

ทั้งสองประตูอ่านเขียนตารางเดียวกัน ไม่มีการ sync ไม่มีข้อมูลสองชุด

## โครงสร้างข้อมูล

```
Area (Class / Hackathon / Financial / Personal)
└── Project (รายวิชา / โปรเจกต์)
    ├── Schedule    ช่วงเวลาประจำ · ใส่ได้หลายช่วง · ไม่แจ้งเตือน
    ├── Task        มีสถานะเสร็จ · due_at ไม่บังคับ
    ├── Reminder    remind_at บังคับ · push เข้ามือถือ · เตือนครั้งเดียว
    └── Shortnote   ไม่มีเวลา · รายการธรรมดาในหน้า project
```

## Stack

| ชั้น | เลือกใช้ |
|---|---|
| ฐานข้อมูล | Supabase (Postgres + RLS) |
| ให้ Claude เข้าถึง | Supabase remote MCP `https://mcp.supabase.com/mcp` |
| เว็บแอป | Next.js บน Vercel · PWA |
| แจ้งเตือน | Web Push (VAPID) · pg_cron + Edge Function |
| ผู้ช่วยที่คิดเป็น | Claude Cowork scheduled task |

---

## เฟส 0 · วางฐานข้อมูล

**เป้าหมาย:** ฐานข้อมูลที่ใส่ข้อมูลผิดรูปไม่ได้

> 📓 คู่มือลงมือทีละขั้นพร้อมสคริปต์ตรวจ อยู่ที่ [`doc/phase0/README.md`](doc/phase0/README.md)
> `SCHEMA.sql` ผ่านการรันและทดสอบจริงบน Postgres 18 แล้ว — **เจอบั๊ก 3 ข้อและแก้แล้ว** อ่านท้าย README

- [x] สร้างโปรเจกต์ Supabase · จด `project_ref` ไว้
- [x] สร้างบัญชีผู้ใช้ใน Authentication → Users (ตารางทุกใบอ้าง `auth.users` จึงต้องมีก่อนใส่ Area)
- [x] รัน [`doc/SCHEMA.sql`](doc/SCHEMA.sql) ทั้งไฟล์
- [x] ยืนยันว่า RLS เปิดครบทุกตาราง (`select * from pg_tables where rowsecurity = false and schemaname='public'` ต้องว่าง)
- [x] ทดสอบ CHECK constraint ว่าทำงานจริง — ลอง insert `reminder` ที่ไม่มี `remind_at` ต้องโดนปฏิเสธ
- [x] ใส่ Area ทั้ง 4 และวิชาที่เรียนอยู่จริงในเทอมนี้
- [x] กรอกตารางเรียนจริงลง `project_schedules` อย่างน้อย 1 วิชา แล้วเรียก `schedule_occurrences()` ดูว่าวันที่ออกมาถูก

**เสร็จเมื่อ:** query `schedule_occurrences(current_date, current_date + 7)` คืนคาบเรียนสัปดาห์นี้ได้ถูกต้อง

✅ **ผ่านแล้ว 18 ส.ค. 2026** — 8 วิชา 11 คาบ 187 occurrence ทั้งเทอม · บวก project UniHack 2026 ใน Area Hackathon

---

## เฟส 1 · ต่อ Claude เข้ากับฐานข้อมูล

**เป้าหมาย:** ใช้งานได้จริงแล้ว ก่อนที่จะมีเว็บสักหน้า

- [x] claude.ai → Settings → Connectors → เชื่อม Supabase (OAuth ผ่านเบราว์เซอร์)
- [x] จำกัดขอบเขตด้วย query parameter — **ห้ามใส่ `read_only=true`** เพราะต้องเขียนข้อมูล
      ```
      https://mcp.supabase.com/mcp?project_ref=minfibikrogtptozszxv&features=database,docs
      ```
- [x] สร้าง Project ใน Claude ชื่อ KeviN แล้วเปิดใช้ connector สำหรับ project นี้
- [x] วาง [`doc/CLAUDE-PROJECT-PROMPT.md`](doc/CLAUDE-PROJECT-PROMPT.md) เป็น custom instructions
- [x] แนบ `doc/SCHEMA.sql` เข้า project knowledge (ประหยัด tool call ตอนเปิดแชตใหม่)
- [x] ทดสอบบนเว็บ: เพิ่มงาน · ถามว่าวิชานี้เหลืออะไร · ลองสั่งลบดูว่าถามยืนยันจริงไหม
- [x] เปิดแอปมือถือ ยืนยันว่า connector โผล่เอง

**เสร็จเมื่อ:** พิมพ์ "คาบแคลพรุ่งนี้มี quiz" บนมือถือแล้วข้อมูลลง DB ถูกที่ถูกชนิด

✅ **ผ่านแล้ว 18 ส.ค. 2026** — ทดสอบครบ 3 อย่าง (อ่านตาราง · ทวนก่อนเขียน · ถามก่อนลบ)
ตรวจ DB จริงผ่าน MCP ซ้ำแล้ว: 9 project · 11 คาบ · 187 occurrence · ทุกแถวเป็นของ user ที่ถูกต้อง

**หมายเหตุ:** connector นี้ต้องใช้ Claude แพลนเสียเงิน (Pro/Max/Team/Enterprise) และเพิ่มได้จากเว็บเท่านั้น มือถือเพิ่มเองไม่ได้

---

## เฟส 2 · เว็บแอปแบบดูอย่างเดียว

**เป้าหมาย:** เห็นภาพรวมทั้งระบบในหน้าจอเดียว

- [x] `create-next-app` · ต่อ Supabase client (ใช้ **anon key** เท่านั้น ห้ามใช้ service_role)
- [x] วาง design token จาก [`doc/globals.css`](doc/globals.css)
- [x] โหลดฟอนต์ Inter + Noto Sans Thai ผ่าน `next/font/google`
- [x] หน้า S1 "วันนี้"
- [x] **หน้า login + auth guard** (แผนเดิมไม่มี แต่ RLS ผูกกับ `auth.uid()` จึงขาดไม่ได้)
- [x] หน้า S4 คลัง และ S5 หน้า project
- [x] แถบนำทาง (มือถือ = แถบล่าง · เดสก์ท็อป = แถบข้างซ้าย)
- [x] deploy Vercel → **https://kevin-rose.vercel.app**
- [x] ~~**งานค้างจากเฟสออกแบบ:** อัปเดต `ux.html` ให้ใช้จานสีใหม่ · ออกแบบหน้าที่เหลือ~~ **เสร็จ 18 ส.ค.** — `ux.html` เป็น Nightfall แล้ว ครบ S1–S9 + สถานะพิเศษ 6 แบบ

**เสร็จเมื่อ:** เปิดเว็บบนมือถือแล้วเห็นงานวันนี้ครบ ตรงกับที่อยู่ใน DB

✅ **เฟส 2 เสร็จแล้ว** — https://kevin-rose.vercel.app
S1 · S4 · S5 ตรงกับ DB ทุกแถว · ยืนยันบนมือถือจริงแล้ว ครบเกณฑ์เสร็จของเฟสนี้
รันด้วย `npm run dev --prefix web` → http://localhost:3001

---

## เฟส 3 · ปฏิทินสองโหมด

**เป้าหมาย:** เห็นตารางเรียนทั้งเทอมโดยกรอกครั้งเดียว

- [x] โหมดเดือน (S2)
- [x] โหมดสัปดาห์ (S3) — **วัน = แกนตั้ง, เวลา = แกนนอน** วางบล็อกด้วย `grid-row: วัน` + `grid-column: เวลาเริ่ม / span ชั่วโมง`
- [x] ตัวจัดการช่วงเวลาประจำ (S6) — ใส่หลายช่วง · สองโหมดการซ้ำ · **ตัวอย่างผลลัพธ์อัปเดตสดขณะพิมพ์**
- [x] ตรรกะคลี่ `week_offsets` → วันที่จริง (ใช้ `schedule_occurrences()` ที่มีใน schema แล้ว)
- [x] จัดการบล็อกที่เวลาซ้อนกัน — แบ่งความสูงในแถวของวันนั้น

**เสร็จเมื่อ:** กรอกตารางเรียนทั้งเทอมของวิชาหนึ่งจบใน 2 นาที แล้วเห็นถูกต้องทุกสัปดาห์

✅ **เฟส 3 เสร็จแล้ว 19 ส.ค. 2026** — https://kevin-rose.vercel.app/calendar
ตรรกะแบ่งความสูงตอนคาบชนกันแยกไว้ที่ `web/lib/layout.ts` มีเทสต์ 7 เคส รวมเคสชนต่อเนื่อง
S6 อยู่ที่ `/project/<id>/schedule` · ตรรกะการซ้ำอยู่ใน `web/lib/weeks.ts` มีเทสต์ 20 เคส
และเทียบกับ `schedule_occurrences()` ของ DB บนข้อมูลจริงแล้วตรงกัน 4/4

---

## เฟส 4 · จัดการได้บนเว็บ

- [x] เพิ่ม ลบ แก้ ติ๊กเสร็จ จัดลำดับ
- [x] แผงรายละเอียด (S7) — เปิดทับหน้าเดิม ปิดแล้วกลับที่เดิมพร้อมตำแหน่งเลื่อน
- [x] เพิ่มเร็ว (S8) — พิมพ์บรรทัดเดียว ตีความให้ก่อนแล้วให้แก้
- [x] archive project
- [x] ลบแล้วมีแถบ "เลิกทำ" ค้าง 8 วินาที

**เสร็จเมื่อ:** ใช้เว็บทำได้ทุกอย่างที่ Claude ทำได้

✅ **เฟส 4 เสร็จแล้ว** — ติ๊กเสร็จ · แก้ · เก็บเข้าคลัง · เลิกทำ 8 วิ · S7 แผงรายละเอียด · S8 เพิ่มเร็ว · archive project · ลากจัดลำดับ
action ทั้งหมดอยู่ที่ `web/app/actions/items.ts` · ลบใช้ `archived_at` ไม่ใช่ DELETE

**ลากจัดลำดับ** — ที่จับอยู่ใน `web/components/ItemList.tsx` ใช้ Pointer Events (ใช้ได้ทั้งสัมผัสและเมาส์)
พร้อมลูกศรขึ้น/ลงสำหรับแป้นพิมพ์ · โผล่เฉพาะแถวที่ `sort_order` เป็นตัวตัดสินลำดับจริง คือ
โน้ตทั้งกลุ่ม และงานที่ยังไม่เสร็จและไม่มีวันกำหนด — กลุ่มเตือนไม่มีที่จับเพราะเรียงตาม `remind_at`
ตอนทำเจอกับดัก `update()` ที่ไม่มี `.select()` แล้วไม่เขียนอะไรเลย บันทึกไว้ใน [`doc/TRAPS.md`](doc/TRAPS.md)

---

## เฟส 5 · PWA และการแจ้งเตือน

**เป้าหมาย:** reminder เด้งเข้ามือถือจริง แม้ปิดเว็บอยู่

- [x] manifest + service worker · ติดตั้งลงหน้าจอโฮมได้
- [x] สร้างคู่กุญแจ VAPID **ครั้งเดียว** เก็บให้ดี ห้าม regenerate
- [x] ขอสิทธิ์แจ้งเตือน เก็บ subscription ลง `push_subscriptions`
- [x] Edge Function `send-reminders` — เรียก `claim_due_reminders()` แล้วยิง push
- [x] ตั้ง pg_cron รันทุกนาที
- [x] หน้า S9 ตั้งค่าแจ้งเตือน + คำแนะนำ iOS (แสดงเฉพาะเมื่อตรวจพบ Safari บน iOS ที่ยังไม่ได้ติดตั้ง)
- [x] ปุ่ม "ส่งการแจ้งเตือนทดสอบ"

**เสร็จเมื่อ:** ตั้ง reminder อีก 2 นาที ปิดเว็บ แล้วมันเด้งขึ้นมาจริง

✅ **เฟส 5 เสร็จแล้ว 19 ส.ค.** — push ส่งออกจริง
pg_cron ยิงทุกนาที → Edge Function ตอบ 200 `{"ok":true,"claimed":0,"sent":0}`
VAPID เก็บใน Supabase secrets + Vercel env · service_role key อยู่ใน Vault ไม่ใช่ใน cron
log ยืนยัน: 09:54 `skipped` (ไม่มีเครื่อง) → 09:55 `{"claimed":1,"sent":1}` หลังสมัครรับ
**บั๊กที่เจอ:** UI เคยถือว่า permission granted = ตั้งค่าเสร็จ ซึ่งคนละเรื่องกับการสมัครรับ
และ Edge Function เคยอ้างสิทธิ์ทั้งที่ไม่มีเครื่องรับ ทำให้ reminder หายเงียบ ๆ — แก้ทั้งสองแล้ว
- [x] กดอนุญาตแจ้งเตือนบนมือถือ แล้วกดปุ่มส่งทดสอบที่ `/settings`

---

## เฟส 6 · ผู้ช่วยที่คิดเป็น

- [x] Cowork scheduled task ทุกเช้า 06:30 — ทดสอบแล้ว สรุปถูกต้อง รวมถึงหาช่องว่างระหว่างคาบเองได้
- [x] รายสัปดาห์ (อาทิตย์ 19:00) — ตั้งใน Cowork แล้วจาก [`doc/phase6/prompt-weekly.txt`](doc/phase6/prompt-weekly.txt)

✅ **เฟส 6 เสร็จแล้ว** — scheduled task ครบทั้งสองตัว

**ทางเลือกอนาคต:** ฝังแชตในแอปเองผ่าน Anthropic API — ดู [`doc/DECISIONS.md`](doc/DECISIONS.md) หัวข้อ "API ในแอปตัวเอง" ประเมินไว้แล้วว่าราว $3–17/เดือน แต่**ยังไม่ทำ** เพราะทาง MCP ไม่มีค่าใช้จ่ายเพิ่ม

---

## หลังเฟส 6 · งานที่ทำเพิ่ม (19 ส.ค. 2026)

งานที่ไม่ได้อยู่ในแผนเดิม ทำหลังครบทั้ง 6 เฟสแล้ว

**ตารางเรียนตรงกับ syllabus จริง** — เจ้าของส่ง syllabus วิชา 01219118 มา ซึ่งเผยว่ามี
ช่องว่าง **สองช่วง** ไม่ใช่ช่วงเดียวอย่างที่เอกสารเดิมรู้ · ตัด `week_offsets` 7
(สัปดาห์สอบกลางภาค 21–27 ก.ย.) และ 9 (Commencement Week 5–11 ต.ค.) ออกจากทุกคาบ
เหลือ 15 สัปดาห์ ตรงกับจำนวน Week ใน syllabus พอดี · occurrence ทั้งเทอม 187 → 165

**ขึ้น GitHub** — `origin` → https://github.com/nicetsu/KeviN แบบ private
ตรวจแล้วว่าไม่มีคีย์จริงหลุดไปในประวัติ commit สักตัว

**เร่งความเร็ว** — เจ้าของบอกว่ากดแล้วหน่วง พอวัดจริงพบว่า Vercel รันฟังก์ชันที่ `iad1`
(เวอร์จิเนีย) ขณะที่ Supabase อยู่ `ap-northeast-1` (โตเกียว) — **ทุก query ข้ามแปซิฟิกไปกลับ**

- ตรึง region ไว้ที่ `hnd1` ผ่าน `web/vercel.json`
- เลิกเรียก `getUser()` ใน server action ทั้งหมด (มันเป็น network call ทุกครั้ง)
- ใส่ `loading.tsx` ครบทุกหน้า
- **ผลวัดจริงหลัง deploy** TTFB หน้า `/login` ค่ากลาง **445 ms → 268 ms**
  และ `X-Vercel-Id` เปลี่ยนจาก `sin1::iad1` เป็น `sin1::hnd1`

**เก็บกฎ React สองข้อที่ค้าง** — `Date.now()` กลางตัว component (แก้ด้วย `connection()`)
และ setState ใน effect ที่ทำให้หน้าคลังกระพริบ (ย้ายสถานะกาง/หุบไปเก็บใน cookie
ตรรกะอยู่ที่ `web/lib/libraryOpen.ts` มีเทสต์ 16 เคส)

**การ์ดคาบถัดไปมองข้ามวันได้** — เดิมหายไปทั้งใบหลังคาบสุดท้ายของวันเลิก
ตอนนี้โชว์คาบของวันถัดไปแทน เหตุผลอยู่ใน [`doc/DECISIONS.md`](doc/DECISIONS.md)

---

## กิจกรรม (events) · 21 ส.ค. 2026

เพิ่มชั้นข้อมูลใหม่ให้เก็บ "งานที่เกิดครั้งเดียวแต่มีกำหนดการข้างใน" ได้ — เช่น UniHack ที่มี
Orientation Day · รับ Booklet · Mentoring Session · Final Round Pitching Day และแต่ละงานมีรันดาวน์ของตัวเอง

**รอบ 1 (เสร็จแล้ว)**

- [x] ตาราง `events` + `event_agenda` + `items.event_id` · RLS ครบ · migration อยู่ที่ `supabase/migrations/`
- [x] ฟังก์ชัน `calendar_entries(from, to)` รวมคาบเรียนกับกิจกรรม และหั่นงานข้ามคืนเป็นบล็อกรายวัน
- [x] ปฏิทินสัปดาห์/เดือนแสดงกิจกรรม · เข้าตัวจัดบล็อกชนกันตัวเดิม
- [x] การ์ดใหญ่หน้าวันนี้รับกิจกรรม · "กำลังเกิดขึ้น" · บรรทัดล่าง "ถัดไป / ทับกับ"
- [x] หน้ากิจกรรมเป็น route ของตัวเอง · กลุ่ม "กิจกรรม" ในหน้าวิชา
- [x] สร้าง/แก้/เก็บเข้าคลัง + ตัวแก้กำหนดการพร้อมตัวอย่างสด + ปุ่มเพิ่มงานที่ผูกกับกิจกรรม
- [x] URL กับอีเมลใน `body` กดได้ (ไม่แตะ schema · ไม่ใช้ `innerHTML`)
- [x] `doc/CLAUDE-PROJECT-PROMPT.md` สอน Claude ให้รู้จักกิจกรรมและเส้นแบ่งสามเส้น
- [x] ตรรกะใหม่มีเทสต์ 23 เคส (`calendar.ts` · `agenda.ts` · `layout.ts` เคสกิจกรรมชนคาบเรียน)

ทดสอบ constraint กับ DB จริงแล้วผ่าน 8/8 — รวมเคสผูกงานข้าม project ไม่ได้
และเคสลบกิจกรรมแล้วงานยังอยู่ (`set null` ไม่ใช่ cascade)

**รอบ 2 (เสร็จแล้ว)** — ตัดทอนเวลาและการไม่ไป

- [x] ตาราง `time_offsets` · ชี้ไปที่คาบเรียนหรือกิจกรรมอย่างใดอย่างหนึ่ง + วันที่
- [x] `calendar_entries()` เอาการตัดทอนมาใช้ · คืน `skipped` กับ `trimmed` เพิ่ม
- [x] ปฏิทินและหน้าวันนี้ทำสิ่งที่ไม่ไปให้จางลงและติดป้าย **ไม่ซ่อนทิ้ง**
- [x] การ์ดใหญ่ไม่หยิบสิ่งที่ตั้งใจไม่ไปมาเป็น "ถัดไป"
- [x] ปุ่มคืนค่าในหน้าวันนี้ · เว็บตั้งค่าตัดทอนไม่ได้ ทำได้แค่คืน
- [x] prompt สอน Claude ให้ตรวจ ถามว่าจะแบ่งตรงไหน แล้วเขียนให้ทั้งสองฝั่ง
- [x] ทดสอบกับ DB จริงผ่าน 10/10 รวมเคสเวลานอกช่วงถูกเมิน · สัปดาห์อื่นไม่โดน · `week_offsets` ไม่ถูกแตะ

**เวลาจริงของมหาลัยและของผู้จัดไม่เคยถูกแตะเลย** — คืนค่าคือลบแถวทิ้ง
เหตุผลทั้งหมดอยู่ใน [`doc/DECISIONS.md`](doc/DECISIONS.md) หัวข้อ "เวลาทับกันและการไม่ไป"

---

## เทสต์กลับเข้า repo · 30 ส.ค. 2026

เทสต์ที่กำกับตรรกะแกนเคยรันนอก repo (`tsc` + `node` ตอนพัฒนา) แล้ว**หายไปกับเครื่อง** —
เอกสารอ้าง 81 เคสที่ไม่มีใครรันซ้ำได้จาก checkout สด ซึ่งเท่ากับไม่มีเทสต์เลย

- [x] `web/test/` · 147 เคส ครอบ 7 ไฟล์ (`time` 44 · `weeks` 28 · `parse` 21 · `libraryOpen` 18 · `agenda` 15 · `calendar` 11 · `layout` 10)
- [x] `npm test --prefix web` — คอมไพล์ด้วย `tsconfig.test.json` แล้วรันด้วย `node --test`
- [x] ไม่เพิ่ม dependency สักตัว (ใช้ `node:test` + `node:assert` ที่มากับ node)
- [x] ตรวจว่าเทสต์กัดจริงด้วย mutation 3 จุด (24:00 · ขอบบล็อกชนกัน · null/[] ของ cookie) → พัง 6 เคสตามคาด

**ชุดนี้เขียนขึ้นใหม่ ไม่ใช่ของเดิมที่กู้กลับมา** — ไล่ตามกับดักใน `doc/TRAPS.md` ทุกข้อแล้ว
แต่ไม่รับประกันว่าครอบเท่าของเดิมทุกเคส

`lib/time.ts` เดิมไม่เคยมีเทสต์เลยทั้งที่เป็นไฟล์ที่ทุกหน้าจอเรียกใช้ — เคสที่คุ้มที่สุดคือ
**ช่วง 17:00–24:00 UTC** ที่วันไทยเดินไปข้างหน้าแล้วแต่วัน UTC ยังไม่เปลี่ยน
ถ้าพลาดตรงนั้น หน้าวันนี้จะโชว์ของเมื่อวานจนถึงเจ็ดโมงเช้าโดยไม่มีอะไรฟ้อง

**เก็บ lint ที่ค้าง** — `app/settings/NotificationSetup.tsx` อ่านความสามารถของเบราว์เซอร์
ใน `useEffect` แล้ว `setState` ทันที (cascading render) · ย้ายไป `useSyncExternalStore`
แบบเดียวกับ `components/Hero.tsx` · เก็บ `no-unused-expressions` ใน `LibraryTree.tsx` ด้วย
· `npm run lint` เหลือ **0 error 0 warning**

---

## ประตูที่สาม · แชตและเสียงในเว็บแอป — วางแผน 30 ส.ค. 2026

**เป้าหมาย:** สั่งงาน KeviN บนมือถือโดยไม่ต้องสลับไปเปิดแอป Claude ทั้งพิมพ์และพูด
**ขอบเขต: อ่านอย่างเดียว ไม่มีสิทธิ์แก้** · **ต้องฟรีทั้งหมด**

> 📐 ดีไซน์เต็มอยู่ที่ [`doc/CHAT.md`](doc/CHAT.md)
> ฉบับภาพ: [pipeline](https://claude.ai/code/artifact/0453a1ca-a08e-4c7f-b845-314aba3d26a9) · [UX/UI](https://claude.ai/code/artifact/0106dd44-993c-48fc-ad90-513c7fcae7b6)

**⛔ ยังเริ่มไม่ได้จนกว่าเจ้าของจะเคาะสองข้อ** (ดู `doc/CHAT.md` §0)
กลับมติ `DECISIONS.md` เรื่อง “API ในแอปตัวเอง” · และยกเว้นกฎ `TRAPS.md` เรื่อง Edge Function

---

### ขั้น 0 · ฟังเสียงจริงก่อน ยังไม่เขียนโค้ด

- [ ] เปิด Google AI Studio คุยกับ Live API เป็นภาษาไทย
- [ ] ตัดสินด้วยหูตัวเองว่าถึงระดับ “คุยเหมือนเพื่อน” ที่ต้องการไหม
- [ ] ดูหน้า rate limit ของบัญชีว่า **Live API อยู่ใน free tier ไหม** และโควตาเท่าไหร่

**เสร็จเมื่อ:** ตอบได้ว่าเดินหน้าต่อด้วย Live API หรือต้องถอยไปใช้ Web Speech + `speechSynthesis`

> ขั้นนี้มาก่อนทุกอย่างเพราะตอบคำถามที่เหลือทั้งหมดโดยไม่ต้องลงแรงเลย
> ถ้าไม่ผ่าน ขั้น 3 หายไปทั้งขั้น และควรรู้ตั้งแต่วันแรก ไม่ใช่หลังเขียนไปครึ่งทาง

### ขั้น 1 · ชั้น tool อ่านอย่างเดียว

- [ ] `lib/ai/tools/registry.ts` — นิยาม tool ครั้งเดียว ใช้ร่วมทั้งสองช่องทาง
- [ ] ตัวกรอง Area — `Financial` และ `Personal` ไม่ออกจากเครื่อง
- [ ] `/api/read/*` รับเฉพาะชื่อ tool ใน allowlist
- [ ] ชั้นที่สาม: role/RPC ที่ทำได้แค่ `select` (**ตรวจก่อนว่าแพลนที่ใช้ทำได้จริงไหม**)
- [ ] `prompt.ts` สร้างจาก `doc/CLAUDE-PROJECT-PROMPT.md` ต้นฉบับเดียว

**เสร็จเมื่อ:** ยิง `/api/read/*` ตรง ๆ แล้วได้ข้อมูลถูก · และ**ลองสั่งเขียนแล้วต้องไม่ผ่านทั้งสามชั้น**

### ขั้น 2 · แชตข้อความ

- [ ] ช่อง `KeviN` ในแถบล่าง — แคปซูลม่วงจาง **ไม่ใช่วงกลมยกขึ้น** (ชนกับปุ่ม + ที่มีอยู่)
- [ ] หน้า `KeviN` + segmented control สลับโหมด
- [ ] `/api/chat` streaming · `runtime = 'nodejs'` · region `hnd1`
- [ ] การ์ดทางลัด — คำปฏิเสธพาไปหน้าที่แก้ได้
- [ ] ตาราง `conversations` · `messages` · `tool_calls` + RLS

**เสร็จเมื่อ:** พิมพ์ถามบนมือถือแล้วได้คำตอบที่ตรงกับ DB · **ใช้งานได้จริงโดยไม่ต้องรอเสียง**

### ขั้น 3 · โหมดโทร

- [ ] `/api/voice/token` แจก ephemeral token ที่ผูก config ตายแล้ว
- [ ] จับไมค์ → PCM16 16kHz · เล่นกลับ 24kHz
- [ ] **หน้าโทรต้องมีสถานะนิ่งเป็นค่าตั้งต้น กดปุ่มก่อนถึงเริ่ม**
- [ ] tool call เด้งผ่านเบราว์เซอร์เข้า `/api/read/*` (ห้ามลัดไป Supabase ตรง)
- [ ] คำบรรยายขึ้นตลอดสาย
- [ ] ต่อสายอัตโนมัติที่ 15 นาที ด้วย `sessionResumption` handle

**เสร็จเมื่อ:** คุยยาวเกิน 15 นาทีโดยไม่รู้สึกว่าสายขาด และ**ตัวจับเวลาไม่รีเซ็ต**

### ขั้น 4 · เก็บให้ครบ

- [ ] ประวัติร่วมกัน — วางสายแล้วพิมพ์ต่อได้โดยจำบทสนทนาเดิม
- [ ] แถบ “กำลังคุย” ตามไปทุกหน้าขณะสายเปิดอยู่
- [ ] ล็อกหน้าจอ = วางสายให้ พร้อมบอก
- [ ] โควตาหมด = ดันไปโหมดแชต
- [ ] `tool ล่ม` ต้องบอกว่าดึงข้อมูลไม่ได้ **ห้ามตอบว่าไม่มีอะไร**

**เสร็จเมื่อ:** กฎทั้ง 8 ข้อในตารางของ `doc/CHAT.md` §8 ทำครบ

---

## หลักการ UX ห้าข้อ (ใช้ตัดสินทุกครั้งที่ลังเล)

1. เปิดมาต้องตอบได้ทันทีว่าตอนนี้ต้องทำอะไร — โครงสร้างสามชั้นเป็นวิธีเก็บ ไม่ใช่วิธีดู
2. สีมีความหมายเดียว ใช้ตรงกันทั้งระบบ ไม่มีสีไหนใช้เพื่อความสวย
3. งานที่ทำบ่อยต้องกดน้อยที่สุด
4. ปฏิทินไว้ดู คลังไว้แก้ — ไม่พยายามให้ทุกหน้าทำได้ทุกอย่าง
5. ไม่มีอะไรหายเงียบ ๆ — ลบต้องยืนยัน archive กู้คืนได้ Claude เขียนอะไรต้องบอก

## สิ่งที่ตั้งใจไม่ทำในเวอร์ชันแรก

ลากย้ายในปฏิทิน · reminder เตือนซ้ำ · งานย่อยใน task · ไฟล์แนบ · ซิงก์ Google Calendar · โหมดหลายคน
