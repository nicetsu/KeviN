# HISTORY — เดินมายังไง และเหลืออะไร

ยุบมาจาก `PLAN.md` เดิม เมื่อ 5 ก.ย. 2026 · **ทุกเฟสเสร็จหมดแล้ว** แผนรายข้อจึงกลายเป็นประวัติศาสตร์
สิ่งที่ยังมีชีวิตอยู่คือหัวข้อ **"สิ่งที่ยังไม่ได้ทำ"** ท้ายไฟล์

> เหตุผลของแต่ละการตัดสินใจอยู่ที่ [DECISIONS.md](DECISIONS.md) · ระบบทำงานยังไงอยู่ที่
> [ARCHITECTURE.md](../ARCHITECTURE.md) · ของที่พังเงียบ ๆ อยู่ที่ [TRAPS.md](TRAPS.md)

---

## ไทม์ไลน์

| เมื่อไหร่ | ทำอะไร | เกณฑ์ "เสร็จ" ที่ใช้วัด |
|---|---|---|
| **17–18 ส.ค.** | **เฟส 0 · ฐานข้อมูล** — schema + RLS + CHECK + `schedule_occurrences()` | `schedule_occurrences(today, +7)` คืนคาบสัปดาห์นี้ถูกต้อง · ได้ 8 วิชา 11 คาบ **187 occurrence ทั้งเทอม** |
| **18 ส.ค.** | **เฟส 1 · ต่อ Claude ผ่าน MCP** — ใช้งานได้จริงก่อนมีเว็บสักหน้า | พิมพ์ "คาบแคลพรุ่งนี้มี quiz" บนมือถือแล้วข้อมูลลง DB ถูกที่ถูกชนิด |
| **18 ส.ค.** | **เฟส 2 · เว็บดูอย่างเดียว** + ทาสี `ux.html` เป็น Nightfall ครบ S1–S9 | เปิดเว็บบนมือถือแล้วเห็นงานวันนี้ครบ ตรงกับ DB ทุกแถว |
| **19 ส.ค.** | **เฟส 3 · ปฏิทินสองโหมด** | กรอกตารางทั้งเทอมของวิชาหนึ่งจบใน 2 นาที แล้วถูกทุกสัปดาห์ |
| **19 ส.ค.** | **เฟส 4 · จัดการบนเว็บ** — เพิ่ม ลบ แก้ ติ๊ก เลิกทำ 8 วิ · ลากจัดลำดับ | ใช้เว็บทำได้ทุกอย่างที่ Claude ทำได้ |
| **19 ส.ค.** | **เฟส 5 · PWA + แจ้งเตือน** — pg_cron → Edge Function → Web Push | ตั้ง reminder อีก 2 นาที ปิดเว็บ แล้วมันเด้งขึ้นมาจริง |
| **19 ส.ค.** | **เฟส 6 · Cowork** — สรุปเช้า 06:30 + สรุปรายสัปดาห์ อาทิตย์ 19:00 | scheduled task ครบทั้งสองตัว |
| **19 ส.ค.** | **จูนความเร็ว** — ตรึง region `hnd1` · เลิกใช้ `getUser()` ใน action | TTFB `/login` 445 → **268 ms** |
| **21 ส.ค.** | **กิจกรรม** — `events` · `event_agenda` · **และ** `time_offsets` (ตัดทอนเวลา/ไม่ไป) | ปฏิทินวาด event ใบเดียว · คาบที่ไม่ไปจางอยู่ที่เดิม ไม่หาย |
| **30 ส.ค.** | **เทสต์กลับเข้า repo** — ของเดิมรันนอก repo แล้วหายไปกับเครื่อง | สิ่งที่รันไม่ได้จาก checkout สด เท่ากับไม่มี |
| **30–31 ส.ค.** | **ประตูที่สาม ขั้น 1–4** — ชั้น tool อ่านอย่างเดียว · แชต · โหมดโทร · ประวัติร่วม | ขึ้น production แล้วใช้จริงบนมือถือ |
| **1 ก.ย.** | **Redesign รอบสอง** — `/library/archive` · แถบล่างมีไอคอน · พื้นอุ่นขึ้น · เวิร์ดมาร์ก KEVIN · FLIP · การ์ดที่บินไป | จอบอกความจริงให้ครบ ไม่ใช่ทำใหม่เพราะเบื่อของเดิม |
| **2 ก.ย.** | **ให้ผู้ช่วยแก้ข้อมูลได้** — `propose_*` + การ์ดยืนยัน + `applyDraft` | พูดขณะเดินกลับหอแล้วงานลง DB ถูกโดยไม่ต้องหยุดเดิน |
| **4 ก.ย.** | **วัดกับโมเดลจริงครั้งแรก** (`web/test/accuracy/`) | รอบแรก แชต 13/15 · โทร 3/6 → หลังแก้บั๊ก+จูน prompt **15/15 · 6/6** |
| **5 ก.ย.** | **แชตไหลทีละคำ** · `/kevin/history` · `propose_update_draft` · การ์ดซ้อนแบบ 06 · เทสต์ของทางเขียนจริง 28 เคส | วัดซ้ำ **แชต 17/17 · โทร 7/7** · เทสต์รวม **394 เคส** |
| **8 ก.ย.** | **เปิดให้ใช้หลายคน** — รหัสเชิญบังคับที่ DB · seed Area อัตโนมัติ · Area สร้าง/แก้/ลบเองได้ · ยืนยันอีเมล · ออกจากระบบ · ถอด `VISIBLE_AREAS` · แก้บั๊กการเตือนข้ามคน · แยก 429 เป็นวัน/นาที | ไม่ต้องแตะโครงข้อมูลเลยสักตาราง เพราะ `user_id` + RLS อยู่ครบตั้งแต่วันแรก · เทสต์รวม **393 เคส** |

**เทสต์โตจาก 147 เคส (ก่อนเริ่มประตูที่สาม) → 301 (จบประตูที่สาม) → 394 → 393 (วันนี้)**
· ลดลงเพราะ**ลบเทสต์ของตัวกรอง Area ทิ้ง 12 เคส** ตอนถอดกลไกนั้นออก ไม่ใช่เพราะมีอะไรพัง

---

## บทเรียนที่ราคาแพงที่สุด

**สามข้อนี้เกิดซ้ำมากกว่าหนึ่งครั้ง** จึงเขียนแยกไว้ — รายละเอียดอยู่ใน `TRAPS.md`

1. **เรื่องสีและขนาดต้องดูบนจอจริงก่อนเคาะ** — `--faint` กลับมติรอบสองหลังใช้บนมือถือ ·
   สีของ event กลับมติในเย็นวันเดียวกันหลังเห็นของจริง · การ์ดคาบถัดไปกับขนาดวันที่
   ก็กลับในวันเดียวกัน · **ภาพเทียบบนจอคอมกับของจริงบนมือถือคือข้อมูลคนละชุด**

2. **ของที่ "ถูกบนจอ ผิดข้างใน และเงียบ" คือศัตรูตัวจริง** — `update` ที่ไม่โดนแถวรายงานว่าสำเร็จ ·
   CHECK ที่ได้ผล NULL ถือว่าผ่าน · ตัวกรอง Area ที่ผูกกับชื่อแล้วชื่อเปลี่ยน ·
   ชนิดใน schema ของ tool ที่ไม่ตรงกับตัวอ่าน · ทุกข้อไม่มี error ให้ใครเห็น

3. **การจูน prompt ต้องวัดก่อนและหลัง** — การแก้ที่ทำให้เคสเป้าหมายผ่านแล้วทำเคสข้างเคียงพัง
   มองไม่เห็นเลยถ้าวัดแต่เคสที่ตั้งใจแก้ · และ **กฎที่มีแต่ "ห้าม" โดยไม่บอกทางเดินต่อ
   จะถูกใช้กว้างเกินเสมอ**

---

## หลักการ UX ห้าข้อ (ใช้ตัดสินทุกครั้งที่ลังเล)

1. เปิดมาต้องตอบได้ทันทีว่าตอนนี้ต้องทำอะไร — โครงสร้างสามชั้นเป็นวิธีเก็บ ไม่ใช่วิธีดู
2. สีมีความหมายเดียว ใช้ตรงกันทั้งระบบ ไม่มีสีไหนใช้เพื่อความสวย
3. งานที่ทำบ่อยต้องกดน้อยที่สุด
4. ปฏิทินไว้ดู คลังไว้แก้ — ไม่พยายามให้ทุกหน้าทำได้ทุกอย่าง
5. **ไม่มีอะไรหายเงียบ ๆ** — ลบต้องยืนยัน · archive กู้คืนได้ · ผู้ช่วยเขียนอะไรต้องบอก

---

## สิ่งที่ตั้งใจไม่ทำ

ลากย้ายในปฏิทิน · reminder เตือนซ้ำ · งานย่อยใน task · ไฟล์แนบ · ซิงก์ Google Calendar ·
แชตหลายห้อง · ค้นประวัติแชต · ให้โมเดลเห็นรูป · แชร์บทสนทนา ·
เสียงทำงานตอนล็อกหน้าจอ · อ่านออกเสียงคำตอบในโหมดแชต

> ~~โหมดหลายคน~~ **ทำแล้ว 8 ก.ย. 2026** — แต่คนละเรื่องกับ *การใช้ข้อมูลร่วมกัน*
> ซึ่งยังไม่ทำและไม่มีแผน · แต่ละบัญชีเห็นเฉพาะของตัวเอง ไม่มีการแชร์ตารางกัน

---

## สิ่งที่ยังไม่ได้ทำ

> **นี่คือรายการงานค้างที่เดียวของโปรเจกต์** — ไม่มีสำเนาที่ไหนอีก

- **แผงแก้ในชั้นสอง (ปุ่ม "แก้") ยังแก้ได้แค่ชื่อกับเวลา** — การย้ายวิชาต้องพูดเอา
  ซึ่ง `propose_update_draft` รับได้แล้ว แต่ปุ่มยังไม่มีช่องนั้น

- **`UXUI-LIGHT.html` ธีมสว่าง 11 จอ** — เป็นข้อเสนอ ยังไม่ได้ลงมือ
  · **เป็นการกลับมติ "ธีมมืดอย่างเดียว"** ใน DECISIONS.md ต้องเคาะก่อนทำ

- **เวลางาน UniHack 2026** ที่เป็น onsite ตั้ง 09:00 ไว้ชั่วคราว รอเวลาจริง

- **กติกาข้อ 4 ใน `lib/ai/prompt.ts` เป็นข้อความตายแล้ว** — มันสั่งให้พูดถึง `hidden`
  ซึ่งเป็น 0 เสมอตั้งแต่ถอดตัวกรอง Area · ยังไม่ลบเพราะ**การแตะ `prompt.ts` ต้องรัน
  ชุดวัดกับโมเดลจริงก่อนและหลัง** ซึ่งกินโควตา · ปล่อยไว้ไม่มีผลเสีย ทำตอนแตะ prompt ครั้งหน้า

- **ยังไม่มี "ลืมรหัสผ่าน"** — เจ้าของเลือกเอง (8 ก.ย. 2026) มีคนไม่กี่คน
  ทักมาแล้วรีเซ็ตให้ใน Supabase dashboard ได้

- **ต้องต่อ SMTP ของตัวเองก่อนมีคนสมัครจริงหลายคน** — ตัวส่งเมลของ Supabase เอง
  จำกัดไว้ระดับไม่กี่ฉบับต่อชั่วโมง ตั้งใจให้ใช้แค่ตอน develop · เกินเพดานแล้ว
  **เมลยืนยันไม่ถึงแบบเงียบ ๆ** ซึ่งแปลว่าสมัครแล้วเข้าใช้ไม่ได้เลย

- **ไม่มีเพดานโควตา AI รายคน** (เจ้าของเคาะ) — คนเดียวคุยรัวจนหมดโควตาวันนั้น
  คนอื่นใช้ไม่ได้ทั้งวัน · ตอนนี้บรรเทาด้วยข้อความที่บอกความจริงเท่านั้น

- **ปุ่มล้างประวัติในตั้งค่า ลบถาวรทันที ไม่เก็บเข้าคลัง** — ตั้งใจ (เจ้าของเคาะ 1 ก.ย. 2026)
  · ตั้งแต่ 5 ก.ย. 2026 มีหน้า `/kevin/history` ให้เห็นก่อนว่ามีอะไรอยู่

- **`doc/CLAUDE-PROJECT-PROMPT.md` กับ `doc/SCHEMA.sql` ต้องเอาไปวางใน Claude Project
  ด้วยมือ** — แก้ไฟล์ในโปรเจกต์แล้วของจริงบน claude.ai ไม่เปลี่ยนตาม

### `next build` ล้มบนเครื่องนี้เครื่องเดียว — **ไม่กระทบการ deploy · อย่าเสียเวลาไล่**

`InvariantError: Expected workStore to be initialized` ตอน prerender หน้าที่เป็น static
(`/login` · `/_global-error` · `/_not-found`) · Next พิมพ์เองว่า "This is a bug in Next.js"

เกิดเหมือนกันทั้ง turbopack และ webpack · เกิดบน master ที่ยังไม่มีของใหม่ ·
ถอด `CallProvider` ออกก็ยังเกิด (แค่ย้ายไปล้มที่ `/login`)
· **ยืนยันแล้วว่า build บน Vercel ผ่าน** ด้วย preview deploy 5 ก.ย. 2026 — ขั้น
"Generating static pages (4/4)" ที่ล้มบนเครื่องนี้ ผ่านที่นั่นใน 179ms

**เช็คของก่อน deploy ให้ใช้ `npx tsc --noEmit` + `npm test` + `npm run lint` ตามเดิม**
· `next build` ไม่ได้รัน eslint อยู่แล้ว จึงไม่มีอะไรบล็อก deploy ให้เลย — ต้องรันเอง
